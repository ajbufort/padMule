[package]
name = "mule-engine"
version = "0.0.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
mule-proto = { path = "../mule-proto" }

[dev-dependencies]
hex = { workspace = true }
