[package]
name = "mule-files"
version = "0.0.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
mule-proto = { path = "../mule-proto" }

[dev-dependencies]
hex = { workspace = true }
