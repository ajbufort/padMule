# padMule Wiki - Log

Append-only, timestamped record of Ingest / Query / Lint passes.

- 2026-07-12 Ingest: project bootstrapped. Extracted upstream amule-3.0.1 zip,
  surveyed layout/build options, wrote [[arch-upstream-amule]]. Ingested
  Anthony's references (eMuleAI GitHub, eMule-Board threads, aMule docs site)
  into [[ref-ecosystem]]. FinalWord-style system installed: CLAUDE.md schema,
  kb-reflect Stop hook, seeded memory.
- 2026-07-12 Ingest: locked decision - engine strategy = Rust rewrite (SwiftUI shell, aMule C++ as reference oracle); wrote [[decisions-and-lessons]], updated index.
- 2026-07-12 Ingest: iPadOS constraints research (2 workflows, adversarially verified) -> docs/raw/ipados-constraints-research-2026-07-12.md + wiki [[ipados-constraints]]. Load-bearing: foreground-only engine, sockets fine, free-team sideload limits, part-file storage plan.
- 2026-07-12 Ingest: upstream recon workflow (5 subsystems, 2.4M tokens, all high-confidence) -> docs/raw/amule-upstream-reference-2026-07-12.md (1746 lines) + wiki [[protocol-reference]]. Key: PARTSIZE 9728000, eD2k part count floor+1 (not ceil), kad UDP overhead 16B, clients.met 119B, ports 4662/4672/4712, this tree is a MODIFIED aMule (wire=upstream, policy may differ).
- 2026-07-12 Ingest: Wave 1 landed - mule-proto crate + verified ed2k_hash (single/multi-part + exact-multiple edge case), 7 tests green. Wrote [[build-progress]]. Design spec + Wave-1 plan committed.
- 2026-07-12 Ingest: Wave 1b landed - mule-proto io (LE reader/writer) + tag (MET-format eD2k tag codec, byte-preserving), 17 tests green. Also noted emule-security.org as fixture/bootstrap source in [[ref-ecosystem]]. Updated [[build-progress]].
- 2026-07-12 Review+Lint: adversarial review of Wave 1/1b (3 dims + docs self-audit). Hashing/endianness/tag-layout/panic-safety CONFIRMED faithful vs aMule C++. Applied corrections (length capping, BOOL/BOOLARRAY, docs) -> 20 tests green. Recorded deliberate divergences in [[decisions-and-lessons]]. No index orphans; docs ASCII-clean.
- 2026-07-12 Trivial: rustfmt-only reflow of the BoolArray variant (no behavior change); no KB impact.
- 2026-07-12 Ingest: Wave 2a+2b - new mule-files crate; byte-compatible server.met + known.met (read/write, header preserved, generic tag/hash/record preservation). 10 mule-files tests. Updated [[build-progress]]. Remaining: part.met + nodes.dat (2c).
- 2026-07-12 Ingest: Wave 2c - byte-compatible part.met + gap-list helpers (gaps()/gap_tags(), start-inclusive/end-exclusive, index-paired). 17 mule-files tests. nodes.dat moved to Wave 6. Updated [[build-progress]]. Wave 2 (.met formats) complete except nodes.dat.
- 2026-07-12 Study+Ingest: deep verified protocol study (5 areas, eD2k server/client + Kademlia algo + Kad2 + open questions) -> docs/raw/protocol-understanding-2026-07-12.md + wiki [[protocol-understanding]]. Caught 2 recon-doc errors (Kad tolerance 2^120 not 2^104; server UDP send +4 not +3). Fixed server-UDP nuance in [[protocol-reference]]. Resolved Wave-3 open Qs (IP order, VBT never, capability gating, keyword hash, ports).
- 2026-07-13 Ingest: Wave 3a - eD2k/eMule TCP packet framing + zlib in mule-proto (streaming read_packet, write_packet, compress/decompress; flate2 miniz_oxide backend). 31 mule-proto tests. Wave 3 decomposed 3a-3d in [[build-progress]].
- 2026-07-13 Ingest: added reference source oracles (eMule 0.50a/0.70b, aMule master) under refs/ (gitignored) -> wiki [[ref-source-trees]]. RESOLVED the "modified tree" caveat: our amule-3.0.1 is faithful aMule (GetMaxSlots N_FLOOR=20 + ALPHA_QUERY=5 byte-identical in aMule master); those are aMule-vs-eMule policy, wire-neutral. CONFIRMED wire landmines canonical in eMule 0.50a (userhash markers, Kad crypt 16B, MAGICVALUE_UDP_SYNC, SetValueBE). Corrected caveat in [[protocol-reference]] + [[decisions-and-lessons]].
- 2026-07-13 Analysis: "can we avoid the background pause?" Answered - not strictly unavoidable; sideloading unlocks the audio/location keepalive (best-effort, jetsam-killable, <100MB bg). Tiered background-persistence plan (keepalive / seedbox mode / BGContinuedProcessingTask / BGProcessingTask); v1 stays foreground-only + clean pause/resume as the required fallback. Recorded in [[lifecycle-and-reactivation]] + [[decisions-and-lessons]].
- 2026-07-13 Ingest: Wave 3b - mule-engine crate + login-handshake message codecs (build_login_request with exact aMule tags/flags/version, parse_id_change/server_message/server_status), grounded in ServerConnect/ServerSocket source. 8 tests. Also recorded the "can we avoid the pause?" analysis (see earlier log line). Search codecs = 3b-2.
- 2026-07-13 Ingest: Wave 3b-2 - server search codec (build_search_request ANDed-terms expression per CSearchExprTarget; parse_search_result) + parse_server_list/parse_server_ident. Network parsers avoid untrusted-count prealloc. 15 mule-engine tests (63 workspace). Deferred: boolean tree OR/NOT, global UDP search, OP_FOUNDSOURCES.
- 2026-07-13 Ingest: Wave 3c-1 - async packet framing (FramedStream over tokio AsyncRead+AsyncWrite) + server login handshake (ServerState/ServerEvent event stream, login_handshake, connect_server). Added tokio (iOS-safe). Mock-server tested: HighID/LowID/rejection/packed-reply/split-read. 21 mule-engine tests (69 workspace). Live smoke + pause/resume ServerLink = 3c-2. This is the first LIVE-capable networking layer.
- 2026-07-13 Ingest+LiveRun: Wave 3c-2 - ServerLink (connect/pause/resume over real loopback socket, honest State events) + mule-cli (login/login-any). 23 mule-engine tests. LIVE RUN: parsed 2 real server.met files (25 + 27 servers) via mule-files (validates against real files!), correct IP decode + state machine, but all eD2k-port connections timeout/unroutable here (P2P ports filtered or servers dead) while HTTP works - environment limit, not a code bug. Real end-to-end proof -> Wave 4 client-to-client vs local amuled, or the iPad later. See [[build-progress]].
- 2026-07-13 Ingest: Wave 3d - client-to-client peer HELLO codec (build_hello/build_hello_answer, baseline MISCOPTIONS1/2 byte-verified vs BaseClient.cpp = 0x34103212/0x438, parse_hello + Capabilities decode). VarInt tags force UINT32 since VBT never advertised. Pivoted from get-sources (server-blocked here) to the locally-testable peer protocol. 28 mule-engine tests (74 workspace). Crypt/captcha off until later waves.
- 2026-07-13 LIVE VALIDATION: padMule logged into a REAL eD2k server (45.87.41.16:6262 via http://upd.emule-security.org/server.met), LowID (NATed box), full connect/pause/resume/reconnect lifecycle worked. CORRECTED earlier wrong finding: WSL2 env does NOT block P2P ports (arbitrary outbound + UDP work); earlier failures were stale server lists. Recorded eMule-vs-aMule format notes (server.met same format; BOM strings display-only; SO_AMULE advertised). Updated [[build-progress]], [[ref-ecosystem]], [[decisions-and-lessons]] + memory.
- 2026-07-13 Ingest: recorded "Server Hunter" feature request (Anthony) as BACKLOG -> wiki [[feature-server-hunter]] + memory. Responsible design (auto-update from trusted URL / verify / OP_SERVERLIST gossip crawl); flagged that literal whole-net scanning is infeasible on iOS + abuse-prone + unnecessary. Also noted eMule addresses.dat auto-update model. Added index backlog section.
- 2026-07-13 Ingest: Wave 4a - client-to-client peer connection + inbound listener (peer_handshake_outbound/inbound, connect_peer/accept_peer). Two engines complete the hello handshake on loopback. accept_peer is the HighID listener prerequisite. 30 mule-engine tests (76 workspace).
- 2026-07-13 Networking: diagnosed HighID block for the WSL2 dev box. Source = WSL2 NAT + nothing configured (NOT AVG). Public <public-ip> (single Comcast router 10.0.0.1, host 10.0.0.33, no CGNAT). Prepared: mirrored-mode /mnt/c/Users/ajbuf/.wslconfig (backed up .bak-padmule-2026-07-13; needs wsl --shutdown), firewall script /mnt/c/Users/ajbuf/Downloads/padmule-firewall.ps1 (run elevated). Router forward (Anthony): TCP 4662 + UDP 4672 -> 10.0.0.33. netsh portproxy is TCP-only (hence mirrored for UDP). HighID also needs padMule to LISTEN (accept_peer, now built).
- 2026-07-13 Ingest: Wave 4b - download-side transfer message codecs (request_filename/setreqfileid/startupload/hashset build; file-status part bitfield decode+encode; request_parts 3-block u32/u64 ends-exclusive; sending_part parse; queue-ranking/hashset-answer parse). Byte-verified vs DownloadClient.cpp/PartFile.cpp. 37 mule-engine tests (83 workspace). Also added mule-cli listen command (Wave 4a) for HighID forward validation. Next 4c = transfer state machine + .part writing (two engines + local amuled).
- 2026-07-13 MILESTONE: Wave 4c - FIRST END-TO-END FILE TRANSFER between two padMule engines (download_file + serve_file). Two engines transfer a 400KB 3-block file on loopback; ed2k hash matches byte-for-byte. Exercises request->file-status->slot->3-block-request-loop->assemble. transfer.rs now bidirectional (upload builders + parse_request_parts + RequestedBlocks alias). FramedStream::read_packet_unpacked added. 40 mule-engine tests (86 workspace). Next: real .part file, multi-part+hashset, differential vs local amuled.
- 2026-07-14 MILESTONE + Ingest: **HIGHID ACHIEVED.** padMule got `Connected { id: <client-id>, low_id: false }` from the live server 45.87.41.16:6262 with `mule-cli listen 4662` running. id <client-id-hex> decodes LE -> <public-ip> = our public IP (confirms the client-ID decode against real server software). Listener logged the server's connect-back from the internet (45.87.41.16:49144); pause/resume kept HighID. All 5 inbound links validated: router forward -> DHCP reservation (10.0.0.33 pinned to MAC <nic-mac>) -> Windows Firewall -> Hyper-V firewall (mirrored-mode trap: DefaultInboundAction=Block, host rules mirrored in) -> WSL networkingMode=mirrored (required: portproxy is TCP-only, Kad needs UDP 4672). Observed: the server's HighID test is a bare TCP connect+close, no eD2k HELLO - a successful accept suffices. Xfinity gateway supports NAT hairpin (cheap self-test). New wiki entry [[net-highid-and-port-forwarding]]; updated [[build-progress]] + index. NOTE: this is a DEV-BOX fix - the iPad needs UPnP/NAT-PMP (raises Wave 7 priority) and will be LowID on cellular/CGNAT regardless. 88 workspace tests still green under mirrored networking.
- 2026-07-14 Ingest: Wave 4d (upload side, credits, source exchange, part files). 4 parallel source-grounded agents -> docs/raw/wave4d-upstream-research-2026-07-14.md. Built: clients.met byte-compat codec (119-byte records; the 64-bit totals are split into NON-adjacent lo/hi dwords with last_seen between them), credit formula (min(2*down/up, sqrt(down/1MiB+2)) clamped [1,10]; threshold is a DECIMAL MB while the sqrt term is BINARY - not a typo), upload queue (score = waited * credits * priority; friend-slot short-circuits BEFORE the banned/uploading checks; N_FLOOR=20 slots; 1h/10MiB session limits; 12-byte OP_QUEUERANKING), source exchange (SX1/SX2, records v1-v4, the v3 id byte-order flip, 354-byte zlib gate, 501 cap), get-sources + LowID callback, and PartFile (gap list, lattice-bounded block allocation, whole-part re-gap on corruption). DELIBERATELY FIXED 4 aMule bugs instead of replicating them (exactly-PARTSIZE file never verifies; standalone OP_REQUESTSOURCES2 always throws; SX id order gated on the wrong version; OBFU userhash flag is 0x80 not 0x08) - see [[build-progress]] Wave 4d notes. 158 workspace tests (was 88). REMAINING for the wave gate: disk-backed .part I/O, multi-source driver, differential vs a local amuled. AICH recovery correctly deferred to Wave 1c (it is an optimisation; basic corruption handling needs only the hashset).
- 2026-07-14 Lesson: our own research pass reported the SX record sizes as 14/30/31; they are 12/28/29 (upstream's checks are literally nCount*(4+2+4+2)[+16][+1]). Because SX1 resolves the record version BY PACKET SIZE, shipping the wrong table would have made padMule reject every real source-exchange answer. A byte-exact test caught it in minutes. Recorded in [[decisions-and-lessons]]: agent-derived constants are a HYPOTHESIS until a test pins them against real bytes - always assert the byte layout, never just the field list.
- 2026-07-14 Review+Fix: Wave 4d adversarial multi-agent review (8 dimensions, 3 skeptics/finding, ~40 agents). 6 real defects, ALL fixed and cross-checked against eMule 0.50a as the wire authority. 3 HIGH & remotely-triggerable (none catchable padMule-to-padMule): copy_from_slice panic on over-long block; zero-length block hang; OP_COMPRESSEDPART (0x40/0xA1) silently dropped -> every compressible real-network download hangs (padMule advertises data_comp=1). Fixed via one hardened BlockReceiver both download loops route through (per-block streaming zlib inflate matching eMule ProcessBlockPacket DownloadClient.cpp:1201; guards for zip-bomb/short-stream/wrong-file/bad-range). 3 MEDIUM: needs_hashset skipped the hashset for an exactly-PARTSIZE file -> unverified accept; large-file boundary was u32::MAX not OLD_MAX_FILE_SIZE=4_290_048_000 (eMule OLD_MAX_EMULE_FILE_SIZE, same value) in build_get_sources + part_store .met. Added OLD_MAX_FILE_SIZE to mule-proto. 180 tests (was 166), 14 new regression tests. Anthony reinforced: eMule is the wire source of truth - confirm every wire fix against it (recorded in [[decisions-and-lessons]]).
- 2026-07-14 Build: amuled oracle build deps installed (cmake + wx3.2.4 + crypto++ + zlib via Anthony's sudo); daemon-only configure now blocks only on libboost-dev (find_package(Boost CONFIG REQUIRED); eMule/aMule use header-only asio). scripts/build-amuled-oracle.sh ready. Differential test vs amuled is the remaining Wave 4 gate.
- 2026-07-14 MILESTONE - WAVE 4 GATE MET: differential test vs REAL amuled 3.0.1 PASSES. padMule downloads a 600KB compressible file (exercises OP_COMPRESSEDPART, the Wave-4d review fix) AND a 400KB random file (raw OP_SENDINGPART) from a headless amuled, byte-for-byte, hashset-verified. Confirmed our ed2k hash == amuled's known.met hash byte-for-byte (independent validation of the hasher). Built amuled via scripts/build-amuled-oracle.sh (daemon-only; deps cmake/wx3.2/crypto++/zlib/boost/pkg-config/glib; optional GeoIP/UPnP/BFD/NLS off). Upstream ctest (11) passes = codec cross-check. INTEROP BUG the differential test caught (invisible to 166 padMule-to-padMule tests): we advertise ExtendedRequestsVersion=2 but sent a bare 16-byte OP_REQUESTFILENAME -> aMule ProcessExtendedInfo (UploadClient.cpp:193) throws + disconnects. Fixed via build_request_filename_ext (appends part-count 0 + complete-sources 0, per aMule SendFileRequest). Diagnosed with new mule-cli peer-probe wire-tracer. 180 tests green. New mule-cli cmds: hash-file, peer-download, peer-probe. build-oracle/ gitignored. Lesson recorded in [[decisions-and-lessons]]: advertise no capability you do not honour.
- 2026-07-14 Differential coverage extended. DOWNLOAD: padMule downloads a 15MB / 2-part file from real amuled byte-for-byte (OP_HASHSETREQUEST/ANSWER + per-part MD4 verify vs amuled's real hashset, mixed compressed+raw); added to differential-test.sh. UPLOAD: new `mule-cli serve-file` (padMule as uploader, computes hashset, serves). Real amuled CONNECTS to padMule's listener (reachability + accept proven) but the full pull is blocked by amuled ORCHESTRATION (not padMule): amuled rejects loopback 127/8 + LAN sources via IsGoodIP unless served on 10.0.0.33 with FilterLanIPs=0; amuled attempts an OBFUSCATED client handshake by default (Wave 5); amuled rewrites amule.conf on startup (chmod read-only to pin edits) and skips a link source if a leftover .part exists; and its offline link-source dialing is irregular. serve() itself is proven padMule-to-padMule. Revisit amuled-pull after Wave 5. Details in [[build-progress]] + [[padmule-amuled-oracle]]. 180 tests.
- 2026-07-14 Wave 5 research: 4 parallel source-grounded agents over eMule 0.50a (WIRE AUTHORITY) + aMule 3.0.1 -> byte-exact spec docs/raw/wave5-crypto-research-2026-07-14.md. Covers TCP obfuscation (EncryptedStreamSocket: initiator handshake [semi-random marker | u32 randomkey | RC4(SYNC 0x835E6FC4 | methods | padlen | pad)], key = MD5(target_userhash[16] | magic{34 req->resp / 203 resp->req} | randomkey[4]) -> RC4 drop 1024; receiver auto-detects via byte[0] in {0xE3,0xD4,0xC5}=plaintext), UDP obfuscation (8-byte / Kad 16-byte header, MAGICVALUE_UDP_SYNC_CLIENT 0x395F2EC1, no RC4 discard), secure ident (RSA 384-bit PKCS1v15-SHA1, 48-byte sig, cryptkey.dat = base64(DER private key); sign the OTHER peer's pubkey || challenge; OP_PUBLICKEY 0x85 / OP_SIGNATURE 0x86 / OP_SECIDENTSTATE 0x87), and negotiation (MISCOPTIONS2 bits 7/8/9 supported/requested/required; gate: obfuscate outgoing when peer supports+we support AND peer-requests-or-we-request). aMule byte-identical on the wire. Slices: 5a TCP obf, 5b UDP obf, 5c secure ident, 5d wiring + obfuscated differential gate vs amuled (also unblocks the uploader-pull test). Advertise support + accept both + obfuscate on request.
- 2026-07-14 MILESTONE - Wave 5a TCP OBFUSCATION VALIDATED vs real amuled 3.0.1. Implemented RC4 (mule-proto/rc4.rs, pinned to standard vectors) + obfuscation key derivation (obf.rs: MD5(target_userhash | magic{34 requester / 203 responder} | randomkey[4 LE]) -> RC4 drop 1024) + initiator handshake byte layout + FramedStream optional-cipher integration (decrypt reads / encrypt writes in order) + obf_handshake.rs (obf_initiate / obf_accept auto-detect) + connect_peer_obf / accept_peer(auto-detect). padMule completed the full RC4 handshake with amuled and downloaded single-part (600KB) AND multi-part (15MB, hashset over the encrypted stream) byte-for-byte. LANDMINE (differential test caught it): the obfuscation key uses the peer's MARKED userhash (byte[5]=14, byte[14]=111 forced - the hello/source-exchange hash), NOT amuled's raw preferences.dat hash (differs at exactly those 2 bytes); wrong hash = silent SYNC-check failure. Recorded in [[decisions-and-lessons]] + memory. 198 tests. Remaining Wave 5: 5b UDP obf (with Kad/Wave 6), 5c secure ident RSA, 5d server-connect obf + gating + amuled uploader-pull (now unblocked).
- 2026-07-14 Wave 5c secure identification (RSA) DONE + interop-validated vs real amuled. mule-engine/secure_ident.rs: RSA-384 PKCS1v15-SHA1 (48-byte sig), Identity keygen + cryptkey.dat, sign_v1 (signs PEER pubkey || challenge u32 LE) / verify_v1 (mirror: our pubkey || challenge-we-issued, checked with peer key), OP_PUBLICKEY 0x85 / OP_SECIDENTSTATE 0x87 / OP_SIGNATURE 0x86. LANDMINE: aMule cryptkey.dat is base64(PKCS#8 PrivateKeyInfo), NOT raw PKCS#1 - Crypto++ InvertibleRSAFunction::DEREncode emits the PKCS#8 wrapper (AlgorithmIdentifier + rsaEncryption OID). The wire PUBLIC key stays PKCS#1 RSAPublicKey (~58 bytes, < 80 cap). Integration test (tests/secure_ident_interop.rs) loads a REAL amuled cryptkey.dat fixture and uses the key - proves our RSA/DER matches Crypto++. 210 tests. New deps rsa/sha1/base64/rand. Remaining Wave 5: 5b UDP obf (Wave 6/Kad), 5d live secure-ident state machine + credit tie-in + server-connect obf + gating.
- 2026-07-14 MILESTONE - Wave 5d secure-ident live state machine VALIDATED vs real amuled 3.0.1. SecureIdentSession (mule-engine/secure_ident.rs) drives the mutual RSA challenge/response mirroring aMule (BaseClient.cpp handlers + ClientTCPSocket.cpp:1249 dispatch): KEYANDSIGNEEDED sends pubkey then defers signature until peer's key arrives. mule-cli sec-ident. padMule VERIFIED real amuled's signature ("peer PASSED secure identification") and completed its half. LANDMINE #2 (differential caught it, after the cryptkey PKCS#8 one): the ON-WIRE public key (OP_PUBLICKEY 0x85) is SubjectPublicKeyInfo (X.509 SPKI, 76 bytes, AlgorithmIdentifier + rsaEncryption OID), NOT the ~58-byte raw PKCS#1 RSAPublicKey the spec claimed - PKCS#1 makes verification silently fail. So: cryptkey.dat=PKCS#8, wire pubkey=SPKI, PKCS#1 nowhere on the wire. HARNESS GOTCHA: aMule remembers a userhash's pubkey across connections (sends SIGNATURENEEDED not KEYANDSIGNEEDED); a fresh Identity needs a fresh peer clients.met. 212 tests. Both Wave-5 crypto subsystems (obfuscation + secure ident) now fully interoperate with genuine aMule. Remaining 5d: credit tie-in, server-connect obf, gating, transfer-session integration, uploader-pull.
- 2026-07-14 Wave 5d policy layer DONE. (1) Credit tie-in (credits.rs): IdentState enum + resolve_ident_state (verified identity bound to its IP; same userhash from another IP = IdBadGuy, per ClientCredits.cpp:219-231) + score_ratio_ident (denies the credit bonus to a key-bearing peer that is unverified/failed/replaying, only when WE have crypto; keyless + verified peers get the normal ratio). This is what makes secure ident MEAN something - a stolen userhash cannot inherit credits. (2) Obfuscation gating (crypt_policy.rs): should_obfuscate_outbound (need peer hash + both support + peer-requests-or-we-request) + should_reject (hard requirement mismatch), default prefs match amuled (supported+requested, not required). (3) serve-file now auto-detects obfuscation (obf_accept) so padMule serves both plaintext and obfuscated downloaders. 221 tests. UPLOADER-PULL vs amuled: serve side obf-aware + proven P2P + amuled reaches it, but the full amuled-downloads-from-padMule run stays blocked by amuled's unreliable OFFLINE link-source dialer (queues the download, does not reliably dial the link source) - amuled orchestration, not padMule. REMAINING 5d (integration, lower-risk): secure-ident + gating driven during a live transfer, server-connect DH obfuscation.
- 2026-07-14 Wave 5d live-transfer secure-ident wiring DONE. run_secure_ident (mule-engine/secure_ident.rs) drives the mutual RSA exchange as a bounded phase right after the hello, before any file request. Composes cleanly with the transfer: each side sends its signature before any transfer packet and completes on receiving the peer's, so the phase returns before transfer data flows (no interleaving, no dropped packets - traced + tested). Integration test: two engines do hello -> mutual secure ident -> file transfer on ONE connection; both verify each other AND the file transfers byte-for-byte. 222 tests. Closes the secure-ident wiring (the exchange validated standalone vs amuled now sits inline with a real transfer).
- 2026-07-14 Wave 6 (Kad) research kicked off: 5 parallel source-grounded agents over eMule 0.50a kademlia/ (nodes.dat format + CUInt128, Kad UDP protocol/opcodes, routing table/XOR/buckets, source+keyword search, UDP obfuscation=5b). Real nodes.dat fixture saved at crates/mule-files/tests/fixtures/nodes.dat (6098 bytes, v2, 179 contacts). Slice plan: 6a nodes.dat+routing (pure, has fixture), 6b UDP framing+obf+bootstrap/hello, 6c iterative lookup, 6d source search + differential gate (join real Kad, resolve a hash).
- 2026-07-14 Wave 6 (Kad) research DONE (5 agents, eMule 0.50a Kademlia) -> docs/raw/wave6-kad-research-2026-07-14.md (byte-exact: CUInt128 encoding, nodes.dat all versions, Kad2 opcode table, BOOTSTRAP/HELLO/FIND_NODE/SEARCH_SOURCE layouts, routing bin-tree + 2^120 tolerance, iterative lookup, Kad UDP obfuscation=5b). Resolved a report conflict: tolerance is 2^120 (0x01000000 in chunk0), not 2^104. eMule-vs-aMule wire diff: KADEMLIA_VERSION byte eMule 0x09 vs aMule 0x08. BOOTSTRAP_REQ is EMPTY (corrects older recon).
- 2026-07-14 Wave 6a STARTED + real-fixture-validated. mule_proto::Kad128 (CUInt128): 4 u32 words MSW-first, XOR distance, MSB-first bit(), 2^120 tolerance; CANONICAL from_hash/to_hash kept STRICTLY separate from the raw wire/disk from_wire/to_wire (each dword byte-reversed: H3 H2 H1 H0 | H7 H6 H5 H4 | ... - reading raw hash as a Kad ID targets the wrong node, the classic Kad bug). mule_files::nodes_dat byte-compat reader/writer (v2 header 0|version|count + 34-byte records: KadID 16 + IP 4 + UDP 2 + TCP 2 + version 1 + UDPkey 8 + verified 1). Interop test parses the REAL 6098-byte nodes.dat -> 179 contacts, first contact canonical ID 8fb8d05c... IP 226.80.49.93 ports 4672/4662 version 8 verified, AND re-serializes BYTE-IDENTICAL. Proves our CUInt128 wire encoding matches the real network. 240 tests. REMAINING 6a: routing bin-tree + k-buckets.
- 2026-07-15 Wave 6a COMPLETE. New crate mule-kad with the Kad routing table (RoutingTable): binary tree of zones, each leaf a bin of K=10 contacts; descend by XOR-distance bit (bit 0 = closer half); split only if shallow (level<KBASE=4) or near self (index<KK=5) and not past MAXLEVELS=127 (eMule CanSplit); full unsplittable far bin drops the contact (caps far capacity). closest_to(target,n) = the n nearest by XOR (the lookup candidate set). Test loads the real 179-contact nodes.dat -> retains ~142 (rest overflow far bins, correct since saved vs a different self_id), tree splits beyond one bin, closest-lookups XOR-sorted. 240 tests. Next 6b: Kad UDP framing (0xE4/0xE5) + obfuscation (5b, 16-B overhead, no RC4 discard) + bootstrap(empty req)/hello handshake; differential gate = talk to a real Kad bootstrap node.
- 2026-07-15 Wave 6b codecs DONE (source-verified + adversarial-reviewed). mule-kad gains frame.rs / udp_obf.rs / message.rs for the Kad UDP wire. Every layout verified against amule-3.0.1 source (EncryptedDatagramSocket.cpp, KademliaUDPListener.cpp SendMyDetails/AddContact2/Bootstrap, SafeFile.cpp WriteTag, Tag.h CTagVarInt), not the research summary. KEY FACT: aMule ENDIAN_SWAP_32 == wxUINT32_SWAP_ON_BE == no-op on our LE target, so the sentinel (0x395F2EC1) and both 4-byte verify keys are plain little-endian on the wire == our to_le_bytes(). 3 bugs found+fixed: (1) is_protocol_byte used the TCP set {E3,C5,D4,E4,E5,F4,F5}; correct UDP set is {C5,D4,E4,E5,A3,B2} - old set dropped inbound 0xF4-marked Kad packets and could emit 0xB2 that a real aMule drops; (2) deobfuscate must reject a zero-payload Kad packet (eMule `if result<=8`) BEFORE learning verify keys, not after; (3) pack threshold is PAYLOAD>200 (GetPacketSize excludes the opcode/header), not frame>200 - was an off-by-2 over-compressing at payload 199/200 [caught by the adversarial-review workflow]. Refuted (correctly): flate2/miniz_oxide != aMule zlib byte-for-byte, but both inflate identically -> interop-safe, not a wire defect (only matters for a future outbound-byte-exact oracle compare). 264 tests. Next 6c iterative FIND_NODE, then the live UDP gate.
- 2026-07-15 Wave 6c DONE (codec + pure lookup) + source-verified. mule-kad message.rs gains KADEMLIA2_REQ (0x21: type&0x1F|target 16|receiver 16 = 33B; receiver must == recipient KadID) and KADEMLIA2_RES (0x29: target 16|count u8|count x 25B contact; exact-len 17+25*count; Kad1 version<=1 dropped) - verified vs ProcessKademlia2Request/Response + Search.cpp SendFindValue. Request-type byte = GetRequestContactCount: FIND_VALUE 0x02, STORE 0x04, FIND_NODE/FIND_VALUE_MORE 0x0B. New lookup.rs: Lookup - deterministic iterative CSearch core; candidates BTreeMap<distance,contact> (XOR distance injective per id => sort key), next_queries(ALPHA=3, frontier=K) returns closest-untried and marks tried, on_response folds in closer contacts, converges when the frontier is fully queried; ALPHA_QUERY=3 (eMule; aMule=5, behavior-not-wire). Timers/JumpStart/reask + IP anti-poisoning dedup deferred to the live layer. Convergence test dogfoods the 6a RoutingTable as each sim node's knowledge (bucketed => navigable; a naive M-nearest graph strands greedy routing) -> converges to the exact k-closest over 160 nodes. 272 tests. NEXT: live UDP gate (real socket: BOOTSTRAP_REQ -> RES, HELLO 3-way -> IP-verified, self-lookup fills routing table), then 6d source search + the Wave-6 differential goal.
- 2026-07-15 WAVE 6 LIVE GATE PASSES. mule-engine::kad_live::KadNode (tokio UdpSocket) + mule-cli `kad-bootstrap <nodes.dat>` join the REAL Kad network: obfuscated BOOTSTRAP_REQ (NodeID-keyed) -> BOOTSTRAP_RES from a live v10 node (20 contacts, decrypted via the ReceiverKey path since the peer echoes the senderVerifyKey we issued) -> routing table seeded -> HELLO_REQ/HELLO_RES with a real peer. Advertising aMule version 0x08 is accepted by a v10 peer. Reliable across runs. LIVE-GATE BUG FOUND+FIXED: the contact IP byte order. eMule keeps a contact IP in HOST order (MSByte=first octet) and WriteUInt32s it LE, so mule-files read_u32 (LE) recovers the host-order value directly (95.236.36.250 -> 0x5FEC24FA); the dotted quad is the BIG-endian view = Ipv4Addr::from(ip), NOT to_le_bytes (which reversed it to the multicast 250.36.236.95 and dropped every packet). This was the research's UNSURE IP-endianness item; the live capture (first octets 224-250 = reserved) confirmed it. Locked with a regression test. The verify-key dance works because the same host-order u32 feeds udp_verify_key on both send(dest) and receive(source). Fresh nodes.dat fetched via curl http://upd.emule-security.org/nodes.dat (6302B, 185 v2 contacts). 274 tests. NEXT 6d: SEARCH_SOURCE_REQ/RES + resolve a known ed2k hash to sources (Wave-6 goal), driving the 6c lookup live.
- 2026-07-15 WAVE 6 (Kad) COMPLETE - GOAL MET LIVE. 6d adds the source search: SEARCH_SOURCE_REQ (0x34: target 16|startPos 2 &0x7FFF|fileSize 8 u64 = 26B) + SEARCH_RES (0x3B: responderID 16|keyID 16|count 2|count x {answer 16|taglist}); SearchResult::as_source pulls SOURCETYPE 0xFF/SOURCEIP 0xFE/SOURCEPORT 0xFD/SOURCEUPORT 0xFC and accepts eMule source types {1,3,4,5,6}. Verified vs Process2SearchSourceRequest/Process2SearchResponse/SendValidSourceResult. read_kad_tag RELAXED to eMule behavior: the tag name is a length-prefixed string (ReadTag: ReadString) - tolerate any name length (first byte = name id), only an unknown TYPE errors (can't skip unknown length). ed2k file hash -> Kad target via Kad128::from_hash (SetValueBE, confirmed vs CUInt128 ctor + PartFile.cpp:1661). Live driver kad_live: request() loops past interleaved stray datagrams until the awaited opcode arrives from the peer; find_node (KADEMLIA2_REQ FIND_NODE) + search_source drive resolve_sources (iterative 6c lookup toward the hash, then SEARCH_SOURCE_REQ to closest-in-tolerance nodes). mule-cli kad-search <nodes.dat> <hash> <size>. LIVE RESULT (reliable): bootstrap -> 15/16 real nodes answer FIND_NODE (lookup converges toward hash) -> 5/10 in-tolerance nodes return a SEARCH_RES -> resolves a real source (type 3 firewalled+buddy, real IP:port 49.118.243.134). 278 tests. Wave 6 done: routing + UDP wire/obfuscation + iterative lookup + source search, all validated live. NEXT: Wave 7 (mule-ec + CLI parity) or return to eD2k integration (5d live multi-source, server DH obfuscation).
- 2026-07-15 Wave 7 RE-SCOPED (user decision) from EC protocol to END-TO-END FETCH integration - "give a hash, get the file" in one driver. Rationale: the iPad UI seam is in-process FFI (Wave 8 = mule-ffi), NOT a separate EC daemon, so EC would be interop/dev-tooling only; wiring the validated pieces (Kad source search + server/peer source exchange + peer download) into one cohesive flow is higher value. mule-engine::fetch: PeerSource normalises a source across origins - THE place the two IP conventions are reconciled: Kad TAG_SOURCEIP is host-order (dotted quad = Ipv4Addr::from(ip), big-endian view; eMule does ED2KID=SWAP(ip) then displays low-byte-first, DownloadQueue.cpp:1592), server OP_FOUNDSOURCES is eD2k low-byte. Only HighID Kad types {1,4} + non-LowID (>=0x01000000) server ids are directly connectable; firewalled/callback skipped. SourceRegistry dedups by addr across backends. fetch_from_sources drives Download across sources (connect_peer_obf when userhash known else connect_peer, per-peer timeouts, skip dead/queued/no-file). mule-cli kad-fetch. 5 unit tests + loopback integration (serve_file peer -> fetch_from_sources -> complete -> ed2k hash verifies) + dead-source test. LIVE run: bootstrapped Kad, resolved a HighID source, completed an obfuscated handshake with a real internet peer (peers_connected 1/1), 0 bytes only because the degenerate empty-file hash isn't shared by that peer. Loopback proves the download half; live proves discovery+connection. 285 tests. EC protocol DEFERRED (optional interop). NEXT: Wave 8 (mule-ffi + iOS shell) needs Apple toolchain; or 5d live multi-source integration; or a real live fetch with a hash a HighID Kad seeder actually holds.
- 2026-07-15 LIVE MILESTONE: real eD2k search-to-download. New mule-cli `search-download <server.met> <keyword> <out>`: login -> keyword search -> rank results by advertised source count (TAG_SOURCES 0x15) -> probe OP_GETSOURCES per candidate until one has a connectable HighID source -> fetch_from_sources with RESUME across reconnects -> verify ed2k hash. Live run: logged into 45.87.41.16:6262 (LowID), searched 'pdf' -> 200 results parsed correctly, found 'Practical Electronics 2026-07.pdf' (14MB) with a HighID source, DOWNLOADED REAL BYTES 0->575K->1.15M->1.78M over 6 resumed rounds (13%). Progress persisted across reconnects (disk-backed .part). Incomplete only because one eD2k source serves ~575KB bursts then queues us (upload-slot rationing) - completing needs multiple HighID sources or queue-position/reask handling (a real download manager). Proves the full pipeline end-to-end vs the real network: server login + search + get-sources + connect + transfer + resume. Ranking candidates by source count is what found a connectable file (most .pdf results had only LowID/firewalled sources). 285 tests.
- 2026-07-15 WAVE 8 TOOLCHAIN (Anthony's hardware: 2011 Mac mini + iPad Pro 4th gen): the 2011 mini maxes at macOS 10.13 High Sierra -> Xcode 10.1 -> iOS 12 SDK; the iPad Pro 4th gen is on iPadOS 17/18 and can't downgrade, so THE MAC MINI ALONE CANNOT BUILD FOR THE IPAD (needs Xcode 15 / macOS Ventura). Working path with existing hardware: (1) build the .ipa in CI (GitHub Actions macOS runners have modern Xcode; also cross-compiles the Rust mule-ffi -> aarch64-apple-ios xcframework); (2) sideload from the WINDOWS host (the WSL2 box) via AltServer (runs on Windows, not just macOS) + AltStore/SideStore; free Apple ID = 7-day signing (auto-refresh), $99 dev acct = 1yr. The mini is optional (iOS 12 simulator, or OCLP'd to run AltServer). Only the final link+sign needs Apple tooling; the Rust core builds+tests on this WSL box.
- 2026-07-15 PRE-WAVE-8 ENGINE HARDENING (Anthony: "do it all before Wave 8"). Four phases, all WSL-buildable/testable, building the foundation Wave 8's UniFFI layer wraps:
  (1) PERSISTENT IDENTITY. mule-files::preferences byte-compat codecs for aMule preferences.dat (version u8|userhash 16; markers 5=14,14=111) + preferencesKad.dat (ip u32|u16 0|KadID 16 wire|u8 0). mule-engine::identity::NodeIdentity (userhash+kad_id+kad_udp_key+RSA) load_or_create(dir) generates+persists only MISSING parts (existing cryptkey.dat skips RSA keygen). UDP key in padMule-native kadudpkey.dat (aMule keeps it in amule.conf INI). Stable identity across launches.
  (2) ENGINE FACADE. mule-engine::engine::Engine - the single object the UI drives + the UniFFI seam. EngineState{Stopped,Running,Paused} + EngineEvent stream. Lifecycle: pause() checkpoints+tears down, resume() rebuilds emitting "Reconnecting..."->"Connected"; both idempotent. The iPadOS foreground-only model, designed in.
  (3) STARTUP STATE-LOAD + RESUME. Engine.start() loads nodes.dat into the routing table + resumes every in-progress NNN.part from its .part.met (emits Kad{contacts} + Progress events); checkpoint() saves identity + routing->nodes.dat (part.met already durable). RoutingTable::contacts() enumerates the tree; multi_source::resume_downloads(dir) opens all NNN.part.
  (4) DOWNLOAD MANAGER. fetch::download_file(dl, sources, me, ManagerConfig{parallel,per_peer,rounds}): parallel peers vs one shared Download (block reservation splits work), sweeping sources `rounds` times to ride out eD2k upload-slot rationing (burst-then-queue). run_peer ALREADY waits out OP_QUEUERANKING for OP_ACCEPTUPLOADREQ, so the missing piece was just parallelism+retry. search-download CLI uses it (4 parallel, 3 rounds/pass) inside its server source-refresh loop. Loopback test: 3 parallel peers -> file completes + ed2k verifies. 300 tests. REMAINING pre-Wave-8 (optional coverage): AICH (Wave 1c), LowID callback download, UPnP/NAT-PMP, Kad keyword search.
- 2026-07-15 PRE-WAVE-8 PHASE 5 (optional coverage). (a) SERVERLESS KAD KEYWORD SEARCH - mule-kad kad_keyword_target=SetValueBE(MD4(utf8(lowercase(kw)))), build_search_key_req (0x33, target|flags), SearchResult::as_file distils FILENAME/FILESIZE/SOURCES; kad_live::resolve_keyword (lookup toward keyword hash + SEARCH_KEY_REQ); mule-cli kad-keyword. LIVE: `kad-keyword pdf` returned 50 real files off the DHT, no server. (b) AICH (Wave 1c) - mule-proto::aich::aich_master_hash, the SHA-1 hash tree (leaf=SHA1(184320-B EMBLOCKSIZE block), node=SHA1(L||R)); tree shape transcribed verbatim from SHAHashSet.cpp (CAICHHashTree split + SetFileSize root-base rule EMBLOCKSIZE if <=PARTSIZE else PARTSIZE). Structurally tested vs SHA1; byte-validation vs a live eMule AICH master hash is a follow-up (via amuled oracle / ed2k h= link). (c) NAT-PMP (RFC 6886, UPnP's lightweight cousin) - mule-engine::portmap map_port + mule-cli natpmp; codec tested, live attempt vs the Xfinity gateway correctly reports no-NAT-PMP (it does UPnP). Full UPnP-IGD (SSDP+SOAP) deferred to on-device. (d) LOWID CALLBACK - the codec (build_callback_request/parse_callback_requested) is done+tested (Wave 4d); the DRIVER (send callback via server -> accept the LowID peer's inbound connect -> download) needs us to be HighID + a server relay to validate, which this env can't reliably provide, so it's DEFERRED rather than shipped unvalidated. 311 tests. Pre-Wave-8 engine work COMPLETE: identity, Engine facade+lifecycle, resume, download manager, keyword search, AICH, NAT-PMP.
- 2026-07-15 INVESTIGATED eMule 0.70b community fork (C:\Users\ajbuf\Downloads\eMule0.70b-Sources.zip; genuine, copyright->2024, changelog to Aug 2024). CONCLUSION: it is a wire-COMPATIBLE continuation of eMule 0.50a - it makes NO wire-protocol/opcode/tag-format changes over 0.50a (grep of the community-era delta lines 6-1206 for protocol/wire/opcode/handshake/version-byte/backwards-compat = EMPTY; the only "sourceexchange protocol change" is line 2379, PRE-0.50a, already in our base). This confirms padMule's wire fidelity: nothing interop-critical is missing, and the network (mostly 0.50a-derived clients) is exactly what padMule already speaks. The two scary-sounding protocol fixes CONFIRM padMule is already correct: (a) "ED2K hash regression if file length is a multiple of part size" (Aug 2024) = eMule re-fixing a regression THEY introduced post-0.50a; padMule's ed2k_hash already does the canonical empty-MD4-sentinel thing (part_count=size/PARTSIZE+1). (b) "KAD tag count was incorrect" = a 0.51c (2019) regression eMule introduced+fixed; padMule follows 0.50a and never had it. ADOPTABLE VALUE (optional, all non-wire): Kad robustness/anti-abuse hardening (anti-flood ignore+ban, drop UDP from port 0, drop publish w/ unreachable buddy IP, remove routing-zone flooders, allow small ping variation in packet tracking, more unrequested-packet sanitizing), regenerate userhash if zero/lost (padMule already always-generates), and FEATURES: magnet-link parsing (name/len/hash/AICH/sources), ed2k://|search|term|/ link type, "Automatic" search-method selection (server vs Kad by connectivity - padMule now has both), and the "try obfuscation once for a server with no obf data" fallback. Extracted at scratchpad/emule070b. Use as a reference (like amule-master) if/when adding those. Files also present: eMule0.50a-Sources.zip (wire SOT), amule-master.zip.
- 2026-07-15 IMPLEMENTED the eMule 0.70b non-GUI features (all engine-level, testable). (1) LINK PARSING - mule-proto::link::parse_link handles ed2k://|file/server/serverlist/search|/ and magnet:?xt=urn:ed2k:...&xt=urn:aich:...&xl=&dn=&xs=, with hand-rolled hex(16B)/base32(20B AICH)/url-decode (mule-proto stays dep-free). mule-cli `link <link> [out]`: prints contents, downloads a file link's embedded sources, or prints the kad-fetch/kad-keyword command. Includes the 0.70b ed2k://|search|term|/ link. (2) KAD ANTI-ABUSE HARDENING (behavioral, non-wire) - mule-kad::hardening: is_acceptable_contact_ip/is_acceptable_contact reject eMule-IsGoodIP's unroutable ranges (0.x/loopback/link-local/private/multicast/reserved) + port-0; FloodTracker = per-IP request-rate limiter with ignore-then-ban (time injected, testable). Wired: KadNode::add_contact filters every BOOTSTRAP_RES/KADEMLIA2_RES contact before it enters the routing table (rejects the very multicast IP a byte-order bug once produced); verified live (bootstrap + keyword still find 50 files). FloodTracker is ready for the future Kad responder role. (3) AUTOMATIC SEARCH METHOD - mule-engine::search::choose_search_method(server_connected, kad_ready): prefer Server (local, fast) else Kad, matching 0.70b's "Automatic". 324 tests. Deferred from 0.70b: LowID-callback DRIVER (needs HighID+server relay), full UPnP-IGD (on-device), the obf-server-fallback UX tweak.
- 2026-07-15 INTELLIGENT TRANSFER ENGINE (go-beyond-0.70b, all client-side / zero wire change - the safe side of the replicate-wire/improve-internals line). Three pieces: (1) RAREST-FIRST - Download aggregates per-part swarm availability from every peer's OP_FILESTATUS (note_status); PartFile::next_blocks gained a rarity fn + orders wanted parts least-available-first (stable over the (missing,part) tie-break). (2) ENDGAME - next_blocks gained an endgame flag (ignore reservations); take_blocks enters it within ~4 blocks of complete so several peers race the tail, fixing the single-queuing-source last-block stall we hit live. (3) PEER SCORING - run_peer/download_from_peer now RETURN bytes delivered; fetch::download_file keeps a PeerScoreboard and orders each sweep best-first (proven deliverers > untried > proven failures; connect-fails penalized by FAIL_PENALTY=1MB). Same OP_REQUESTPARTS bytes on the wire - only WHOM/WHAT we ask changes. 5 new tests (rarest-first, endgame, scoreboard) + the loopback multi-peer download still completes+verifies. Live search-download: clean login/search/connect (no regression); 0 bytes that run because the one connectable source dropped us (network variance, not code - a prior run pulled 1.78MB). This is exactly the strategic direction: shine via client intelligence, never touch the wire. 327 tests. Design note in the "beyond community" discussion: next candidates are trust/fake-detection + search ranking (client-side) and, with adversarial peer validation, an optional padMule-to-padMule enhancement channel on a provably-ignored tag.
- 2026-07-16 LIVE THREE-FILE COMPLETION (proves the fetch engine end-to-end). padMule downloaded three real files to completion, one each from a keyword search for pdf/wav/txt, all ed2k-hash-verified: Chicks.txt (1416 B, c0e97d89...), "T.I. ... Live Your Life.WAV" (5888780 B, df13c362...), "ESAMI URINE .pdf" (4232 B, 40b3fc7a...). Content uncontrolled -> only name/size/hash recorded. Driver: mule-cli fetch-complete <server.met> <keyword> <out> [max] [min]. LOWID CALLBACK NOW PROVEN LIVE: wav + pdf were both delivered by firewalled LowID sources via OP_CALLBACKREQUEST (we bind a listener during login to earn HighID -> the LowID peer dials us back -> streams the file; the wav's whole 5.9 MB arrived this way). Completion is a source-availability HUNT; the wins were all client-side (zero wire change): (1) queue fast-bail - run_peer returns TransferError::Queued the instant a source sends OP_QUEUERANKING, turning a 25s dead-end into ~2s; (2) diversity-aware sweep - a keyword can be saturated by ONE sharer's collection ("wav" = 200 results ALL from one IP's Age-of-Empires game sounds), so skip files whose only source is an already-stalled IP; (3) min_size to escape a saturated keyword (>=800KB surfaced diverse real audio the game sounds had crowded out); (4) progress-aware callback wait + per-candidate part dirs - wait WHILE bytes flow (patient before first byte, bail after progress stalls), and give each candidate its own dir so a slow callback isn't clobbered by the next candidate (an earlier run delivered a full 5.9MB file then LOST it to a shared 001.part); (5) size-adaptive config (tiny=12s/1 round sweep, larger=40s/5 rounds). Files: catalog.rs (dedup/rank/trust), mule-cli cmd_fetch_complete, multi_source.rs (fast-bail), transfer_session.rs (Queued). 331 tests, fmt+clippy clean, ASCII-only. Recorded in [[build-progress]] + [[decisions-and-lessons]]. Marks tasks: search-intelligence + trust delivered via catalog.rs; LowID callback done. Wire was already correct (Wave 4 differential) - this was purely HOW we pick files and spend time.
- 2026-07-16 KB-sync note: the two code commits since the prior docs update - 057ddad (search catalog: dedup/rank/trust) and a1a07b9 (fetch-complete HighID + catalog-driven sweep) - are reflected above (Wave 7.6 row + the three-file milestone in [[build-progress]]). The uncommitted follow-on code (queue fast-bail, diversity-aware sweep, progress-aware callback wait, per-candidate part dirs, min_size arg, size-adaptive config) is documented in the same milestone and awaits commit.
- 2026-07-16 ENHANCEMENT CHANNEL Layer 1 (task #31, the highest-compat-risk feature) DONE + adversarially validated. Carrier chosen from SOURCE-GROUNDED recon (Explore agent over aMule 3.0.1 + eMule 0.50a): a HELLO taglist entry with an unknown NAME but a STANDARD type byte is read-and-skipped by both stock clients (aMule hello switch has no default BaseClient.cpp:477-478,635; eMule benign default 585-592); a NONSTANDARD type byte is unsafe (aMule THROWS->disconnect Tag.cpp:179; eMule DESYNCS packets.cpp:565-572); unknown TCP opcode on 0xE3/0xC5 is also ignored but a novel proto byte disconnects; Kad carriers are RISKY (throw->dropped on UDP). Implementation (peer.rs): string-named "padMule" UINT32 marker = <caps:u24><version:u8> in every HELLO/HELLOANSWER (tag count 7->8); ParsedHello::padmule()->Option<PadMuleInfo>; PADMULE_CHANNEL_VERSION=1, caps=0 (advertise nothing we don't honour). 4 unit tests. Two-layer design: Layer 1 detection (passive, must be ignored - it is) + Layer 2 negotiation (custom 0xC5 opcode, only ever sent to a CONFIRMED padMule so stock never sees it) - Layer 2 + NAT-traversal enhancement designed, not built. ADVERSARIAL GATE: amuled differential test passes with the marker in every hello (real aMule serves all 3 files byte-for-byte). That same run caught a REGRESSION in my own queue fast-bail: it was unconditional, so single-source peer-download aborted when amuled rationed slots (2nd/3rd files failed). Fixed by making fast-bail a policy - download_from_peer(.., bail_on_queue): hunt bails, single dedicated source waits. 335 tests, fmt+clippy clean, ASCII-only. New entry [[padmule-enhancement-channel]]; lesson in [[decisions-and-lessons]] (the differential test catches your own optimizations too).
- 2026-07-16 RESEARCH for task #32 (Layer 2 + NAT traversal) - 3 parallel source/web agents, synthesized into design specs. (1) LAYER 2 CARRIER (source, eMule 0.50a + aMule): a 0xC5 extended packet is [0xC5][u32 len LE][opcode][payload], drained by the SIZE field (opcode never consulted during framing: EMSocket.cpp:367-425 / 199-266), so an unknown 0xC5 opcode with arbitrary payload does NOT desync a stock peer - dropped at the switch default, no disconnect (ListenSocket.cpp:2075-2078 / ClientTCPSocket.cpp:1777-1780). Safe opcode = 0xD8 (0xC0-0xDF dead zone, far above eMule's 0xB2 frontier; NOT 0xB3+ where stock grows next). Packable via 0xD4 (opcode survives; keep decompressed < 50000). Only sent after the hello handshake (client object exists). Spec in [[padmule-enhancement-channel]]. (2) EXISTING NAT MACHINERY (source): CONFIRMED no hole punching anywhere - CanDoCallback (amule.cpp:2142-2189) hard-refuses both-firewalled; every callback ends in a TCP dial FROM the firewalled callee. Reusable rendezvous primitives: Kad key-mailbox (firewalled peers CAN publish/search), STUN-equivalent (KADEMLIA2_PING/PONG observed port + FIREWALLED_RES observed IP + UDP firewall tester intern/extern), buddy relay pattern (needs a custom Layer-2 opcode for arbitrary bytes). (3) NAT TRAVERSAL SOTA (web): cone x cone punches (~70-80% direct, libp2p DCUtR 2025 ~70%+-7%), symmetric x symmetric needs relay; borrow DCUtR (RTT/half-RTT synced punch) + BEP-55 (rendezvous via mutual peer); transport = quinn QUIC over the punched socket (pure-Rust QUIC layer, TLS 1.3, keep_alive for NAT upkeep; punch raw UDP first then bring up QUIC); iOS UDP works, foreground-only -> re-punch on resume. Full design + phased plan (Phase 0 discovery -> 1 MVP buddy+punch+QUIC -> 2 Kad rendezvous -> 3 robustness -> 4 relay fallback) + risks (adoption bootstrap, symmetric/CGNAT, iOS lifecycle) in new [[nat-traversal-design]]. Next: implement Layer 2 (opcode 0xD8) + a first message, adversarially re-validate vs amuled, then Phase 0/1 of traversal.
- 2026-07-16 DECISION (Anthony): mass-adoption goal -> DEFER Layer 2 + NAT traversal, keep Layer 1. Rationale: every channel enhancement works only padMule<->padMule, so value is 100% adoption-gated; on today's ~all-stock network Layer 2 never fires. Chicken-and-egg: get peers by shipping a good client, not by pre-building peer-to-peer features (a "no speculative features" call). KEEP Layer 1 (deployed marker accrues recognition for free) + the research (de-risks Layer 2/NAT traversal to a few days of ready work). Near-term priority redirected to ADOPTION DRIVERS, all single-user-valuable day one: Wave 8 iPad app (the whole value prop - no aMule on iPad), on-device HighID (UPnP/NAT-PMP), and the already-landing fetch/search/trust quality. Task #32 parked. Recorded in [[decisions-and-lessons]]; status notes added to [[padmule-enhancement-channel]] + [[nat-traversal-design]].
- 2026-07-16 UPnP-IGD DONE (on-device HighID, task toward Wave 8). mule-engine::upnp - hand-rolled zero-dep full UPnP-IGD (SSDP M-SEARCH discovery -> HTTP GET device description -> parse WANIP/PPP controlURL -> SOAP AddPortMapping + GetExternalIPAddress), the mapping path NAT-PMP (portmap.rs) does not cover and the one the dev-box Xfinity gateway needs. Minimal HTTP/1.1 + XML tag extraction over tokio sockets (iOS-safe, no new deps), same style as link.rs. mule-cli `upnp <port>`. 10 tests: pure helpers (URL split, xml_tag namespace-insensitive, resolve relative controlURL, parse_wan_service picks WANIP over other services, SOAP body shape, HTTP status/header parse) + a LOOPBACK INTEGRATION test driving http_get+parse_wan_service+soap_add_mapping+external_ip against a mock IGD over a real socket (validates HTTP/SOAP framing end-to-end). Live vs the real gateway: NO SSDP response, but an independent raw-Python M-SEARCH also got nothing -> ENVIRONMENT limit (WSL2 multicast egress and/or Xfinity UPnP off), not our code; should work on a real iPad on a UPnP home net. HighID now has both NAT-PMP + UPnP. Decision context: chosen as an ADOPTION driver (single-user-valuable) after deferring the enhancement channel Layer 2 - see [[decisions-and-lessons]]. Next: mule-ffi (UniFFI) seam.
- 2026-07-16 FFI SEAM DONE (mule-ffi, Wave 8a - toward the iPad app). New crate crates/mule-ffi (lib + cdylib + staticlib) is the UniFFI seam between the Rust engine and the native SwiftUI shell. MuleEngine wraps mule_engine::Engine in an FFI-friendly facade: opaque [u8;16] hashes -> hex Strings, EngineState/EngineEvent -> #[uniffi::Enum], IdentityInfo/DownloadInfo #[uniffi::Record], FfiError. The engine's async &mut self lifecycle runs on an internal tokio runtime (block_on) so exported methods are simple + synchronous: new(config_dir) constructor + start/pause/resume/shutdown/state/identity/kad_contacts/downloads/drain_events (events are polled, not callback-pushed - a deliberate MVP simplification). uniffi 0.28 proc-macro (setup_scaffolding!, no UDL); a uniffi-bindgen bin (uniffi::uniffi_bindgen_main, "cli" feature) generates bindings from the compiled cdylib. VALIDATED on this box: crate builds, a Rust-side test drives the full lifecycle + reads events through the FFI types, and `uniffi-bindgen generate --library target/debug/libmule_ffi.so --language swift` emits mule_ffi.swift (open class MuleEngine + all records/enums/error). Added a small Download::name() accessor for DownloadInfo. On-device (link staticlib + import mule_ffi.swift in the SwiftUI shell) waits for an Apple toolchain. Chosen as an adoption driver after deferring the enhancement-channel Layer 2. 346 tests, fmt+clippy clean, ASCII. Also captured the Mac-mini upgrade path (OpenCore Legacy Patcher; 2012/HD4000 is the good road, 2011/HD3000 the slow non-Metal road; need Xcode matching the iPad's iPadOS) - see [[decisions-and-lessons]] context.
- 2026-07-16 Added [[mac-toolchain-setup]] runbook (Anthony confirmed the Mac is a genuine 2011 Macmini5,x upgraded to 32GB -> non-Metal). Step-by-step: SSD + identify model -> OCLP install of a non-Metal-friendly macOS (Ventura best; Sonoma if the iPad is on iPadOS 18) -> Xcode (15 on Ventura / 16 on Sonoma, chosen by the iPad's iPadOS version) + rustup aarch64-apple-ios -> build libmule_ffi.a for the device + generate mule_ffi.swift -> Xcode SwiftUI shell linking the staticlib + wiring ScenePhase to MuleEngine pause/resume -> free-Apple-ID sign + Developer Mode + deploy to the PHYSICAL iPad (Simulator won't run: non-Metal). 7-day resign or AltStore. Indexed under Platform; memory [[padmule-ios-app-path]] updated to confirmed-2011-non-Metal.
- 2026-07-16 CORRECTION to the Mac toolchain plan (Anthony: the iPad is on **iPadOS 26.5.2**, and the mini is a confirmed genuine 2011 Macmini5,x/32GB = non-Metal). WEB-VERIFIED BLOCKER: iPadOS 26 -> Xcode 26 -> **macOS Tahoe 26.2+** (Apple system requirements), and **OCLP has NO Tahoe 26 support** (dortania issue #1167; v3.0 missed its winter-2025 deadline; OCLP's Intel road architecturally ENDS at Tahoe as macOS goes Apple-silicon-only). Non-Metal is a degraded tier anyway and the iOS Simulator REQUIRES Metal. => the 2011 mini CANNOT run the Xcode this iPad needs; my earlier iPadOS<=17/18 decision table is void. ESCAPE HATCH: Apple's Xcode-26/iOS-26-SDK mandate (2026-04-28) is **App-Store-submission only** - padMule is sideload-only, so it does not bind us; sideloading has no minimum-SDK gate and iOS is backward compatible, so an older-SDK build runs on iPadOS 26. Three paths, all ending in an AltStore/Sideloadly install (AltServer runs on Anthony's Windows host): (A) mini via OCLP -> Ventura 13 + Xcode 15, build-only, no debugger/simulator, free; (B) used M1 mini -> Tahoe 26 + Xcode 26 native, FULL debugging, ~$300-400 - recommended if iterating on UI; (C) GitHub Actions macOS runner (Xcode 26), zero hardware, slowest loop. DE-RISK FIRST: prove the sideload leg with a hello-world .ipa on the iPadOS 26 iPad before investing in any OCLP install. [[mac-toolchain-setup]] rewritten; [[padmule-ios-app-path]] memory updated.
- 2026-07-16 PATH C SET UP (iOS shell skeleton + CI). Wave 8 was "not started", so path C needed an app to build: created ios/project.yml (XcodeGen spec - the .pbxproj is generated in CI, never hand-written or committed; wires uniffi's module.modulemap onto the Swift include path + `-lmule_ffi`; iPad-only; deployment target iOS 16 so an older-SDK build still installs on iPadOS 26) and ios/padMule/Sources: PadMuleApp (ScenePhase -> MuleEngine pause/resume, pausing only on .background since .inactive is transient), EngineModel (every blocking FFI call off the main thread via a work queue; 1s Timer polls drainEvents()), ContentView (renders all three HARD [[lifecycle-and-reactivation]] requirements: honest foreground-only notice, Reconnecting banner, per-transfer Paused badges). .github/workflows/ios-build.yml builds it on macos-latest: rustup + aarch64-apple-ios -> libmule_ffi.a; bindings generated from the HOST cdylib (they're arch-independent - the mode uniffi-bindgen --library likes); brew xcodegen; xcodebuild with CODE_SIGNING_ALLOWED=NO; zip Payload/ -> padMule.ipa uploaded as an artifact. KEY: needs NO Apple credentials/secrets - the .ipa ships UNSIGNED and AltStore re-signs with Anthony's Apple ID at install (legit because padMule is sideload-only; the Xcode-26 SDK mandate is App-Store-only). Generated outputs (gen/, libs/, build/, .xcodeproj, Info.plist) gitignored. CAVEAT: none of the Swift/xcodegen wiring has ever been compiled (no macOS on this box) - CI is the first real compile, expect iteration. BLOCKED on: the repo has NO git remote, so it must be pushed to GitHub first (public = free unlimited macOS Actions minutes; private = 10x multiplier against the 2000/mo free tier ~= 200 macOS min). Awaiting Anthony's call on repo visibility.
- 2026-07-16 PUBLISHED + FIRST .ipa BUILT (Wave 8 milestone). (1) SCRUB BEFORE PUBLISHING: a pre-publish scan found the dev box's real network identity in tracked files AND git history AND commit messages - the public IP, the host NIC MAC, and critically the HighID client id, which ENCODES that IP in both decimal and hex (so redacting the dotted form alone would have leaked it anyway). Since a public repo publishes its whole history, fixed HEAD (upnp.rs test fixtures -> RFC 5737 203.0.113.5; docs -> <public-ip>/<client-id>/<client-id-hex>/<nic-mac> placeholders, technique preserved) then `git filter-repo --replace-text --replace-message` purged all four forms from all 121 commits + messages. Safe because the repo had never been pushed (no forks/collaborators); .git backed up first. VERIFIED: searched every blob of every commit + every commit message = zero traces; 346 tests still green. The real values live ONLY in private memory ([[padmule-dev-box-networking]] updated: the repo is PUBLIC now, never commit them again). (2) PUBLISHED public at github.com/ajbufort/padMule (public chosen for free unlimited macOS Actions minutes). (3) CI GREEN ON THE FIRST RUN (5m46s) - notable since the Swift/XcodeGen/uniffi wiring was authored blind with no macOS here. VERIFIED the artifact rather than trusting the green check: downloaded the 588 KB padMule.ipa, unzipped -> Payload/padMule.app/padMule is a **Mach-O 64-bit arm64 executable**; Info.plist us.ajbconsulting.padMule / MinimumOSVersion 16.0 / UIDeviceFamily [2] (iPad); and the Rust engine is really inside (432 uniffi/mule_ffi strings, mule_ffi_rustbuffer_free, EngineStateFfi, the "Reconnecting..." banner string). So padMule now has a real, installable iPad binary containing the eD2k/Kad engine - built with no Apple hardware at all. NEXT: sideload via AltStore (AltServer on the Windows host) + Developer Mode on the iPad, then wire the live server/Kad behind the UI.
- 2026-07-16 ENGINE WIRED LIVE BEHIND THE UI. Engine::start() now brings the real network up (was: load state only). LIVE-VERIFIED from an empty config dir = exactly a fresh install: fetch lists -> Kad 133 contacts -> open port -> **HighID** login to a real server -> Kad bootstrap (21 live contacts) -> State(Running)/Status("Connected"). (1) FRESH-INSTALL GAP FOUND + CLOSED: nothing fetched server.met/nodes.dat (hand-placed on the dev box), so a real install knew no servers and no Kad contacts and could reach NOTHING. New bootstrap.rs fetches both from upd.emule-security.org - fetch not bundle, because a bundled list ROTS (see the 2026-07-13 stale-list failure). Hand-rolled HTTP: byte-safe (these are BINARY; UTF-8 decode corrupts them) and on a RAW SOCKET, which sidesteps iOS ATS entirely (ATS governs URLSession/CFNetwork, not BSD sockets) -> cleartext http:// works on-device with no Info.plist exemption. Best-effort + validated (an HTML error page must never be saved as a server list); failure = come up offline, never crash. Live-checked: server.met 1281 B, nodes.dat 5928 B. (2) HIGHID LANDMINE: order matters - the listener must bind BEFORE login, because the server decides HighID/LowID by connecting back to the advertised port. First live run gave **LowID**; adding start_listener() before connect_server() flipped the identical code to **HighID**. The server's probe is a bare TCP connect+close so merely ACCEPTING passes it. map_port() also tries UPnP so a real device with no router rule can earn HighID. (3) pause() drops the Kad UDP socket + aborts the listener (frees 4662); resume() rebinds the port FIRST, re-runs the handshake, re-bootstraps Kad - correct across an IP change. online_status() is HONEST - never claims Connected when it isn't (the old resume() emitted a hardcoded "Connected"; the test now pins the honesty instead). (4) TESTING: unit tests must never touch the network -> Engine::set_offline(true); engine tests went 2.67s -> 0.05s proving they no longer dial out. Real proof = engine::live::fresh_install_goes_online_and_bootstraps_kad (#[ignore]d). 352 tests, fmt+clippy clean, ASCII. REMAINING: expose search + add-download through the FFI facade (the engine already does both).
- 2026-07-16 Expanded [[mac-toolchain-setup]] Phase C into the real sideload runbook now that path C is the ACTIVE route (CI emits the unsigned .ipa; AltStore re-signs with a free Apple ID). Records the landmines so they are not re-derived: iTunes+iCloud MUST come from apple.com and NOT the Microsoft Store (the #1 AltServer-on-Windows failure); Developer Mode is required on iPadOS 16+ but its toggle only APPEARS after a dev-signed app install is attempted; "Sync over Wi-Fi" in iTunes is what enables AltStore's 7-day auto-refresh; free-ID limits = 7-day expiry + 3 apps; artifacts download as a .zip. Also notes that with no Xcode device support for iPadOS 26, the app's own status line IS the diagnostic, and what a healthy first run looks like.
- 2026-07-16 SIDELOAD TRAP + verified links ([[mac-toolchain-setup]] Phase C). Anthony hit it immediately: apple.com/itunes now advertises ONLY the Microsoft Store build - which is precisely the one AltServer CANNOT use. The standalone installers still exist, Apple just stopped linking them. Verified live by checking the actual responses (not guessing): `https://www.apple.com/itunes/download/win64` 301s to a real iTunes64Setup.exe (208 MB, built 2026-03 - current, just unlisted); iCloud standalone `https://updates.cdn-apple.com/2020/windows/001-39935-20200911-1A70AA56-F448-11EA-8CC0-99D41950005E/iCloudSetup.exe` (161 MB, the link AltStore's own FAQ specifies - looks ancient but is the last standalone iCloud Apple shipped); AltServer `https://cdn.altstore.io/file/altstore/altinstaller.zip` (9 MB). Also: if Store iTunes/iCloud are installed, UNINSTALL them first (a common "AltServer can't find your device"); run AltServer as ADMINISTRATOR; iPadOS 26 calls the profile screen "VPN & Device Management" (AltStore's docs use the older "Profiles & Device Management").
- 2026-07-16 SIDELOAD TRAP #2 (UNRESOLVED, routed around): **-22411** "This action cannot be completed at this time" on Install AltStore ([[mac-toolchain-setup]] Phase C). I first attributed it to a never-device-activated Apple ID - a REAL documented trigger, but WRONG here: Anthony corrected me that he used his PRIMARY, long-activated ID. Corrected in the entry; recording the miss because I reached for the first plausible cause the web offered and fitted it to the symptom instead of first checking it fit HIM. RESEARCHED PROPERLY: the code has NO documented root cause anywhere - AltStore's own error-codes page lists -22410 but not -22411, their troubleshooting guide never mentions it, and issues #417/#785/#1720 are all OPEN with "reinstalled everything, no change". It is Apple's generic developer-service failure surfaced through AltServer, raised in the portal stage (register device -> App ID -> provisioning profile) - Apple's to explain, and they don't. DECISION: do not sink time into a black-box error on someone else's auth path. Switch to **Sideloadly** (sideloadly.io) - independent implementation of the same free-signing trick, does NOT share AltServer's Apple-auth path, confirmed on iPadOS 26/Windows, and SIMPLER for our purpose (USB + drag the .ipa; no AltStore, no Files shuffle, no Wi-Fi sync, no app slot). Same 7-day cert mechanism so trust-the-cert + Developer Mode still apply; loses only auto-refresh, which does not matter for a first-launch test. Cheap AltServer checks kept in the entry if we ever return: Apple System Status, AltServer >= 1.7.3, MDM profile, retry later. LESSON: the sideload leg is the DE-RISK step precisely because it is the part we do not control - when a third-party auth path fails opaquely, swap the tool, do not debug Apple.
- 2026-07-16 **padMule RAN ON THE IPAD** (Wave 8 milestone, sideload leg PROVEN). Sideloadly worked where AltServer died on -22411, so the de-risk step is done: State running, Status Connected, Kad 21 -> 158 contacts, identity rows populated - the Rust eD2k/Kad port live on the target device, on the real network, built with no Apple hardware anywhere in the chain. Anthony's screenshots were the entire diagnostic and they earned two findings. (1) **AN EVENT IS NOT STATE**: the HighID/LowID answer was computed at login (connect_server) and emitted as a one-shot `Server("Connected to <addr> (HighID)")`, then `start()` emitted `Status("Connected")` right after - both land in the SAME 1s drainEvents() batch, EngineModel applies them in order, so the honest line was clobbered before a single frame rendered. The most important diagnostic on a device with no debugger was computed and thrown away. FIXED: new `ServerInfo {addr, low_id}` is durable engine state, `online_status()` carries the ID type, `server_info()` is gated on `is_online()` (no server -> no claim), resume() re-records low_id because it can FLIP across an IP change, and the UI polls it as a SNAPSHOT into its own colour-coded row. 2 new tests pin the honesty gate; 354 tests green. LESSON: anything the UI must KEEP showing has to be readable at any time, not announced once. (2) **UPnP IS DEAD ON iOS** - the "find devices on local networks" prompt was `upnp::discover()` firing SSDP M-SEARCH at MULTICAST 239.255.255.250. Blocked twice over: `NSLocalNetworkUsageDescription` was missing from our Info.plist and without it iOS 14+ SILENTLY DROPS every LAN packet (no error at all - developer.apple.com/forums/thread/661606); and multicast on real hardware needs the RESTRICTED `com.apple.developer.networking.multicast` entitlement, which requires Apple approval and is UNREACHABLE for a free-signed sideloaded app. Added the plist key (it gates the unicast paths too), but the entitlement is a hard wall => on-device HighID needs UNICAST port mapping: `portmap.rs` (NAT-PMP, unicast gateway:5351) is ALREADY BUILT but never wired into `map_port()`, which only tries UPnP. Not urgent: this box forwards 4662/4672 to the WINDOWS host not the iPad, so the iPad is LowID regardless, and LowID is survivable (the live wav + pdf both arrived by callback). (3) TWO IP LEAKS ONTO THE SCREEN, caught and closed: map_port() emitted the gateway-reported PUBLIC IP into a UI event, and the login event embedded the client id which ENCODES the public IP on HighID - the exact value scrubbed from the public repo. Both removed; ServerInfo carries low_id ONLY, by design, because this screen gets screenshotted. [[mac-toolchain-setup]] gained a "what the first real device run taught us" section.
- 2026-07-16 **SEARCH + DOWNLOAD REACH THE IPAD** (the app stops being a status screen). The engine could already do both (it fetched the three live files), they were just not exposed through the FFI. What the wiring exposed: (1) **ServerLink owned the socket but could not TALK** - only connect/pause/resume/disconnect; nobody ever read from `conn` after login, and mule-cli bypassed ServerLink entirely to hand-roll the stream. Added `request(pkt, want, wait)` + search/get_sources/request_callback. Unsolicited server chatter seen while waiting for a reply is FORWARDED, not dropped - extracted `classify_server_packet` out of login_handshake so the same mapping serves both (a server interleaves MOTD/status/serverlist with replies for the whole connection, not just during login). (2) **VERIFICATION GAP FOUND**: `download_file` NEVER calls `verify_ready_parts`, so `is_complete()` means "all bytes arrived", NOT "verified" - and a SINGLE-part file (<PARTSIZE) has no part hashes at all, so nothing checks it (the documented multi_source.rs:333 landmine). => finish_download now re-derives the WHOLE-FILE ed2k hash and refuses to save on mismatch. This is the only thing between corrupt bytes and the user's Files app. (3) **New `ed2k_hash_parts`** in mule-proto: mule-cli verifies by `parts.concat()` - the whole file in RAM, fine on a 32GB dev box, not for a big file on an iPad. The hash is structurally part-wise, so it streams. Pinned against `ed2k_hash` at every boundary (0, 1, PARTSIZE-1, PARTSIZE, +1, 2*PARTSIZE, +1) and asserted never to read the trailing sentinel part - the part_count(floor+1)-vs-data_part_count(ceil) landmine, now handled in ONE place. (4) **Attacker-controlled filenames**: P2P names are hostile input; `safe_filename` collapses them to one inert path component (tested: "../../Library/Preferences/evil" -> "_.._Library_Preferences_evil", asserted to join INSIDE the parent) and `unique_dest` never overwrites an existing file. (5) **LowID cannot ask for callbacks** - a LowID asking a LowID to call back is the one case eD2k cannot route, so callbacks are only requested when WE are HighID (uses the new ServerInfo). (6) The **listener was dropping every inbound peer** after the handshake - it now routes them into an unfinished download (the shared Arc<Mutex<Vec<Arc<Download>>>>), which is what makes a callback deliver. (7) **Downloads land in Documents** + UIFileSharingEnabled + LSSupportsOpeningDocumentsInPlace, so they show in the Files app: Application Support is invisible, and a file the user cannot open is not a finished download. (8) **THE SAME EVENT-CLOBBERS-STATE BUG AGAIN**: `.server` events were writing to `status`, which would have wiped the polled "Connected to <server> (HighID|LowID)" line the moment anything said "Saved 'x'". Server events now feed the transient `notice`; `status` stays the connection state. Watch for this shape - it has now bitten twice. Also fixed: map_port()'s doc claimed it "tries NAT-PMP and UPnP" while the body only ever tried UPnP. 363 tests, clippy clean, ASCII.
- 2026-07-17 **THE FULL DOWNLOAD LOOP RAN ON THE IPAD** (Wave 8 - the app is now a real client, not a demo). From the SwiftUI app on the device: Anthony typed `test`, the connected server (45.87.41.166, LowID, 162 Kad) returned a full page of results with sizes + source counts, he tapped Get, and the engine downloaded a 168KB .doc from a SINGLE source, verified the whole-file ed2k hash, saved it to Documents, and it opened in the Files app under "On My iPad > padMule". Banner trail across his screenshots: "Downloading '...'" -> "Saved '...'" -> the file visible + readable in Files. FIRST end-to-end search->download->verify->save->open done entirely on-device. Finding: **LowID did NOT block it** - the sole source was HighID and reachable, so a LowID client is only walled off from files whose ONLY sources are themselves LowID, not from the network at large; downloads work today, and HighID is a throughput/reach improvement, not a prerequisite. INSTALL LESSON (cost a cycle): the previous re-sideload looked like "the exact same thing as before" because an older/cached .ipa got installed - the search feature was verifiably in the CI Mach-O the whole time. New standing workflow at Anthony's request ("just download it for me from GitHub from now on"): fetch with `gh run download <id> -R ajbufort/padMule -n padMule-ipa`, PROVE the feature is in the binary (`strings padMule | grep "Search the eD2k network"`), stage into /mnt/c/Users/ajbuf/Downloads/ with the run id IN the filename, and delete the old app off the iPad before reinstalling - never point him at the Actions page (many identically-named artifacts). Updated [[padmule-ios-app-path]] + [[padmule-live-downloads]] memory; content policy held (name/size only, no contents). Next candidates, Anthony to pick: on-device HighID via unicast SSDP-at-the-gateway, Kad keyword search, upload/serve (stop being a pure leech), or a cancel/delete control.
- 2026-07-17 **UPLOAD / SERVE + LEECH MODE** (padMule stops being a pure leech; feature 1 of 4 Anthony greenlit). New `share.rs`: a `SharedFile` is a COMPLETE file on disk we will serve; `serve_shared` answers the eD2k upload sequence (filename -> status -> hashset -> slot -> parts), reading each requested block off disk so a large file is never held in RAM. The inbound listener now tells a LEECHER (sends OP_REQUESTFILENAME first) from a called-back LowID SOURCE (stays silent, waiting for us to drive one of OUR downloads) by PEEKING the first packet with a 3s timeout - cancel-safe because `framed::read_packet`'s only await is the socket read and bytes are appended to its buffer synchronously after, so a stalled first read buffers nothing and loses nothing. `finish_download` seeds each verified file into the shared library (capturing the hashset before `into_store` consumes it); `serve_inbound` gates on the sharing switch + an 8-slot concurrency cap - a peer that finds us leeching or full gets OP_FILEREQANSNOFIL and moves on, not a hang. Engine `set_sharing`/`is_sharing` is an `AtomicBool` the listener reads lock-free -> FFI `setSharing`/`isSharing` -> a SwiftUI "Share uploads" toggle polled as a SNAPSHOT (same "an event is not state" discipline as the server login). Flipping it OFF is the private-use "Leech Mode": a NEUTRAL pause-uploads control (the mechanism Anthony chose), not a separate untracked feature - because a CI-built .ipa can only carry committed code, a literally-untracked button could never reach the device. Only COMPLETE files are shared for now; serving in-progress parts (range reads out of a live `.part` + a read-side availability bitfield) is the next step, noted in the module docs. 6 new tests (serve a held file single- and multi-part-with-hashset, refuse an unheld hash, Leech Mode refuses a file we DO hold, sharing-on serves through `serve_inbound`). 368 workspace tests, clippy + fmt + ASCII clean. Updated [[padmule-ios-app-path]].
- 2026-07-17 **LICENSE + NOTICE at the repo root** (GPL compliance for the public derivative). Anthony asked whether vendoring the aMule codebase is inappropriate. It is NOT: aMule 3.0.1 is GPL-2.0-or-later, and the GPL PERMITS redistributing the source in a fork/port as long as the license + notices travel with it and the derivative is GPL too - `amule-3.0.1/LICENSE.md` + `docs/COPYING` + `docs/AUTHORS.md` are preserved intact, and the tree is our documented reference oracle. The REAL gap he surfaced: padMule had NO top-level license of its own. Added `./LICENSE` (GPL v2 text, copied verbatim from `amule-3.0.1/docs/COPYING`, pure ASCII, 338 lines) + `./NOTICE` (padMule (C) 2026 Anthony Bufort, GPL-2.0-or-later, states the derivation from aMule/eMule and that the vendored tree is the reference oracle). Consistency wrinkle recorded, not acted on: `refs/` (eMule/aMule oracles) is gitignored while `amule-3.0.1/` is tracked - keeping it tracked preserves the in-repo divergence record, so it stays. See [[padmule-licensing]].
- 2026-07-17 **CANCEL / DELETE A DOWNLOAD** (feature 2 of 4). A download can now be removed mid-flight. The wrinkle: `fetch::download_file` spawns DETACHED worker tasks (dropping the outer future does NOT abort them - a dropped `tokio::spawn` handle detaches, it does not cancel), so aborting the driver would leave workers writing to a `.part` we are about to delete. Fix = COOPERATIVE cancellation: `Download` gains a lock-free `cancelled: AtomicBool`; `take_blocks` returns empty when set (the peer session ends, the worker loop falls through), and both the round loop and worker loop in fetch.rs check `is_cancelled()`. Engine `cancel_download(hash)`: finds the download, `cancel()`s it, removes it from the registry, and deletes the `.part` + `.part.met` (`PartStore::remove_backing_files` - an open handle a straggler holds keeps the bytes readable via the unlinked inode, but the paths leave disk at once so a restart will not resume it). The outer add_download task bails without saving when it sees `is_cancelled()`. FFI `cancelDownload(hash) -> bool` -> SwiftUI swipe-to-delete (destructive "Remove") on each transfer row. 2 new tests (cancel removes it + deletes both files + unknown hash is a no-op false; a cancelled download hands out no more blocks). 370 workspace tests, clippy + fmt + ASCII clean.
- 2026-07-17 **KAD SEARCH MERGED INTO THE SEARCH BOX** (feature 3 of 4; the box stops depending on a live server). `engine.search()` now queries the connected server AND Kad CONCURRENTLY (`tokio::join!` over the two DISJOINT fields - server link + Kad node - so the wait is the slower of the two, not the sum), then folds both into ONE `catalog` pass. Kad's already-distilled `FileResult {hash,name,size,sources}` is converted to a synthetic tagged `SearchResultFile` (id tags 0x01 name / 0x02 size / 0x15 sources) so dedup/rank/trust apply uniformly and a file found on both merges by hash with the better availability winning. Either half may be absent: a SERVERLESS client still gets Kad hits (Kad needs no server), a client with no Kad contacts still gets server hits. Kad bounded by KAD_SEARCH_WAIT=15s (concurrent, so usually free) + KAD_PER_QUERY=750ms; a lookup that misses the budget contributes nothing. Reuses `KadNode::resolve_keyword` (already proven by `mule-cli kad-keyword`). NO FFI/Swift change - the merge is transparent at the engine seam, so the existing search box just returns more. Added tokio `macros` to the main dep (was dev-only) for `join!`. 1 new test (a Kad hit + a server hit for one hash collapse to one ranked file, best availability wins). 371 workspace tests, clippy + fmt + ASCII clean. Closes the caveat from the search-wiring milestone ("search only queries your connected server, not Kad").
- 2026-07-17 **ON-DEVICE HighID via UNICAST SSDP** (feature 4 of 4; the biggest and least verifiable). iOS SILENTLY DROPS multicast SSDP without the restricted multicast entitlement (unreachable for a free-signed app), so UPnP discovery could never earn HighID on the iPad. Fix: UNICAST M-SEARCH aimed straight at the gateway - same UPnP protocol, no multicast, no entitlement. `upnp.rs`: `msearch_message()` (MX only on the multicast form), `gateway_candidates()` (infer .1/.254 of our /24, since iOS exposes no default-gateway API), `local_lan_ip()` (the connect-a-UDP-socket-to-a-public-host trick; no packet sent, just reads the chosen source addr), `discover_unicast()` (M-SEARCH per candidate, trust ONLY replies from the queried addr, then reuse the existing `http_get` + `parse_wan_service` + SOAP AddPortMapping). `map_port()` now tries multicast then falls back to unicast, so the SAME binary earns HighID on desktop AND iPad. `map_port_unicast()` + `mule-cli upnp-unicast <port>` expose the unicast-only path for live verification. 3 new tests (gateway_candidates .1/.254 and never self; unicast M-SEARCH omits MX + multicast keeps it; `discover_unicast` end-to-end against a mock UDP gateway + mock description server over loopback). 374 workspace tests, clippy + fmt + ASCII clean. **VERIFICATION STATUS: unit + mock PROVEN; LIVE against a real gateway UNVERIFIED from here.** WSL2 returns NO SSDP response by any method (multicast, unicast, or raw `nc`), and the Xfinity gateway at 10.0.0.1 is reachable on :80 but SILENT on SSDP:1900 - so its UPnP is apparently OFF (likely why manual forwarding was needed originally, not UPnP). Gateway INFERENCE is correct: `local_lan_ip`=10.0.0.33, `candidates[0]`=10.0.0.1=the real gateway. The real verdict is the on-device test: iPad flips to HighID => unicast UPnP works; iPad stays LowID => the gateway's UPnP is off and the fallback is a manual forward to the iPad's OWN LAN IP (the dev-box forward points at the Windows host 10.0.0.33, which does nothing for the iPad). See [[padmule-dev-box-networking]].
- 2026-07-17 Reboot check (Anthony asked if our port-opening reverted across several reboots). DURABLE pieces survived: `.wslconfig` still `networkingMode=mirrored`, WSL still shares host IP 10.0.0.33 (mirrored ACTIVE), Windows Firewall rules "padMule eD2k TCP 4662" + "UDP 4672" still present, and the router forward lives on the router (survives reboots unless the DHCP reservation moved). SUSPECT: the Hyper-V VM firewall `DefaultInboundAction` now reads `NotConfigured` - the least-persistent layer (the mirrored-mode trap), which a reboot/WSL update can reset; if it is blocking inbound, the DEV BOX is LowID again. This is separate from the iPad and from the UPnP finding above. The dev-box HighID never used UPnP - it used a manual router forward - so the UPnP silence is the gateway's setting, not a reboot regression.
