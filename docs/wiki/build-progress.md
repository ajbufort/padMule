# Build Progress

Updated: 2026-08-04 (row 8bt: THE FETCH FUNNEL - one peer session in 1583 dials delivered a byte; OP_OUTOFPARTREQS had no handler, and slots were being asked of peers holding nothing we need. Row 8bs: retry budget becomes a ceiling, the 99-vs-12 badge, and the QUEUE-BAIL root cause. Row 8br: the idle-retry sweep starved all but one download; amber activity row. Row 8bq: the serial-queue engine half - UI polls off the engine lock. Row 8bp: the on-glass UI round - Files button, real Open, floating Top, sorting. Row 8bo: the fourth port field, and events off the serial queue. Row 8bn: continuous block-request top-up + a source-origin badge. Row 8bm: the serve side was sending every block up to three times. Row 8bl: device-verification - the resume fix PROVEN on glass, UPnP defaults OFF. Row 8bk: the independent post-wave-11 sweep - the AICH
serve answer was unreachable on the path real clients use, and a second
corruption round blamed the first round's source. Row 8bj: WAVE 11 - AICH block
recovery, closing the last security-scorecard PARTIAL at 24/0/2. 2026-08-03:
rows 8av-8bi - portability audit + Tier-1 slice, Settings, the server crawl, the
usage-feedback round, VPN readiness, and the on-glass UI batch)

Wave-by-wave status of the padMule Rust engine (waves defined in
`docs/superpowers/specs/2026-07-12-padmule-design.md` section 10). Each wave
ends at a differential/round-trip gate.

## Status

| Wave | Scope | Plan | State |
|------|-------|------|-------|
| 1 | `mule-proto`: eD2k/MD4 file hashing | `plans/2026-07-12-wave1-mule-proto-ed2k-hash.md` | DONE. |
| 1b | `mule-proto`: LE byte I/O + eD2k tag codec | `plans/2026-07-12-wave1b-io-and-tags.md` | DONE. Reviewed + corrected (see below). 20 tests total, clippy clean. |
| 1c | `mule-proto`: AICH SHA-1 hash tree + search-expr encoding | (implemented directly) | AICH DONE (2026-07-15): mule_proto::aich::aich_master_hash - SHA-1 tree (leaf=SHA1(EMBLOCKSIZE block), node=SHA1(L||R)), verbatim from SHAHashSet.cpp; structurally tested vs SHA1. [UPDATE 2026-07-19: BYTE-VALIDATED vs real amuled - row 8u.] [UPDATE 2026-07-19: boolean OR/NOT search tree DONE - row 8t.] |
| 2a | `mule-files` crate + `server.met` | `plans/2026-07-12-wave2a-mule-files-server-met.md` | DONE (5 tests). |
| 2b | `known.met` | `plans/2026-07-12-wave2b-known-met.md` | DONE (10 mule-files tests total). |
| 2c | `part.met` (+64-bit + gap list) | `plans/2026-07-12-wave2c-part-met.md` | DONE (17 mule-files tests). `nodes.dat` moved to Wave 6 (Kad). |
| 3a | `mule-proto` packet framing + zlib | `plans/2026-07-13-wave3a-packet-framing.md` | DONE (31 mule-proto tests). |
| 3b | `mule-engine` crate + login-handshake codecs (offline) | `plans/2026-07-13-wave3b-login-codecs.md` | DONE (8 tests): build_login_request, parse_id_change/server_message/server_status. |
| 3b-2 | search codec + server list/ident | `plans/2026-07-13-wave3b2-search-codec.md` | DONE (15 mule-engine tests): build_search_request (ANDed-terms), parse_search_result, parse_server_list, parse_server_ident. Full boolean tree (OR/NOT) + global UDP search + OP_FOUNDSOURCES deferred. |
| 3c-1 | async framing + login handshake (tokio) | (implemented directly, no separate plan doc) | DONE (21 mule-engine tests): FramedStream, ServerState/ServerEvent, login_handshake, connect_server. Mock-server tested. |
| 3c-2 | pause/resume ServerLink + mule-cli live harness | `plans/2026-07-13-wave3c2-link-and-cli.md` | DONE (23 mule-engine tests): ServerLink connect/pause/resume over a real loopback socket; mule-cli login / login-any. Live run: see note below. |
| 3d | client-to-client peer HELLO codec | (implemented directly) | DONE (28 mule-engine tests): build_hello/answer, baseline MISCOPTIONS1/2 (0x34103212/0x438) byte-verified, parse_hello + Capabilities. Pivoted from get-sources (server-dependent, untestable here) to the peer protocol (locally testable). |
| 4a | client-to-client peer connection + inbound listener | (implemented directly) | DONE (30 tests): peer_handshake_outbound/inbound, connect_peer/accept_peer; two engines handshake on loopback. mule-cli `listen` command for HighID validation. |
| 4b | download-side transfer message codecs | (implemented directly) | DONE (37 tests): request_filename/setreqfileid/startupload/hashset, file-status bitfield, request_parts (3-block u32/u64), sending_part, queue-ranking. |
| 4c | first end-to-end transfer (two engines) | (implemented directly) | DONE (40 tests): download_file + serve_file; two engines transfer a 3-block file on loopback, ed2k hash matches byte-for-byte. Next: write to a real .part, multi-part+hashset, differential vs local amuled. |
| 4d | upload side + queue/slots + credits + source exchange + corruption; get-sources codec | (implemented directly) | **DONE - Wave 4 GATE MET (180 tests + differential test vs real amuled passes).** 4d-1/2 credits + clients.met + upload queue/slots/ranking; 4d-3 source exchange (SX1/SX2 v1-v4) + get-sources + LowID callback; 4d-4/5 PartFile block allocation + corruption handling; 4d-6 disk-backed PartStore (.part + byte-compat .part.met, resume, atomic save); 4d-7 multi-source Download driver; 4d-8 adversarial review + 6 fixes; 4d-9 DIFFERENTIAL TEST: padMule downloads from a real amuled 3.0.1 byte-for-byte (raw + compressed paths). Fixed 4 aMule bugs + 6 review findings + 1 interop bug the differential test caught. See notes below. |
| 5a | TCP protocol obfuscation (EncryptedStreamSocket) | (implemented directly) | DONE + VALIDATED vs real amuled. RC4 (textbook vectors) + key derivation MD5(target_userhash \| magic \| randomkey) + handshake + FramedStream cipher integration + connect_peer_obf/accept_peer auto-detect. padMule downloaded single-part AND multi-part (hashset) from amuled through the RC4 stream, byte-for-byte. Research: docs/raw/wave5-crypto-research-2026-07-14.md. |
| 5b | UDP obfuscation (EncryptedDatagramSocket) | - | DONE - absorbed into Wave 6b (`mule-kad::udp_obf` implements CEncryptedDatagramSocket; see row 6b). |
| 5c | secure identification (RSA) | (implemented directly) | DONE + INTEROP-VALIDATED vs real amuled. RSA-384 PKCS1v15-SHA1 (48-byte sig); Identity keygen + cryptkey.dat (PKCS#8, NOT PKCS#1 - Crypto++ writes the PKCS#8 wrapper); sign_v1 signs PEER pubkey \| challenge, verify_v1 mirrors; OP_PUBLICKEY/SECIDENTSTATE/SIGNATURE codecs. Integration test loads a REAL aMule cryptkey.dat. v1 only (v2 challenge-IP deferred). Remaining: live state machine + credit tie-in (5d). |
| 5d | crypto wiring + gating; secure-ident state machine; differential gates | (implemented directly) | MOSTLY DONE + VALIDATED vs amuled. (1) TCP obfuscation wired into peer connect/accept + serve-file, validated (download + secure-ident vs amuled). (2) SecureIdentSession live mutual RSA - padMule VERIFIED real amuled's signature. Caught on-wire pubkey = SPKI not PKCS#1. (3) Credit tie-in: IdentState + resolve_ident_state (IP binding) + score_ratio_ident (no bonus for unverified/failed/badguy). (4) Obfuscation gating: should_obfuscate_outbound / should_reject (crypt_policy.rs). REMAINING (integration, lower-risk): drive secure-ident + gating during a live transfer (multi_source); server-connect (DH) obfuscation; amuled uploader-pull (serve is obf-aware + proven P2P; blocked only by amuled's flaky offline link-source dialer). |
| 6a | Kad128 (CUInt128) + nodes.dat + routing table | (implemented directly) | DONE + real-fixture-validated. mule_proto::Kad128 (XOR distance, MSB-first bit(), 2^120 tolerance; canonical from_hash/to_hash vs raw from_wire/to_wire - dword-byte-reversal landmine). mule_files::nodes_dat byte-compat (parses + round-trips the REAL 6098-B nodes.dat byte-identical). NEW crate mule-kad: routing bin-tree (K=10, KBASE=4, KK=5, split rule, closest_to) - loads the 179-contact fixture (retains ~142, far bins cap at K). Research: docs/raw/wave6-kad-research-2026-07-14.md. |
| 6b | Kad UDP framing + obfuscation (5b) + bootstrap/hello | (implemented directly) | DONE (codecs) + source-verified + adversarial-reviewed. mule-kad: frame.rs (0xE4/0xE5, pack iff PAYLOAD>200 - opcode excluded), udp_obf.rs (CEncryptedDatagramSocket: 16-B header, contiguous RC4 no-discard, sentinel 0x395F2EC1 + verify keys plain-LE since aMule ENDIAN_SWAP is a no-op on LE, NodeID/ReceiverKey derivations, GetUDPVerifyKey, is_protocol_byte = UDP set {C5,D4,E4,E5,A3,B2}), message.rs (BOOTSTRAP/HELLO/RES_ACK + Kad tag <type><nameLen u16=1><name><value> w/ CTagVarInt minimal sizing; version 0x08 aMule). Pipeline test: build->frame->obfuscate->deobfuscate->deframe->parse (NodeID + ReceiverKey paths, 20-contact packed RES). 3 bugs fixed pre-commit (proto-byte set, zero-payload reject, pack off-by-2). LIVE GATE PASSES (2026-07-15): mule-engine::kad_live::KadNode + `mule-cli kad-bootstrap <nodes.dat>` - against a fresh nodes.dat from upd.emule-security.org, padMule sends an obfuscated BOOTSTRAP_REQ to a real node (v10), decodes BOOTSTRAP_RES (20 contacts, ReceiverKey path), seeds routing, and completes HELLO_REQ/RES. aMule version 0x08 accepted by a v10 peer. Fixed the IP byte-order landmine (contact ip is HOST order; dotted quad = Ipv4Addr::from(ip) big-endian view, NOT to_le_bytes). |
| 6c | iterative node lookup (FIND_NODE) | (implemented directly) | DONE (codec + pure lookup) + source-verified. message.rs: KADEMLIA2_REQ 0x21 (type&0x1F|target|receiver, 33B, zero-type rejected) + KADEMLIA2_RES 0x29 (target|count|25B contacts, exact-len 17+25*count, Kad1 dropped); type bytes FIND_VALUE 0x02 / STORE 0x04 / FIND_NODE=FIND_VALUE_MORE 0x0B (GetRequestContactCount). lookup.rs: Lookup state machine - candidates BTreeMap keyed by XOR distance (injective per id), next_queries(ALPHA=3, frontier) returns closest untried + marks tried, on_response folds in closer contacts, converges when frontier fully queried. Convergence test: 160-node net where each node's knowledge is a real 6a RoutingTable (navigable) -> lookup reaches the exact k-closest. Timers/JumpStart/IP-dedup left to the live layer. REMAINING 6c: drive it live over UDP (self-lookup fills the routing table). |
| 6d | source/keyword search + differential gate (resolve a hash) | (implemented directly) | **DONE - WAVE 6 GOAL MET (live).** Codecs (mule-kad): SEARCH_SOURCE_REQ 0x34 (target 16|startPos 2 &0x7FFF|fileSize 8 = 26B) + SEARCH_RES 0x3B (responderID 16|keyID 16|count 2|count x {answer 16|taglist}); SearchResult::as_source distils source tags (SOURCETYPE 0xFF/SOURCEIP 0xFE/SOURCEPORT 0xFD/SOURCEUPORT 0xFC; accepts types {1,3,4,5,6}). read_kad_tag relaxed to eMule behavior (name = length-prefixed string, any length; unknown TYPE still errors). Driver (kad_live): find_node+search_source drive resolve_sources (iterative lookup toward the hash, then SEARCH_SOURCE_REQ to closest in-tolerance); ed2k hash -> Kad target via from_hash (SetValueBE). mule-cli `kad-search <nodes.dat> <hash> <size>`. LIVE: bootstrap -> 15/16 nodes answer FIND_NODE -> 5/10 in-tolerance return SEARCH_RES -> resolves a real source (type 3, real IP:port), reliably. |
| 7 | end-to-end fetch (give a hash, get the file) - RE-SCOPED from EC | (implemented directly) | DONE (orchestration) + loopback-validated + live discovery/connect. mule-engine::fetch: PeerSource unifies Kad/server/peer sources (reconciles the TWO IP conventions in one place - Kad TAG_SOURCEIP = host-order/big-endian view Ipv4Addr::from(ip); server FOUNDSOURCES = eD2k low-byte; verified vs DownloadQueue::KademliaSearchFile). SourceRegistry dedups by addr; fetch_from_sources drives Download across sources (obf when userhash known). Only HighID Kad types {1,4} + non-LowID server ids connectable. mule-cli kad-fetch <nodes.dat> <hash> <size> <out>. 5 unit tests (IP conventions/filtering/dedup) + loopback test (serve->fetch->verify ed2k hash) + dead-source test. LIVE: kad-fetch bootstrapped, resolved a HighID source, completed an OBFUSCATED handshake with a real internet peer (1/1); 0 bytes only because the degenerate test hash isn't a file that peer holds. NOTE: EC protocol deferred (iPad seam is FFI/Wave 8, not a separate EC daemon; EC would be interop-only). |
| 7.5 | pre-Wave-8 engine hardening (identity, Engine facade+lifecycle, resume, download mgr) | (implemented directly) | DONE (WSL-tested). NodeIdentity (persistent userhash/KadID/udpkey/RSA; aMule-compat preferences.dat/preferencesKad.dat). Engine facade = the UniFFI seam: EngineState/EngineEvent + idempotent pause/resume lifecycle (foreground-only). start() loads nodes.dat + resumes .part downloads; checkpoint() saves identity+nodes.dat. download_file = parallel multi-source + retry manager (rides out eD2k upload-queue rationing). 300 tests. Optional coverage remaining at the time - all since closed except one: LowID callback PROVEN live (three-file milestone), full UPnP done (7.8), Kad keyword search done (8c); the last one, AICH byte-validation, closed 2026-07-19 vs real amuled (row 8u). |
| 7.6 | intelligent fetch engine + LIVE three-file completion | (implemented directly) | **DONE - live-proven (2026-07-16).** Search catalog (catalog.rs: dedup by ed2k hash, aggregate availability FT_SOURCES/FT_COMPLETE_SOURCES, trust flags, rank Ok-before-suspect then sources-desc) + completion-optimized fetcher (mule-cli fetch-complete). Downloaded THREE real files to completion, one each from a pdf/wav/txt keyword search, every one ed2k-hash-verified. See the milestone section below. |
| 7.7 | padMule-to-padMule enhancement channel (Layer 1) | (implemented directly) | **DONE + adversarially validated (2026-07-16).** Layer 1 detection: every peer HELLO/HELLOANSWER carries a string-named "padMule" UINT32 marker (`<caps:u24><version:u8>`); `ParsedHello::padmule()` recognizes another padMule. Carrier chosen from SOURCE-GROUNDED proof that stock eMule 0.50a + aMule 3.0.1 read-and-skip a string-named tag with a standard type byte (and THROW/DESYNC on a nonstandard type - so never do that). ADVERSARIAL GATE: the amuled differential test passes with the marker in every hello (real aMule serves all 3 files byte-for-byte). Also fixed a regression the differential test caught in my own queue fast-bail (see below). Layer 2 (opcode on 0xC5, only to confirmed padMule) + NAT-traversal enhancement: designed, not built. Full design: [[padmule-enhancement-channel]]. |
| 7.8 | UPnP-IGD port mapping (on-device HighID) | (implemented directly) | **DONE (2026-07-16), socket-validated.** mule-engine::upnp - full UPnP-IGD, hand-rolled zero-dep (tokio sockets + minimal HTTP/XML), the path NAT-PMP (portmap.rs) does not cover and the one our Xfinity gateway needs: SSDP M-SEARCH discovery -> HTTP GET device description -> parse the WANIP/PPP service control URL -> SOAP AddPortMapping + GetExternalIPAddress. mule-cli `upnp <port>`. 10 tests incl. a loopback integration test that drives http_get + parse_wan_service + soap_add_mapping + external_ip against a mock IGD over a real socket (validates the HTTP/SOAP framing end-to-end). LIVE attempt vs the real gateway got NO SSDP response - but an independent raw-Python M-SEARCH also got nothing, so it is an ENVIRONMENT limit (WSL2 multicast and/or Xfinity UPnP disabled), NOT our code; discovery should work on a real iPad on a UPnP-enabled home net. On-device HighID now has both mapping protocols (NAT-PMP + UPnP). |
| 8a | `mule-ffi` UniFFI seam (Rust <-> Swift) | (implemented directly) | **DONE (2026-07-16), builds + generates Swift bindings.** New crate `crates/mule-ffi` (lib + cdylib + staticlib) wraps `Engine` in an FFI-friendly facade `MuleEngine`: opaque hashes -> hex strings, `EngineState`/`EngineEvent` -> `#[uniffi::Enum]`s, `IdentityInfo`/`DownloadInfo` records, `FfiError`. The async `&mut self` lifecycle is driven on an internal tokio runtime so exported methods are simple/sync (start/pause/resume/shutdown/state/identity/kad_contacts/downloads/drain_events - events polled). uniffi 0.28 proc-macro (`setup_scaffolding!`); a `uniffi-bindgen` bin generates the bindings from the compiled cdylib. VALIDATED here: crate compiles, a Rust-side test drives the full lifecycle + reads events through the FFI types, and `uniffi-bindgen generate --library ... --language swift` emits mule_ffi.swift (open class MuleEngine + all records/enums). Needed a `Download::name()` accessor. On-device wiring waits for the Mac. |
| 8 | `ios/padMule` SwiftUI shell + lifecycle + sideload | (implemented directly) | **BUILDS - a real arm64 .ipa exists (2026-07-16), CI GREEN ON THE FIRST RUN.** Path C from [[mac-toolchain-setup]]: no Apple hardware - a GitHub-hosted macOS runner builds it. `ios/project.yml` (XcodeGen; pbxproj generated in CI, never committed; wires uniffi's module.modulemap + `-lmule_ffi`; iPad-only; deployment target iOS 16 so an older-SDK build installs on iPadOS 26). `ios/padMule/Sources`: PadMuleApp (ScenePhase -> pause/resume, only on `.background`), EngineModel (blocking FFI calls off the main thread; polls `drainEvents()`), ContentView (all three HARD [[lifecycle-and-reactivation]] requirements: honest foreground-only notice, Reconnecting banner, per-transfer Paused badges). `.github/workflows/ios-build.yml` -> `padMule.ipa` artifact, UNSIGNED, needing NO Apple secrets (AltStore re-signs at install; legit because padMule is sideload-only). VERIFIED by downloading the artifact: 588 KB .ipa -> `Payload/padMule.app/padMule` is a **Mach-O 64-bit arm64 executable**; Info.plist = us.ajbconsulting.padMule / MinimumOSVersion 16.0 / UIDeviceFamily [2]; and the Rust engine is genuinely linked in (432 uniffi/mule_ffi strings, `mule_ffi_rustbuffer_free`, `EngineStateFfi`, the "Reconnecting..." string). REMAINING: sideload it to the device and see it run (AltStore + AltServer on the Windows host; Developer Mode on the iPad); then real server/Kad wiring behind the UI. |
| 8b | on-device search + download + HighID/LowID surfaced | (implemented directly) | **DONE + on-device-proven (2026-07-16/17).** Search + add-download exposed through the FFI (`MuleEngine::search`/`add_download`, SearchHit/AddOutcome records) and wired to a SwiftUI search box + Get buttons. `finish_download` re-derives the whole-file ed2k hash (the only check for a single-part file) before saving to Documents (Files-app visible). ServerInfo made durable state + polled snapshot after the "an event is not state" bug hid the ID type. FULL LOOP PROVEN ON THE IPAD: a `test` search -> Get -> a 168KB .doc pulled from one HighID source -> hash-verified -> saved -> opened in Files. Two public-IP leaks aimed at the screenshotted screen closed. |
| 8c | on-device feature round: uploads, cancel, Kad-in-search, unicast HighID | (implemented directly) | **DONE (2026-07-17).** Four features on the running app: (1) **upload/serve** completed files + a "Share uploads"/Leech-Mode toggle (`share.rs` serve_shared reads blocks off disk; the listener PEEKS the first packet to tell a leecher from a called-back source; AtomicBool sharing switch + 8-slot cap; `serve_inbound`). (2) **cancel/delete** a download (cooperative AtomicBool cancel checked in take_blocks + the fetch loops; `.part` deleted; SwiftUI swipe-to-delete). (3) **Kad keyword search merged INTO the search box** (server + Kad concurrently via `tokio::join!` over the disjoint link+node fields; Kad FileResult -> synthetic tagged SearchResultFile so ONE catalog pass dedupes across both by hash; either half may be absent). (4) **on-device HighID via UNICAST SSDP** M-SEARCH at the inferred gateway (`upnp::discover_unicast`; `map_port` falls back multicast->unicast; iOS has no gateway API so infer .1/.254 of our /24; `mule-cli upnp-unicast` for live checks). Feature 4 was initially LIVE-UNVERIFIED (the Xfinity XB8's UPnP toggle is cosmetic - it never answers SSDP, confirmed exhaustively from Windows natively), but is now **LIVE-VALIDATED (2026-07-17)**: Anthony bridged the XB8 behind a TP-Link Archer BE9700, and `mule-cli upnp-unicast 4662` mapped the port against the BE9700's real IGD and returned the real public IP (no double-NAT) - proving the exact iOS unicast-SSDP path end to end. Then confirmed ON THE IPAD itself: padMule earned HighID, the "Port mapping" row read "UPnP: mapped port 4662", and the BE9700's UPnP client list shows `padMule 192.168.0.182 4662->4662`. (Root cause of the earlier LowID: a leftover permanent 4662->dev-box mapping from a validation run squatted the port + a lenient query masked it; fixed with delete-then-add + an honest query.) See [[net-highid-and-port-forwarding]]. Also landed: root LICENSE (GPL v2) + NOTICE + README. 374 workspace tests, clippy + fmt + ASCII clean. |
| 8d | search-panel eMule parity + launch splash + app icon | docs/superpowers/plans/2026-07-17-search-richness-and-splash.md | **DONE + MERGED to main (2026-07-18).** Search now matches eMule's SearchListCtrl FUNCTIONALITY (touch-native, not the 15-col grid): rich rows (New/Downloading/Have status dot, type + media metadata line, complete-sources), client-side sort (7 keys asc/desc) + filter (name/type/trusted/hide-have), and a detail sheet (all fields, ed2k link copy, Download, Search-related). Engine `catalog` surfaces type + media tags it already receives (FT_FILETYPE/FT_MEDIA_* pinned from opcodes.h) + `hit_status` (New/Downloading/Have). FFI `SearchHit` + `HitStatusFfi`. No wire change; sort/filter is pure client-side over the fetched set. Also: a 3s launch splash (2px-stroked splash.png) and the app icon (mascot; white corners flood-filled to an opaque 1024 square since iOS icons cannot have alpha). 379 Rust tests, clippy/fmt/ASCII clean; Swift verified by CI. Brainstorm->spec->plan->execute (specs/plans in docs/superpowers/). |
| 8e | code-fix round (7 fixes from the 2026-07-18 lint) | - | **DONE (2026-07-18).** See the code-fix-round section below. |
| 8f | GUI round (splash 7s; eMule-style top-toolbar nav; real Shared library screen) | - | **DONE (2026-07-18).** Icons Search/Transfers/Shared/Status switch one content area, inline title (never collapses); Shared screen lists the persisted library via FFI `shared_files`. |
| 8g | 0.70b first feature slice (IP filter, search history, wire search filters) | [[emule-070b-features]] | **DONE (2026-07-18).** IP-filter blocklist (ipfilter.dat/.p2p) gating sources+inbound; persisted search recents; availability+size filters pushed onto OP_SEARCHREQUEST. All gated incl. the differential test. |
| 8h | 0.70b second feature slice (server ratings on search rows; per-file unshare) | [[emule-070b-features]] | **DONE (2026-07-18).** Catalog parses FT_FILERATING (masked, aMule decode) -> rating pill + Fake-flag; per-file unshare drops a file from the library + known.met (keeps the file). Comments (OP_FILEDESC, post-connect) deferred. |
| 8i | 0.70b third slice (categories; comment channel; per-source sheet; verified/encryption badge) | [[emule-070b-features]] | **DONE (2026-07-19).** Client-side download categories (buckets + filter chips); OP_FILEDESC comment channel (per-source rating+comment, averaged onto the download row); per-source detail sheet (SourcesView: software, encryption lock, LowID, verified seal, rating, comment); verified checkmark wired (secure-ident-in-fetch deferred). CI-verified iOS build; differential still byte-for-byte. |
| 8j | verified-identity badge goes LIVE | [[emule-070b-features]] | **REVERTED (2026-07-19, commit dbfecad).** The attempt (fetch_one running mutual secure-ident with a delivering source post-transfer, as initiator) had a CONFIRMED HIGH deadlock: padMule advertises sec_ident=0 in the fetch HELLO, so a real uploader never issues OP_SECIDENTSTATE and padMule-as-initiator hangs the full 8s timeout on EVERY delivering source (never verifies, delays completion ~8s). The mock unit test was a false positive (both ends initiated). Reverted; #4 back to PARTIAL (encryption lock live, identity checkmark deferred). Correct future approach recorded in [[emule-070b-features]] #4 + task #32. |
| 8k | 0.70b #20 (author your OWN rating/comment + serve via OP_FILEDESC) | [[emule-070b-features]] | **DONE (2026-07-19), commit ad971f1.** Rate (0-5) + comment your own shared files on the Shared screen (tap/swipe-Rate -> RatingEditorView); stored in known.met (set_file_rating updates the live library + rewrites the entry via update_shared_file_meta); served to downloaders via OP_FILEDESC right after the filename answer, byte-faithful to aMule SendCommentInfo (gated on the peer's AcceptCommentVer + a set rating/comment). serve_shared/serve_inbound thread the peer's accept_comment; the listener captures the HELLO for it (HighID probe still bails). FFI set_file_rating + SharedFileInfo.rating/comment. 243 tests (3 new), clippy/fmt/ASCII clean, differential byte-for-byte, bindings verified. Adversarial review clean (5/5). |
| 8l | 0.70b #7 (per-download priority Low/Normal/High) | [[emule-070b-features]] | **DONE (2026-07-19), commit 7cf3948.** Transfer-row context-menu Priority submenu + up/down glyph. Honestly honored: download_file reads dl.priority() live each round -> concurrent peers Low2/Normal4/High6, rounds 6/8/12; resume_fetches orders by priority. Byte-faithful part.met (FT_DLPRIORITY 0x18 + legacy 0x13, both UINT32, pinned vs PartFile.cpp:928-933; unknown->Normal), byte-layout test. Auto deferred (no periodic source-count recompute exists). Normal == old default (no regression). 406 tests, differential byte-for-byte, Swift bindings verified. Review found 7 real issues, all fixed (ManagerConfig enum, PR_AUTO->High, finish race). |
| 8m | 0.70b #4 REDO (live verified-identity via secure-ident in fetch) | [[emule-070b-features]] | **DONE (2026-07-19), commit 1bd4e00.** The correct redo of the reverted 8j. Fetch HELLO advertises SecureIdent v1 (HelloInfo::with_secident) so a real uploader initiates; run_peer builds the dual-role SecureIdentSession (RESPONDER), sends one proactive OP_SECIDENTSTATE, handles the 3 secure-ident opcodes INLINE in its four read loops (handle_aux_packet) - never waiting, so no deadlock/no delay - and note_source_verified lights the SourcesView seal. NodeIdentity.rsa is now Arc-shared into fetch workers. VALIDATED against a faithful other-side: a mock-uploader-initiates unit test AND the amuled differential (real aMule 3.0.1 "verified: true", byte-for-byte). Blueprint = a source-cited eMule 0.50a study. Adversarial review clean (0 confirmed). Live eMule confirmation = [[emule-peer-oracle]]. 408 tests. |
| 8n | finish-race fix (#35) + the ORACLE SET | - | **DONE (2026-07-19).** (a) #35 (commit 3ad90dd): finish_download moves a completed file THROUGH the lock (PartStore::finish_in_place / Download::finish_to), not via Arc::try_unwrap, so a concurrent Arc holder (downloads() poll, cancel, set_priority) can no longer strand a byte-complete .part. (b) The oracle set for real-peer/real-server validation is now complete: real eMule on the Windows host ([[emule-peer-oracle]], scripts/emule-oracle.sh, mirrored 127.0.0.1:4663) + real Lugdunum eserver run LOCAL + ISOLATED ([[ed2k-server-oracle]], scripts/eserver-oracle.sh, unshare -rn zero-egress) - padMule logs into real eserver. NEXT: #9 global server UDP search against eserver. |
| 8o | 0.70b #9 (global server UDP search) | [[emule-070b-features]] | **DONE (2026-07-19), commit 1d0b81e.** Codec (search.rs): build_global_search_udp reuses the TCP search tree wrapped as OP_GLOBSEARCHREQ 0x98; parse_global_search_res walks chained [0xE3][0x99] records (no count field). CLI global-search fans out to each server UDP port = TCP+4 (landmine), paced, anti-spoof + hash-dedupe, zlib-packed reply handled. VALIDATED: send interoperates with the local isolated eserver ([[ed2k-server-oracle]]); full round-trip proven against LIVE public eservers (real replies -> correct names+hashes). [SUPERSEDED: the follow-ups landed - OP_OFFERFILES is rows 8p/8q, and engine/FFI/app global search shipped (Engine::global_udp_search + the "Search all servers" toggle).] Byte-tested; adversarial review before push. |
| 8p | OP_OFFERFILES (announce shares to the server) | [[ed2k-server-oracle]] | **DONE (2026-07-19), commit 0239a36.** build_offer_files (server_messages.rs) writes the eD2k offered-file packet (LowID complete file -> FILE_COMPLETE_ID/PORT; HighID -> real id+port [SUPERSEDED b236ed0: padMule now ALWAYS sends FILE_COMPLETE markers, so a HighID public IP never enters the server index]; FT_FILENAME+FT_FILESIZE tags), faithful to aMule CreateOfferedFilePacket. ServerLink::offer_files; the engine offers its whole shared library right after login (offer_shared_to; id used locally, never emitted). VALIDATED vs real eserver: it accepted+indexed the file ("1 files, 1 complete, 2 keyw") - making the eserver a DETERMINISTIC search oracle. CLI offer-hold/offer-search. Follow-up (DONE, row 8q): re-offer on a live share change. 413 tests. |
| 8q | OP_OFFERFILES re-offer on a mid-session share change | [[ed2k-server-oracle]] | **DONE (2026-07-19), commit 8dcf7cb.** v1 only offered on connect, so a download finishing mid-session was unfindable until a reconnect. finish_download raises a `shared_dirty` flag; the 1s downloads() poll drains it via Engine::maintain_shares and re-announces the library while a server is connected (offline changes ride connect_server's full re-offer on reconnect). offer_shared_to became an associated fn (shared, link) for disjoint-field borrows. Adversarial 2-lens review (each finding verified) caught + fixed TWO real defects: (1) the flag is only CONSUMED once an offer will actually run - a poll firing while the link is PausedForBackground now KEEPS it, so the next connected poll after resume() (whose success fast-path does NOT re-offer) still announces; (2) offer_shared_to now takes the NEWEST 200 (finish_download appends to the tail), so a just-finished file is never the one the 200-cap drops. Wire packet (build_offer_files) unchanged -> the [[ed2k-server-oracle]] validation still holds. 415 tests (1 new), clippy/fmt/ASCII clean. |
| 8r | #10 related-files search (eMule `related::` protocol) | [[ed2k-server-oracle]] | **DONE (2026-07-19), commit 61166d8.** eMule's "Search related" is NOT a new opcode - it is a normal OP_SEARCHREQUEST whose string is `related::<UPPERHEXHASH>` (CSearchResultsWnd::SearchRelatedFiles; literally a shortcut for typing that into the search box). The server parses the `related::` prefix and returns files its index says co-occur with that hash. Gated on the server advertising SRV_TCPFLG_RELATEDSEARCH (0x40) in the OP_IDCHANGE flags word. Threaded 0x40 -> ServerState::Connected.related_search (connect + resume) -> ServerInfo.related_search -> FFI ServerInfoFfi.related_search; search::related_keyword builds the string (UPPERCASE hex, one keyword leaf, matching eMule md4str); Engine::related_search is server-only + gated, ranked via the same catalog() as a normal search. The app's "Search related" button runs the true protocol when supported, else falls back to a filename keyword search (eMule just greys it out). CLI related-search harness. **LIVE-VALIDATED vs the real Lugdunum eserver 17.15: it ADVERTISES the flag (padMule read related_search=true off the live server) AND accepted the related:: query** - the flag read + query acceptance are proven against the real server software (a populated related RESULT needs a co-occurrence index + a searcher on a different IP than the offerer, not reproducible in the single-IP isolated oracle). 3-lens adversarial review (protocol/Rust/Swift-binding, each finding verified) came back CLEAN. 417 tests (2 new), clippy/fmt/ASCII clean. |
| 8s | Tier-2 #14 statistics tab (Swift Charts) | [[emule-070b-features]] | **DONE (2026-07-19), commit 81a58c6.** A live transfer-rate chart (down/up, last ~60s) + session byte totals + up:down ratio, as a 5th toolbar screen. Split: the ENGINE keeps two process-global monotonic byte counters (`crate::stats`, incremented where received blocks are committed in multi_source and where served blocks are written in share - file DATA only, matching eMule "session down/up"); the UI does all the statistics (EngineModel samples the totals from the 1s poll -> rolling rate history -> Swift Charts StatsView). Global-not-per-Engine avoids threading two counters through the whole transfer call chain (one Engine per process). 3-lens adversarial review caught ONE real bug (all 3 lenses independently): refresh() also fires OFF the 1s cadence (command completions, pause/resume), so sub-second deltas were charted as per-second rates -> FIX: rate = delta / ACTUAL elapsed seconds, and a point is folded only once ~1s of real time passed (off-cadence refreshes keep totals fresh but never pollute the chart). 418 tests (1 new), clippy/fmt/ASCII clean, CI Swift build green. |
| 8t | Tier-2 #30 boolean search expression (AND/OR/NOT) | [[protocol-reference]] | **DONE (2026-07-19), commit 92a67e0.** The keyword field is now parsed as a full boolean expression (was sent as ONE string leaf): implicit-AND between adjacent words, explicit AND/OR/NOT (uppercase), parentheses, "quoted phrases" -> a binary QueryExpr tree serialized in PREFIX order with eMule's exact operator bytes (AND=00 00, OR=00 01, NOT=00 02; NOT is binary "left AND-NOT right", from CSearchExprTarget). Also fixes multi-word search to split into ANDed leaves like eMule. Precedence matches eMule parser.y:52-54 - AND LOWEST (also implicit adjacency), then OR, then NOT highest, all left-assoc (so `a b OR c d` == `a AND b OR c AND d`). Adversarial review found + fixed TWO: (1) unbounded parser recursion aborted the process on deeply-nested input (uncatchable stack overflow, worse on the ~512KB iOS thread stack) -> a nesting depth guard (fallback leaf past 64 levels) + iterative stray-token skip; (2) precedence was INVERTED (OR-lowest) -> restructured to AND-lowest. VALIDATED: 9 byte-layout tests + the real Lugdunum eserver accepted "linux OR (ubuntu AND iso) NOT windows" and "movie 2024 OR 2023 NOT cam". New CLI server-search harness; global-search reuses the encoder. |
| 8u | AICH byte-validation vs real amuled | [[padmule-amuled-oracle]] | **DONE (2026-07-19), commit 61f206b.** aich_master_hash was only structurally cross-checked vs SHA1 (interop-test-fidelity warns a same-way-wrong reference gives false confidence). Now byte-validated against a FAITHFUL other-side: a deterministic 10 MB file (> PARTSIZE, so the multi-part tree branch runs; 55 AICH blocks) hashed by real amuled 3.0.1 yields master root `bc301c26...351100`, read from its known2_64.met AICH backup (`version | root(20) | count(u32) | count*hash`) - padMule produces the IDENTICAL root. scripts/aich-golden.sh reproduces the golden. |
| 8v | Tier-2 #17 preview/open incomplete media (AVPlayer) | [[emule-070b-features]] | **DONE (2026-07-19), commit 39c59fe.** Play a still-downloading media file. A per-Download `preview` atomic switches block selection from rarest-first to forward-SEQUENTIAL (PartFile::next_blocks), so the raw sparse `.part` grows contiguously from offset 0; the UI snapshots the contiguous-from-start prefix (PartFile::contiguous_prefix + copy_file_prefix) to a temp file and plays it with AVKit VideoPlayer. FFI set_preview / preview_snapshot / DownloadInfo.preview+contiguous_prefix. 3-lens adversarial review found 5 issues (3 distinct), ALL fixed: (1) the snapshot copy held the download lock + FFI engine mutex across a large fsync'd copy -> resolve (path,len) under a brief lock, then copy on a blocking thread with NO lock held (safe: `[0,len)` is completed data the writer never rewrites - writes fill gaps at/past len); (2) preview latched on (no setPreview(off)) disabling rarest-first for the session -> revert on sheet dismiss; (3) the "fetch last part early" bias was defeated by the head-only snapshot -> pure sequential + an honest "not enough downloaded yet" message for moov-at-end files (an AVAssetResourceLoader serving ranges is the future full fix). 429 tests (3 new), clippy/fmt/ASCII clean. |
| 8w | Hands-on FFI app-simulation + Kad-only download fix | [[ed2k-server-oracle]] | **DONE (2026-07-19), commit 64d4bf4.** A hands-on APP SIMULATION (crates/mule-ffi/examples/simulate.rs + scripts/simulate.sh) drives the REAL MuleEngine FFI through EngineModel's exact call sequence (boot->start->connect->search->get->transfers->stats->preview->related->leech->pause/resume->shutdown), rendering each "screen" as text - a review of the on-device experience WITHOUT a device (the SwiftUI shell is a thin layer over exactly these calls). Runs inside the isolated eserver namespace (guaranteed Connected/LowID/related-search + lifecycle) or against a live config. It IMMEDIATELY FOUND A REAL BUG: with all eD2k servers down but Kad up, search returned 59 real hits via Kad yet add_download refused every one "NoServer" - it hard-gated on self.server.is_none() BEFORE find_sources (which is explicitly serverless-capable). FIX: drop the gate; a Kad-only client now downloads from HighID Kad sources (LowID-only-without-a-server -> NoSources, not a stuck download). Re-run CONFIRMED the download Started + progressed to 1% via Kad with no server. 430 tests. |
| 8x | Servers screen (no auto-connect; user picks a LIVE server; disconnect; kick dialog; stats table) + result colors | [[ed2k-server-oracle]] | **DONE (2026-07-19), commits 4dd9ecb (feature) + 05fbe5a (review fixes) + 7ceee31 (oracle seeding).** eMule does NOT auto-connect; padMule's start() no longer connects (Kad still bootstraps for serverless search/download). New Servers screen loads server.met at startup and PROBES each via OP_GLOBSERVSTATREQ (UDP, TCP+4): LIVE servers render selectable with users/files stats in a table, dead ones greyed+disabled; the user picks one, THEN connect_to_server dials it. disconnect_server at will. A clean kick raises a prominent alert, driven by EngineEvent::ServerDropped from the 1s heartbeat's ServerLink::poll_incoming. Result rows are colored like eMule GetSearchItemColor (green=have, red=0-source, else primary). **Adversarial review found 3 real bugs, all fixed (05fbe5a) + live-validated:** (1+2 HIGH) the status probe carried NO challenge and parse_serv_stat_res read the response off by 4 bytes - the real OP_GLOBSERVSTATRES is `<u32 challenge><u32 users><u32 files>[...]` (min 12B; the server ECHOES the request's 4-byte challenge first), verified vs eMule 0.50a UDPSocket.cpp + aMule ServerUDPSocket.cpp; the opcodes.h challenge-less `<users><files>` comment is STALE legacy (the stale-header-comment landmine again). Request now sends 6 bytes (proto, opcode, challenge) and the handler verifies the echo == SERV_STAT_CHALLENGE. (3 MEDIUM) peek_dropped only PEEKED one byte, so a kick delivered as a MESSAGE-then-close read as "still up"; replaced with poll_incoming which DRAINS buffered packets (kick message/MOTD -> events) and reports the drop on EOF (cancel-safe: FramedStream buffers). (4 LOW) the Servers ForEach keyed on \.addr, but server.met is not deduped -> keyed on row index. **Live proof vs the isolated Lugdunum eserver: after seeding, the probe reports users=1 files=3 alive=true and SEARCH returns 3 real typed sourced results.** Oracle seeding (7ceee31): offer-hold takes a `|`-separated name list so one client offers a shared library in one OP_OFFERFILES; simulate.sh seeds it before the run. **CORRECTION (a later gapped run overturned the first read):** the boolean-`NOT`/global 0-results were NOT operator quirks - padMule's boolean search is FULLY CORRECT. Spacing searches past the eserver's per-client SEARCH RATE-LIMIT (MATRIX_GAP=20 in simulate.rs), every operator returns the exact right count (implicit-AND=1, AND=1, OR=2, NOT=2/3, phrase=1). The 0s were a rapid-search-burst artifact (the eserver answers only the FIRST search then silently drops the rest); global-0 is the deliberate skip-the-connected-server (the sim's server.met has only that one server). Real takeaway = a padMule UX gap: no client-side search throttle/"wait N s" feedback like eMule. 433 tests, clippy/fmt/ASCII clean. |
| 8y | Group 1: search/servers UX - throttle (#32) + load-more (#13) + server-manager (#18) | [[emule-070b-features]] | **DONE (2026-07-20), commits 9c37495 (features) + 67d7cd4 (review fixes).** Grounded by a map+design workflow over the code + eMule 0.50a authority. **#32** client-side search throttle: aMule's silent 2s guard (SearchDlg.cpp:277), IMPROVED to surface the remaining seconds; SearchOutcome::Throttled; LIVE-VALIDATED vs the eserver oracle (immediate repeat -> "wait 2s"). Wire-neutral. **#13** load-more: OP_QUERY_MORE_RESULT=0x21 (bodiless; server holds the query), parse_search_result_page reads the lenient trailing more-byte (only remaining()==1, per SearchList.cpp iAddData==1), Engine::search_more continues the SAME query on the SAME connection (SearchSession guarded by server_addr + cleared on connect/disconnect), folds pages into the same catalog dedupe, cap 5. **#18** server-manager: reuse bootstrap::http_get_bytes (zero deps, ATS-free raw socket, now 16MiB-capped) to fetch + MERGE server.met from a URL (merge_server_met keeps existing entries+tags, appends new (ip,port)s, validated before an ATOMIC write), pins in a pinned.txt SIDE store (server.met byte-identical), prune_dead_servers keeps alive||pinned||connected. Shared SearchOutcome enum (search/search_more/related_search). FFI + SwiftUI (throttle notice, Load-more button, URL update + star pin + Prune); EngineModel.searchUpdate pure nonisolated helper. **Adversarial review (4 dims -> per-finding verify) confirmed 9, all fixed in 67d7cd4** - HIGH: read_server_met untrusted-count OOM (grow incrementally, the #18 URL weaponized it); MED: prune-drops-connected, togglePin-blocks-main-thread, http-no-size-cap; LOW: stale-session-on-reconnect, related-throttle-ordering, throttle-hides-load-more (x2), non-atomic-write. Gate: 439 tests, clippy/fmt/ASCII clean; Swift 11/11 on the iPad simulator (ios-test). Feature rows 13/18/32 -> DONE. |
| 8z | Group 2: v1 READINESS AUDIT (multi-agent adversarial sweep) + fixes | [[security-model]] | **DONE (2026-07-20), commit 029acdb.** A 7-subsystem-finder audit (codec-robustness / engine-lifecycle / transfer-integrity / privacy-security / kad / ios-swift / resource-bounds), each finding pipelined into adversarial verification + a completeness critic (32 agents, ~2.1M tokens). Confirmed 19 (7 HIGH, 7 MED, 5 LOW); all actionable ones fixed. HEADLINE = the whole FOREGROUND-ONLY lifecycle failure cluster the green test-gate missed: resume() never re-drove downloads (frozen after any bg/fg cycle) -> resume_fetches + re-offer; gap progress never persisted mid-session (suspend-kill lost ALL progress) -> pause()/shutdown() flush .part.met via Download::persist; whole-file-hash-fail stranded at 100% -> re-gap + reset_all_gaps + re-fetch; Kad bootstrap held the shared lock ~48s -> capped 5s; LowID-callback completions never finalized -> heartbeat finalize_completed sweep (try_begin_finalize once-guard); verify_whole_file rehash moved off-lock (spawn_blocking). SECURITY/PRIVACY: HighID client-id (== public IP) leaked into the on-screen notice via Debug-formatting ServerState -> server_state_label; untrusted-count OOM siblings of read_server_met (known.met/part.met/kad message.rs) -> grow incrementally; trapping Int64(u64) UI crash -> Int64(clamping:); unbounded inbound listener -> 200-permit semaphore + EMFILE backoff; Kad sybil (unlimited per-IP//24) -> eMule-style cap at the live layer (interop-safe). Critic gaps: PartFile::normalize now CLAMPS gaps to size (tampered .part.met underflow); UPnP/NAT-PMP parsers inspected + SAFE. Deferred (noted): aborting in-flight tasks in pause() (iPadOS suspends all threads; resume re-drives - low value vs a CancellationToken refactor). Gate: 441 tests + Swift on-simulator; a focused adversarial RE-REVIEW of the subtler fixes (finalize race / gap-persist / rehash-off-lock / sybil) followed. Feeds [[security-model]] part 2 (non-burdensome hardening). |
| 8aa | Reanalysis lint round: doc-drift fixes + test gaps + Rust CI | - | **DONE (2026-08-01).** A full-repo reacquaintance audit (6 parallel area reviewers + a fresh gate run) confirmed the B6/B8/finalize-race fixes in code and found DOC DRIFT, not code rot. Fixed: the stale engine.rs "KNOWN GAP" header (8z's lifecycle fixes ARE in; the comment claimed otherwise and actively misled a reviewer); [[security-model]] scorecard re-synced 11/12/3 -> 13/10/3; index strategy refreshed (#9/#10 had landed); mule-cli docs (20 -> 26 subcommands, 5 commands absent from usage, phantom peer-probe arg, 2 stranded doc comments); CLAUDE.md counts. Code: tag.rs FLOAT32 expect() removed + the 5 untested tag types round-trip-tested; nodes.dat v0/v1 + part.met 0xE1 read tests; Kad responder-builder count-byte caps (kad2_res 255 + test; bootstrap/search u16; BSOB u8); decorative Acquire ordering normalized (multi_source finish_to); the FFI lifecycle test made HERMETIC via an offline_engine test helper (it used to fetch lists + bind 4662 in a unit test); downloads()-is-the-heartbeat documented on BOTH sides of the seam; dead Swift shutdown() removed. NEW: `.github/workflows/rust.yml` runs the unit gate (test+clippy+fmt) in CI - previously CI never ran a single Rust test. Deferred to its own session: wiring FloodTracker + receiver_vk verification (real security-gate work vs eMule 0.50a - see [[security-model]] Band B). Gate: 450 tests, clippy/fmt/ASCII clean, differential byte-for-byte (3 files vs real amuled). |
| 8ab | Bulletproof security batch: Kad receiver-key/verified-bit, ipfilter/sybil/answer-validation, per-part poison recovery, per-IP inbound cap, inbound TCP obfuscation, search availability cap | [[security-model]] | **DONE (2026-08-01).** The user asked to close the security gate: fulfill every eMule/Kad commitment + add non-burdensome interop-safe hardening. Method: a 3-agent review grounded EACH measure in eMule 0.50a authority (classify already-faithful / close-now / defer), then TDD each change, then a 10-agent adversarial workflow re-reviewed the diff (4 confirmed findings, all fixed). Landed (all eMule-grounded): **Kad** - receiver-verify-key computed on receive (bValidReceiverKey == udp_verify_key(our_key,senderIP)) driving a per-contact `verified` bit that is set on the responder, CLEARED on any ip change (anti-inherit), and persisted to nodes.dat; user ipfilter now gates Kad routing inserts; sybil cap 2->1 per IP (eMule RoutingBin.cpp:56) + a known-id IP-change faces the cap (no free hijack); find_node drops over-count (>11 requested, Search.cpp:377) + self/dup-IP answers; port-53 reflector guard version-gated to <=5 (faithful, not blanket). **Peer** - localize_corruption blames the individual bad part per-MD4 and re-opens only it (off-lock, closes the full-re-download loop); per-IP inbound connection cap (16/IP); inbound obfuscation wired (obf_accept auto-detect) + listener advertises crypt-SUPPORTED so crypt-required peers reach us (plaintext byte-identical, differential passes). **Search** - Suspect-row availability cap (min(sources,5), eMule SearchList.cpp:813); a cross-hash filename-spam score was built then REMOVED after the adversarial review showed it false-positived on legit generic names (and it is not a real eMule feature). KEY LESSON re-confirmed: the receiver-key must NOT drop a packet - eMule only hard-drops HELLO_RES_ACK on a bad key; gating responses on it would have broken interop (the authority pass caught this before it shipped). Scorecard 13/10/3 -> **18/5/3**. DEFERRED (documented in [[security-model]], each needs the real-eMule oracle or is a larger feature): serve-side secure-ident, Kad verified-bit ENFORCEMENT, per-source corruption attribution, full AICH recovery, server TCP/UDP obfuscation, credit system. Gate: 463 tests, clippy/fmt/ASCII clean, amuled differential byte-for-byte (3 files). |
| 8ac | Serve-side secure-ident: investigated, DoS-cap landed, feature deferred; AICH-bit decision | [[security-model]] | **DONE (2026-08-01).** Attempted the top remaining Band-A item (serve-side secure-ident). A 4-agent study workflow blueprinted a non-blocking, reweight-only mirror of the proven download-side; implemented test-first (mutual-verify + non-block real-role tests, differential still byte-for-byte). But a 10-agent adversarial review + a hand-traced protocol check found the feature is FUNDAMENTALLY more entangled than the blueprint assumed: (a) advertising sec-ident on the LISTENER hello makes sec-ident-capable peers LEAD with OP_SECIDENTSTATE, which the leecher-vs-source peek can't classify -> it DROPS a leecher's upload AND a called-back LowID source's download (a HIGH regression the direct-serve tests structurally could not catch); (b) NOT advertising doesn't help either, because verifying a leecher REQUIRES it to challenge us, which requires us to advertise. So serve-side secure-ident needs advertise + a listener-peek refactor (drain sec-ident before classifying) + the credit store - a coherent unit that must be validated against a real eMule DOWNLOADING from padMule ([[emule-peer-oracle]]). Per the [[interop-test-fidelity]] rule + the 8j revert lesson, the wiring was REVERTED (do not ship a high-risk hot-path change unvalidatable here). KEPT: a genuine DoS hardening the review surfaced - the secure-ident session now caps RSA signature verification at ONE per connection (secure_ident.rs verify_attempted), bounding a peer that streams OP_SIGNATURE to force repeated public-key verifies (eMule self-throttles the same, m_dwLastSignatureIP); this hardens the EXISTING download path too. Also VERIFIED + corrected an earlier recommendation: the AICH capability bit STAYS advertised - 0x34103212 is byte-verified against real aMule and an unanswered AICH request is non-breaking (ClientAICHRequestFailed re-downloads the part, DownloadClient.cpp:2295); clearing it would diverge from every real client. Scorecard unchanged at 18/5/3 (serve-side reverted). Gate: 464 tests (1 new), clippy/fmt/ASCII clean, amuled differential byte-for-byte. LESSON: a study-workflow blueprint is a hypothesis; the adversarial review + a first-principles protocol trace are what caught that "advertise + handle inline" silently breaks the connection classifier. |
| 8ad | REVERSE peer oracle (real amuled downloads FROM padMule via the eserver) + the multipacket serve bug it caught | [[emule-peer-oracle]] | **DONE (2026-08-01).** Built the reverse-direction oracle that validates padMule's SERVE-side wire bytes against a real client - the direction the amuled differential (padMule downloads FROM amuled) never covered. scripts/amuled-reverse-oracle.sh: an isolated `unshare -rn` namespace with PUBLIC-looking IPs on distinct /24s (amuled filters PRIVATE server IPs; a source must differ from the server IP + amuled's own IP), the real Lugdunum eserver relaying the source (amuled will NOT dial an offline link-embedded source, but WILL request sources from a connected server and dial them), padMule serving + offering the file (serve-file gained an optional `[eserver-host eserver-port]` to log in + OP_OFFERFILES), and amuled driven headlessly via its ED2KLinks file. Getting there took: multi-IP namespace, the server.met IP byte-order (dotted-quad, not reversed), public-vs-private server-IP filtering, and separating padMule's HighID IP from the eserver IP. **It IMMEDIATELY CAUGHT A REAL SERVE-SIDE BUG:** a real amuled could not download from padMule - it saw padMule's HELLO advertise ext_multipacket and sent the bundled OP_MULTIPACKET_EXT (0xa4) request, which padMule's serve loop drops (it answers only the individual opcodes), so the transfer died at 0 bytes. Same class as the Wave-4d extended-requests bug (advertise no capability you do not honour); every prior test missed it because padMule-as-downloader sends the INDIVIDUAL opcodes, so no symmetric path ever delivered a multipacket to padMule's serve side. FIX (commit 04d9116): clear the multipacket + ext_multipacket bits so real clients fall back to the individual OP_REQUESTFILENAME sequence padMule serves; honouring multipacket serve-side + re-advertising is a tracked FOLLOW-UP. With the fix, the reverse oracle PASSES - real amuled 3.0.1 downloads a file from padMule byte-for-byte, the FIRST validation of padMule's serve bytes against a genuine client. Gate: 464 tests, clippy/fmt clean, amuled differential still byte-for-byte (download unaffected). |
| 8ae | Serve-side OP_MULTIPACKET(_EXT) - HONOUR the capability (follow-up to 8ad's "stop advertising it") | [[emule-peer-oracle]] | **DONE (2026-08-02, commit deab2ab).** 8ad stopped advertising multipacket (padMule dropped the bundled request); this makes padMule actually ANSWER it, so it can advertise the capability byte-equal to a real aMule/eMule again. TDD: `build_multipacket_answer(hash,name,available)` -> `<hash><REQFILENAMEANSWER+name><FILESTATUS+status>`, and it deliberately does NOT parse the bundled sub-requests - the downloader's OP_MULTIPACKETANSWER reader processes each sub-answer WITHOUT cross-checking it against its own request (aMule ClientTCPSocket.cpp:1264, eMule ListenSocket.cpp:1347), so answering name+status unconditionally is safe and sidesteps needing the peer's ExtendedRequestsVersion (untracked serve-side). eMule DOES `default: throw` on an UNRECOGNISED sub-answer opcode, so the builder only ever emits the two both readers handle (0x59/0x50). Wired into BOTH serve paths (transfer_session::serve and the production share::serve_shared); re-advertised the bits in peer.rs (MISCOPTIONS1 0x34103212, MISCOPTIONS2 0x438). **The 10-agent-equivalent adversarial review earned its keep: it caught a CRITICAL production regression the reverse oracle initially MISSED** - a re-advertised multipacket bit makes a capable downloader LEAD with OP_MULTIPACKET(_EXT) instead of OP_REQUESTFILENAME, but the inbound listener's `is_upload_request()` gate (engine.rs start_listener) did not recognise 0x92/0xa4, so the production upload path silently DROPPED the connection unanswered. The oracle missed it because serve-file used the minimal serve() path, bypassing the gate. Fix: add the opcodes to is_upload_request + unit test, AND route serve-file through the REAL production path (is_upload_request gate + serve_shared) so the oracle now validates the classifier end-to-end (also caught: serve_shared must not clear a file already latched on a not-found multipacket - aMule sets UploadFileID only when found; and a doc comment that mis-stated eMule as "no default throw"). Gate: 469 tests, clippy/fmt/ASCII clean, amuled differential still byte-for-byte, reverse oracle PASSES through the production path with multipacket ON (SERVE_DEBUG trace confirms padMule receives + answers 0xa4). LESSON reinforced: an oracle only tests the code path it actually drives - point it at the PRODUCTION path, not a demo shim. |
| 8af | SERVE-SIDE secure identification (Batch 1 of the secure-ident + credits gate item) | [[security-model]] [[emule-peer-oracle]] | **DONE (2026-08-02, commit 4d874e5).** Closes the "SERVE side does none" half of the secure-ident scorecard row: padMule now verifies a DOWNLOADING peer's RSA identity on the upload role (the mutual counterpart of the oracle-proven download-side verify). Grounded by a 5-agent understand-workflow (eMule wire authority + the reverted 8ac + the credit dead-code + the oracle plan). The 8ac attempt was reverted because advertising secure-ident makes a capable peer (leecher OR called-back source) LEAD with OP_SECIDENTSTATE, which the old first-packet peek dropped - killing both the leecher's upload and the source's download. FIX: `classify_inbound` - a bounded DRAIN that feeds the secure-ident prefix into a SecureIdentSession (writing replies) and re-applies the leecher-vs-source discriminator on what FOLLOWS (first upload-request opcode => leecher; per-read timeout/silence => source). The leecher's OP_PUBLICKEY/OP_SIGNATURE arrive AFTER its file request, so `serve_shared` finishes verification interleaved with serving (a new aux branch mirroring the download-side handle_aux_packet), NEVER blocking the transfer. Both serve entry points (engine accept task + CLI serve-file) share the path, so the oracle validates what the device runs. Invariants (adversarial review found NO bug): NEVER-REFUSE (verification is observational this batch - engine on_verified is a no-op, CLI logs); cancel-safe (timeout per-read, never around a write); the source path survives (download_from_peer passes sec=None, run_peer tolerates stray secure-ident). Hardened the drain with a packet-COUNT bound AND a per-packet SIZE cap (a real secure-ident packet is <=~120 B). Tests: classify leecher/source/drain/flood; a faithful mock LEECHER that INITIATES (the real role) verified mutually; the 8ac guard end-to-end (sec-ident-then-silent => Source => clean download_from_peer). **Reverse oracle now ALSO asserts padMule verified amuled's secure identity serve-side - PASSES against real amuled 3.0.1** (byte-for-byte download + verified). Gate: 296 mule-engine lib tests, full workspace green, clippy/fmt/ASCII clean, differential byte-for-byte. NEXT (Batch 2/3): wire the DEAD credit store (clients.met + accrual + score_ratio_ident) so the verified identity reweights the upload queue - Batch 3 (the live-queue reweight) is a USER-DECISION-gated, no-oracle hot-path change. |
| 8ag | LIVE credit store (Batch 2 of the secure-ident + credits gate item) | [[security-model]] | **DONE (2026-08-02, commit dbcc0ab).** Turned the DEAD credit code ([[decisions-and-lessons]]: score_ratio_ident/resolve_ident_state/clients.met had ZERO live callers) into a running account. New `credit_store::CreditStore` (userhash-keyed clients.met CreditEntry map): loaded on Engine::new (expiry-pruned), saved in checkpoint() on pause() (the only reliable iPadOS hook); binds a peer's verified key on serve-side verification (ServeSec's on_verified sink now carries the pubkey) with eMule's anti-theft WIPE (ClientCredits.cpp:377 - a first key-bind to a record with prior credits deletes them to 1 byte, so a stolen userhash cannot inherit keyless history via a fresh key); accrues UPLOAD bytes per peer (serve_shared, next to stats::add_uploaded) - accrual UNGATED, the secure-ident gate lives at SCORE time (score_ratio_ident, as eMule gates GetScoreRatio); and exposes CreditStore::score (resolve ident + ratio, clamped [1.0,10.0] - REWEIGHT-ONLY, never denies a slot). All LOCAL policy, nothing on the wire. Tests: 6 store unit tests (clients.met round-trip, expiry prune, anti-theft wipe, ident gate) + an end-to-end accrual test (serving a file lands uploaded bytes in the record byte-compatibly) + a pause()-writes-clients.met lifecycle assertion. Gate: 303 mule-engine lib tests, full workspace green, clippy/fmt/ASCII clean. REMAINING credit pieces (Batch 3, still open): (a) DOWNLOAD-side accrual (credit a source for bytes it gives us - needs the source userhash threaded through the download hot path's ~4 callers); (b) the upload-queue REWEIGHT that CONSUMES the score - the one no-oracle hot-path change (the UploadGate FIFO semaphore needs priority admission), a USER-noted behavioral change to serving. Verification (8af) is thus still OBSERVATIONAL until the reweight lands. |
| 8ah | Credit REWEIGHT + download accrual (Batch 3 - COMPLETES the secure-ident + credits gate item) | [[security-model]] | **DONE (2026-08-02, commit b6262c8).** The credit system now actually reorders serving. REWEIGHT: replaced the fair-FIFO upload semaphore with a priority `UploadGate` - a mutex over a score-ordered waiter set (`BTreeSet<(Reverse(score_key), seq)>`) + a tokio `Notify`. `try_grant` serves a free slot only when nobody is queued (no queue-jumping); `enqueue` admits by CREDIT SCORE; a freed slot goes to the BEST-scored waiter. `leecher_score_key` resolves the leecher's stored ratio gated by this-session verification (score_ratio_ident). NEVER-REFUSE preserved: score is clamped >=1.0 so it only reorders - the sole refusal is a full queue, identity-independent. `QueueWaiter::granted` uses the `Notify` enable()-before-check pattern (no lost wakeups); its Drop removes it from the set (a disconnect/timeout never blocks the peers behind it). DOWNLOAD ACCRUAL (REQUIRED, not optional - score_ratio gates on downloaded<1MB, so without it every leecher scores the 1.0 floor and the reweight is inert): a source's bytes are credited to ITS userhash, threaded credit_store -> download_file -> fetch_one -> run_peer + the engine's called-back-source arm. Adversarial review (7-point concurrency trace: lost wakeup / slot leak / double-free / starvation / try_grant race / accrual keys / reweight timing / BTreeSet ordering) came back CLEAN - no findings. Tests: gate ordering + drop-cleanup + refuse-only-at-capacity; upload + download accrual end-to-end (byte-compat via clients.met); AND an end-to-end reweight SIMULATION (Anthony's point that queue reordering IS provable by simulating clients) - two competing leechers over loopback, a HIGH-credit one queued SECOND is served before a fresh one queued FIRST (FIFO could never do this), 10/10 non-flaky. Gate: 307 mule-engine lib tests, full workspace green, clippy/fmt/ASCII clean, amuled differential + reverse oracle still byte-for-byte (NO interop regression from the reweight/accrual threading). This CLOSES the secure-ident + credits gate item (Batches 1-3): serve-side verification (8af) is no longer observational - the verified identity now feeds the upload-queue reweight. |
| 8ai | Per-source corruption attribution + ban (eMule CorruptionBlackBox) | [[security-model]] | **DONE (2026-08-02).** Builds on the per-part blame from 8ab (localize_corruption): when a downloaded part fails its MD4, the SOURCE that delivered it is now attributed + BANNED from being a source for that file. Download tracks which source IP(s) contributed each data part (recorded on commit, under a std lock off the async transfer lock); on a part-hash failure localize_corruption bans the SOLE contributor of each bad part and clears that part's contributor set (so a re-fetch re-attributes). BOTH serve paths skip a banned IP: the outbound fetch sweep AND the inbound called-back-source arm. KEY SAFETY: only a SOLE contributor is blamed - a part fed by >1 source blames NOBODY, because without AICH block hashes we cannot tell which block was bad, so a good source is NEVER false-banned (tested: two-contributor part bans neither). ADVERSARIAL REVIEW caught two coupled bugs (same class as the multipacket is_upload_request gap): (1) the ban was enforced ONLY in the outbound sweep - the engine's called-back (LowID) source arm bypassed it, so a LowID poisoner (which reaches us ONLY by callback) could re-poison forever; (2) a SocketAddr key would never match a LowID source anyway (fresh ephemeral port each callback). BOTH fixed by keying the ban on IP + checking it in the callback arm (tested: a banned IP is caught on a different port). HONEST LIMITATION: an attacker who always shares a poisoned part with a good source evades the sole-contributor ban (finer per-block attribution needs the deferred AICH block recovery); this is strictly better than no attribution and never false-positives. Ban is per-download/transient (matches eMule's per-file CorruptionBlackBox); a re-query can still find fresh sources if the banned one was the only one (better to stall than accept a poisoned file). Tests: the localize test now asserts the bad part's sole source is banned + the good source is not; a two-contributor-no-false-positive test; single-part decline unchanged. Gate: 308 mule-engine lib tests, full workspace green, clippy/fmt/ASCII clean, amuled differential byte-for-byte (the commit source-threading did not regress the download path). Scorecard Poisoning-defense row PARTIAL -> OPERATIONAL; 20/4/2 -> 21/3/2. |
| 8al | Outbound crypt-dial policy: obfuscate only a source that advertised crypt | [[obfuscation-posture]] [[security-model]] | **DONE (2026-08-02, commit 48065ab).** The last open finding from the 8aj audit. padMule dialed RC4 whenever it knew a source's userhash; eMule's `Connect()` (BaseClient.cpp:1647) ALSO requires `SupportsCryptLayer()` - the hash is the RC4 key seed, necessary but NOT sufficient. Obfuscating blind hands a crypt-DISABLED peer a handshake it reads as a malformed eD2k packet, so it hangs up; the eMule source was re-read to check for a plaintext retry and there is NONE (SetConnectionEncryption is called exactly once in Connect), so such a source was simply lost. `crypt_policy.rs` already held the correct predicate but had ZERO live callers - the audit's "dead code" finding and the "no fallback" finding were the same bug. KEY INSIGHT (why this is fixable at all): the peer's crypt bits arrive WITH THE SOURCE, before any hello exists - eMule publishes them per source and reads them back (Kad `TAG_ENCRYPTION` 0xF3, Search.cpp:798/:1096), and the SX v4 record + obfuscated server source/callback carry the same connect-options byte. padMule already parsed the SX/server byte but DROPPED the Kad tag; it is now parsed. New `PeerCrypt::from_connect_options` decodes it exactly as `SetConnectOptions` does (0x01 supported / 0x02 requested / 0x04 required; 0x08 is direct-UDP-callback and must NOT be read as crypt), and `PeerSource::should_obfuscate` routes every dial through the one shared predicate (`Capabilities` gets a `From` impl so the hello path shares it). No crypt info -> PLAINTEXT, which is eMule's own default and can never break a peer. Also fixed: SourcesView's encryption lock reported "obfuscated" whenever a userhash was known - it now reports the connection ACTUALLY made. TDD: unit RED first (byte decode + the unknown-peer rule); the two end-to-end loopback tests were written after, so the plaintext one was VERIFIED to FAIL under the restored old rule before being kept. Gate: 506 tests, clippy/fmt/ASCII clean. Oracles: amuled differential byte-for-byte; the reverse oracle PASSES; AND - since the differential test only covers plaintext - a dedicated run proved an OBFUSCATED download from real amuled is byte-for-byte with secure-ident verified through the RC4 stream (that check first "failed" on a HARNESS bug: aMule applies the eMule marker bytes hash[5]=14/hash[14]=111 in memory, so the raw preferences.dat bytes are not the userhash that seeds RC4 - [[verify-before-reporting]] again). NOTE: source-exchange sources are still not fed into the fetch registry (only Kad + server are), so their crypt byte is parsed but unused - a pre-existing gap, unchanged here. |
| 8am | Source exchange WIRED (ask + answer) - the third discovery channel | [[protocol-reference]] [[emule-peer-oracle]] | **DONE (2026-08-02, commit 8973752).** Anthony asked how far to take SX and chose BOTH halves. The audit line was "the SX crypt byte is parsed but unused"; investigating showed the whole CHANNEL was codec-only - not one of build_request_sources/build_request_sources2/parse_answer_sources/parse_request_sources/build_answer_sources had a caller outside its module, and opcodes 0x81-0x84 appeared in NO peer read loop - while padMule advertised `source_exchange = 3` in its byte-verified hello. So padMule (a) never learned sources from peers, leaving Kad + server as its only channels, and (b) advertised a capability it ignored (the 8ad/8ae bug class). SEVERITY was checked, not assumed: an unanswered SX request is a NORMAL protocol outcome (aMule rate-limits it and may legitimately send nothing, ClientTCPSocket.cpp:1110-1125, `if(tosend)`), no throw and no ban - unlike the multipacket bug, where the unanswered packet WAS the file request. **ASK:** run_peer sends OP_REQUESTSOURCES2 alongside its file request, where aMule sends it (DownloadClient.cpp:249-258 bundled / :305-320 standalone), gated on the peer advertising SX (IsSourceRequestAllowed's first clause) and on a once-per-peer claim - upstream rate-limits per client at SOURCECLIENTREASKS=40min (x MINCOMMONPENALTY=4 for a common file), so once per download is simpler and never MORE aggressive. handle_aux_packet folds the answer in; download_file drains it into the dial pool each round, re-validated by from_sx (LowID + unroutable refused) AND re-checked against the user blocklist, which a mid-sweep source would otherwise bypass (the ip_filter is now threaded into download_file for exactly this). **ANSWER:** serve_shared answers OP_REQUESTSOURCES(2) from the file's SERVED list - the peers we are uploading to or holding queued - which is precisely aMule's m_ClientUploadList/CreateSrcInfoPacket, not a general "every peer we ever saw" list. The registry lives on UploadGate (upstream's own data model: uploading + on-queue), entries are removed by an RAII ServedGuard on every exit path, the asker is never named to itself, unroutable/LowID peers are skipped, and we stay SILENT when we know nobody (upstream returns NULL and sends nothing). Per-connection extras became PeerSession / ServeSession bundles, keeping both hot paths under clippy's argument limit. Tests: an end-to-end ask against a faithful responder (which asserts we request the newest SX version and that the transfer still completes), and an answer read back through parse_answer_sources incl. the SX v4 crypt byte + the never-name-the-asker rule. One test bug was informative: 203.0.113.x/198.51.100.x are TEST-NET DOCUMENTATION ranges, so is_routable_public_v4 correctly refused to hand them out. Gate: 509 tests, clippy/fmt/ASCII clean; amuled differential byte-for-byte + reverse oracle PASS (no serve-path regression). HONEST LIMITS: a real client CONSUMING our answer is not yet oracle-proven (amuled need not ask during a run), and an SX1 request goes unanswered unless the asker announced an SX1 version in its hello (SX2 - what eMule 0.50a and aMule 3.x send - is fully answered). |
| 8an | GUI: eMule-style function strip (icon over name) | [[emule-070b-features]] | **DONE (2026-08-02, commit 4226d7d).** Anthony asked for stylish, non-garish iOS-appropriate icons with the function NAME under each, taking cues from eMule and improving on them. The six functions moved OUT of the cramped navigation-bar icon row into a proper FUNCTION STRIP under the title: each item is a ~19pt hierarchical SF Symbol above an 11pt caption, evenly spread, selection carried by the accent tint + a soft rounded fill (no borders, no gradients, no chrome) on a `.bar` material with a hairline divider. This keeps eMule's habit - every function always visible, always in the same place, so nothing is hunted for - while reading as iOS rather than as a ported Windows toolbar. Icons were also re-chosen to say what each function DOES: Transfers is now `arrow.up.arrow.down.circle` (padMule uploads as well as downloads, as eMule's own Transfers pane covers both) and Status is `network` (the screen is about connectivity - server link, ID, Kad, UPnP) rather than a bare gauge; Statistics is labelled "Stats" so six names sit comfortably. All symbols ship with iOS 16 (the deployment target) so none renders as a blank square, and each carries its full accessibility label + `.isSelected` trait. Swift is type-checked ONLY in CI on this box, so this shipped as a CI-validated push. |
| 8ao | Wave-10 Batch B: ENFORCE the Kad IP-verified bit | [[kad-verify-oracle]] [[security-model]] | **DONE (2026-08-02, commit 5ef4c2e) - wave 10 COMPLETE.** The bit had been TRACKED, persisted and IP-change-cleared since 8ab, and read NOWHERE. eMule enforces it unconditionally in `CRoutingBin::GetClosestTo` (RoutingBin.cpp:244): a contact is offered as a lookup candidate ONLY when `IsIpVerified()`, because an unverified contact has never proven it lives at the address we hold - which is exactly the opening a spoofer uses to steer a search. `closest_to` now filters identically (a FILTER, not a reordering), with an off-by-default escape hatch for a live diagnostic. Also landed eMule's companion rule from `IsAcceptableContact` (RoutingZone.cpp:1014-1020): a lookup ANSWER naming an already-VERIFIED KadID at a DIFFERENT ip/port is refused outright - a KadID is semi-public, so re-pointing a known node's identity is the takeover move; wired into find_node's answer validation beside the existing self/dup-IP filters. THE RISK (checked, not assumed): a naive port could STARVE lookups, since a freshly downloaded nodes.dat carries verified=0 for every contact. It does not, and the reason is structural - bootstrap dials the nodes.dat list DIRECTLY (never through closest_to), the responder is verified by its receiver key, and a Lookup expands from its own answers rather than re-seeding from the table, so the verified pool fills as the session runs. PROVEN LIVE against the real Kad network with the gate ON: bootstrap resolved 20 contacts and `kad-keyword linux` returned a full page of real results. Two pre-existing fixtures (routing closest-by-XOR, the 160-node lookup-convergence sim) now mark their contacts verified - both test ordering/convergence, not verification. Gate: 512 tests, clippy/fmt/ASCII clean. Scorecard Kad node-ID/IP row PARTIAL -> OPERATIONAL; 22/2/2 -> **23/1/2**, leaving AICH block recovery (an optimization) as the only PARTIAL. |
| 8ap | Research-pass fixes: SO_REUSEADDR, free-space guard, serve-path request scoring | [[ipados-constraints]] [[security-model]] | **DONE (2026-08-02, commits 6e027ea + 2ff5939).** Three gaps found by the aMule-3.0.0->3.0.1 delta review and the eMuleAI mining pass, each VERIFIED against padMule's code before acting. **(1) SO_REUSEADDR on the Kad UDP socket** - aMule 3.0.1 PR #121 fixed exactly this ("without this Kad and the ed2k client UDP stay broken until the user restarts amule", LibSocketAsio.cpp:1447-1456) and padMule shared its SHAPE on the one path CLAUDE.md calls a HARD requirement: a FIXED Kad port that pause() closes and resume() rebinds every background cycle. Verified in the mio source that tokio's `UdpSocket::bind` does NOT set the option while `TcpListener::bind` DOES - so the TCP listener was already covered and only Kad UDP was exposed. Now bound via socket2. Honest scope: UDP has no TIME_WAIT so a clean drop frees the port anyway; this covers iPadOS reclaiming the socket WITHOUT a clean close. **(2) Free-space guard** - [[ipados-constraints]] required "guard free space before preallocating part-files" and it was NEVER BUILT: `PartStore::create` called `set_len` with no check anywhere in the tree. A sparse set_len succeeds INSTANTLY for a size the volume cannot hold, so the failure surfaced mid-transfer instead of at the tap. Now refused up front, keeping a 256MB margin (iPadOS degrades badly at zero free space), failing OPEN when the volume cannot be read, with the reason reaching the user (AddResult::Failed -> AddOutcome::Rejected -> the Swift `.rejected` case). Its own test caught a real policy bug: `saturating_add` silently stops enforcing the margin at the top of the u64 range, so an overflowing size is now refused outright. **(3) Serve-path request scoring** - padMule answered every metadata request with no per-peer limit; raised in priority by 8am, since OP_REQUESTSOURCES answering is precisely the cheap-request/expensive-response shape upstream throttles. Follows eMule 0.50a, which DIFFERS from aMule here: `AddRequestCount` (UploadClient.cpp:895-918) scores per (client, FILE) - re-asking inside MIN_REQUESTTIME (10 min) increments, BADCLIENTBAN=4 refuses, a polite gap DECREMENTS - whereas aMule uses a per-client +3/ban-at-10 counter. Scored opcodes are metadata only; OP_REQUESTPARTS is never scored (a real downloader streams them) and a peer holding a slot is exempt, mirroring upstream's DS_DOWNLOADING carve-out. Gate: 516 tests, clippy/fmt/ASCII clean; differential + reverse + Kad oracles ALL re-run PASS - a real amuled still downloads FROM padMule byte-for-byte, so no legitimate client is caught by the gate. |
| 8aq | part.met previous-generation backup + recovery on load | [[ipados-constraints]] | **DONE (2026-08-02, commit cb57ca1).** Item 4 off the 2026-08-02 research-pass backlog, and plain upstream parity rather than an invention. padMule's atomic tmp+rename already made a TORN .part.met impossible, but a LOST or UNREADABLE one still stranded a download outright - and on iPadOS the app is killed at suspension as ROUTINE behavior, so that is not the exotic case it is on a desktop. BOTH authorities keep a previous generation and NEITHER treats it as decorative: eMule 0.50a LOADS `<name>.part.met.bak` when the live met fails (DownloadQueue.cpp:103), and aMule installs it by renaming the old met aside before the atomic install (PartFile.cpp:855-865) - one content write plus two metadata renames, with the live met never absent or partial at any observable moment. padMule now follows aMule's ordering exactly, and `PartStore::open` falls back to the .bak when the live met is MISSING or UNREADABLE. The promotion is best-effort: a first save has nothing to promote, and a failure to promote must never cost the new content. NOTE the test caught a FIXTURE error rather than a code error (the opposite of the usual case): `create()` already writes the first met, so the .bak legitimately appears on the NEXT save, not the first - the test was corrected, not the behavior. Gate: 517 tests, clippy/fmt/ASCII clean, amuled differential byte-for-byte (the .part.met stays byte-compatible - only its save ORDER changed). REMAINING from that backlog (task #5): Download Inspector, known/cancelled marking in search results, majority-filename rename, throughput-based slot recycling, bulk select, persisted search results, ipfilter auto-update. |
| 8ar | Post-3.0.1 aMule delta review + the UPnP IGD:2 fix it found | [[ref-source-trees]] [[net-highid-and-port-forwarding]] | **DONE (2026-08-02, commit 6a9af52).** Reviewed aMule 3.0.1 -> master (`3.0.1-405`) after refreshing the study snapshot. METHOD NOTE worth keeping: a tree diff was USELESS as a filter because commit 041a2fa3 clang-formatted every file (260+ files 'differ' with zero semantics), so the review switched to PER-FILE GitHub commit history, which is what produced the load-bearing NEGATIVE results. **THE THREE NEGATIVES matter most:** SHAHashSet.cpp (AICH), CorruptionBlackBox.cpp and ClientCredits.cpp have had ONLY clang-format/clang-tidy changes since 3.0.1 - so wave 11 can be built against the vendored oracle's AICH code with no risk of porting an already-fixed bug, and padMule's 8ai attribution + 8ag/8ah credit store have no upstream counterpart fix to mirror. **THE ONE SHARED BUG (fixed here): UPnP version-exact service matching.** An IGD:2 gateway advertises `WANIPConnection:2`; padMule compared serviceType with an EXACT equal against a hard-coded `:1`, so every IGD:2 router failed classification -> NoWanService -> no port map -> stuck LowID, on the path padMule's whole HighID story runs through. aMule hit the identical pathology (75cdaf2b, TypeMatchesIgnoringVersion). Fixed version-tolerantly, with the version required present+numeric (so `WANIPConnectionFoo` cannot match) and WLANConfiguration still rejected at any version; SEARCH_TARGETS stays versioned deliberately (an M-SEARCH ST must carry one, and UPnP back-compat already makes a v2 device answer a v1 search); the matched type is carried back as-is because it becomes the SOAP xmlns:u. Live-checked against the real gateway (an IGD:1, proving no v1 regression). **AUTHORITY SPLIT EARNED ITS KEEP TWICE:** (a) aMule master defines FT_LASTUPLOADED 0x55 / FT_SHAREDSINCE 0x56, but eMule 0.50a already owns 0x55 as FT_MAXSOURCES and has the same semantic at FT_LASTSHARED 0x34 - padMule follows eMule and must NOT import aMule's numbers (recorded as a landmine in [[protocol-reference]]; harmless today only because known_met is tag-generic and round-trips verbatim); (b) aMule master's [3,24] BDP block-depth clamp cites 'eMule's own pending range' but eMule 0.50a only ever requests 1-3 on the peer path, so that is aMule-only policy, not parity. **QUEUED, NOT DONE:** continuous block-request top-up (task #7 - padMule is stop-and-wait, shallower than BOTH authorities; eMule tops up as each block completes) and the wave-11 AICH design inputs folded into task #1 (do NOT port 3.0.1's racy orphan-prune, which the vendored oracle still has; route localize_corruption into block-level recovery; ship the AICH rate limit with the O(1) index). Everything else classified N-A or already-done, incl. the Kad HELLO_REQ 0x04 assert padMule had already anticipated. |
| 8as | Status line goes stale after a connect (found by DRIVING the device) | [[ipad-usb-tooling]] | **DONE (2026-08-02).** The first agent-driven on-device session (touch control via WebDriverAgent) read the Status screen and caught a self-contradiction no test had: the "Status" row said **"Not connected - pick a server"** while the SAME screen showed `Server: <addr>` and `ID: HighID`, and the Servers screen said "Connected". Root cause is the [[decisions-and-lessons]] "an event is not state" bug in its mirror image: the app's durable `status` field is fed by `EngineEvent::Status` ALONE (`EngineModel.apply`), `Server(..)` text is routed to a TRANSIENT notice banner, and `connect_to_server` emitted only `Server("Connected to ...")` - so the row kept whatever `start()` last set, forever, until a pause/resume. `start`/`pause`/`resume` all emit `Status(online_status())`; connect and disconnect did not. FIX: emit it on connect (AFTER `self.server` is set, since `online_status` asks `is_online`), on disconnect, AND on a FAILED dial (which has already dropped any previous link, so the row must stop claiming it). The misleading Swift comment that caused the gap - it called the line "polled" when it is event-fed - is corrected in place. TDD: the test reproduces the device symptom offline against a mock login server and was RED first (`got [Server("Connecting"), Server("Connected to ... (HighID)")]` - no Status event at all). Gate: 519 tests, clippy/fmt/ASCII clean. **DEVICE-VERIFIED 2026-08-02** (build 0cf6791, signed on this box, installed via Sideloadly): with NO pause/resume performed, the Status row reads `Connected to 176.123.5.89:4725 (HighID)` the moment the connect returns - the exact spot where the old build still said "Not connected - pick a server". It also survives a background/foreground cycle. LESSON: CI, 518 unit tests and all three oracles were green while the main status screen contradicted itself - some classes of bug are only visible on the device, which is the whole argument for [[ipad-usb-tooling]]. |
| 8at | Kad checkpoint gap + UPnP re-verification (the two reanalysis findings) | [[net-highid-and-port-forwarding]] [[kad-verify-oracle]] | **DONE (2026-08-02).** (1) **Kad checkpoint gap.** `start_kad` folded the live node's table into `self.routing` exactly ONCE, at the end of bootstrap; after that the two diverged, and `pause()` dropped the node before `checkpoint()` wrote the STALE snapshot - so every contact learned by a lookup and every per-peer UDP verify key captured by `note_responder` was discarded. That defeats the wave-10 goal stated in `routing_to_nodes` ("so we can echo it after a restart"), and on iPadOS pause is the ROUTINE path. Fixed in `checkpoint()` itself rather than at the drop site, so `shutdown()` is covered too: `checkpoint_contacts` unions persisted + live with the LIVE entry winning (it is the fresher observation and the one carrying this session's key). TDD, and the test was MUTATION-CHECKED - neutering the merge turns it RED with `udp_key: 0, verified: false`, the exact stale snapshot. Observed live the same evening: the device's Kad table grew 172 -> 192 contacts during one session, all of which the old code would have thrown away. (2) **UPnP re-verification.** The research pass OVERTURNED the planned fix: a finite lease was going to be the remedy for a stale mapping, but eMule 0.50a (miniupnpc `upnpcommands.c:339-340` AND the Windows-COM path `UPnPImplWinServ.cpp:866-867`), eMule 0.70b, aMule 3.0.1 and aMule master ALL request `NewLeaseDuration=0` - nobody uses a finite lease, so nobody needs a renewal timer. What eMule does instead is `CheckAndRefresh` (`UPnPImplMiniLib.cpp:190-210`): ask `GetSpecificPortMappingEntry` whether OUR mapping still exists and re-add ONLY if it is gone, never deleting. padMule mapped the port once per launch and never looked again, so a mapping lost while suspended left it silently LowID for the rest of the session. Ported as `upnp::refresh_mapping` + `refresh_and_remap`, wired to BOTH of eMule's applicable triggers: on resume (eMule refreshes on resume from system suspend, `emuleDlg.cpp:3692`) and on a LowID answer from a server (`ServerSocket.cpp:334`) - the latter being the trigger that targets the real failure, since a stale mapping is INVISIBLE until a server refuses to reach us. SPAWNED, not awaited, because discovery + SOAP costs seconds and resume is the path the user waits behind. Its test asserts eMule's actual contract by counting what the mock gateway RECEIVED: an intact mapping is verified and then LEFT ALONE (one SOAP call, no delete, no re-add). Lease kept at 0 as the named `PERMANENT_LEASE` with the four citations, so the deviation that was considered and rejected is legible. Also landed: a ConflictInMappingEntry now NAMES the holder by querying the gateway (mirroring eMule's honest "already redirected to another IP" bail, `UPnPImplWinServ.cpp:448`, improved) - the one fact that was missing when this cost hours to diagnose by hand. Gate: 521 tests, clippy/fmt/ASCII clean. **PARTIALLY DEVICE-VERIFIED 2026-08-02** (build 0cf6791): the UPnP half ran clean on device - a fresh container mapped 4662 and earned HighID, and a background/foreground cycle kept both, exercising the new spawned refresh on the resume path without regression (it is SILENT when the mapping is intact, by design). The Kad checkpoint half is NOT observable through the UI and was not device-verified: `kad_contacts()` reports `self.routing`, which `start_kad` re-folds on every resume, so a resume MASKS the difference - the fix only changes what survives a suspend-KILL, and `nodes.dat` lives in Application Support where the scriptable AFC path cannot read it. It rests on the mutation-checked unit test. |
| 8au | Explicit STOP action, `set_kad` invariant, periodic checkpoint | [[ipad-usb-tooling]] [[net-highid-and-port-forwarding]] | **DONE (2026-08-02).** Anthony asked how to exit padMule cleanly. There is no exit (iOS has no app-quit an app may call) and backgrounding is the clean path since `pause()` flushes - but the question surfaced that BOTH authorities delete their UPnP mapping on exit (eMule `CloseUPnPOnExit`, DEFAULT TRUE, Preferences.cpp:2501; aMule unconditionally, amule.cpp:1747-1751) and padMule never did, at any lifecycle point. Since only the OWNING address may delete a mapping, releasing it while we still are that address is the only moment cleanup is possible - which is exactly the orphan that stranded port 4662. **STOP:** `Engine::shutdown` now disconnects, releases every socket (it previously checkpointed and set the state while leaving TCP 4662 bound and the Kad UDP socket open), flushes, awaits `upnp::unmap_port`, announces "Stopped", and stays RESTARTABLE in place; the Status screen gets a destructive Stop control with a confirmation, becoming Start once stopped. **THE BUG IT EXPOSED:** the 8at Kad checkpoint fix was INEFFECTIVE on the pause path - `pause()` sets `self.kad = None` BEFORE `checkpoint()`, so the union helper was handed `None` on the one path that matters most on iPadOS. The test had passed because it called the HELPER directly with a live table; it tested the helper, not the path (the 8ae lesson, walked into headfirst). Fixed, then HARDENED: every assignment to `self.kad` now routes through `set_kad`, which absorbs the outgoing node's table, so the rule is structural rather than remembered. Both tests mutation-checked - deleting the absorb turns them RED. **PERIODIC CHECKPOINT:** padMule checkpointed only at pause/shutdown, so a kill that never delivers `.background` cost the session. Added at 300s off the 1s heartbeat, flushing download progress first. Checked the authorities before building and the premise was FALSE - neither eMule nor aMule saves nodes.dat periodically, both write it only from `CRoutingZone`'s DESTRUCTOR (RoutingZone.cpp:137-142 / :118-123) - so it is documented as a DELIBERATE DEVIATION justified by the platform (iPadOS kills at suspension as routine; a desktop app gets its destructors) and not dressed up as parity. Gate: 525 tests, clippy warning-free, fmt/ASCII clean. **DEVICE-VERIFIED 2026-08-02** (build 11eec87, clean Sideloadly reinstall with the correct single-suffix bundle id). The port release was proven by asking the GATEWAY, not by trusting the app's own message: running -> `:4662 -> 192.168.0.182`; after Stop -> **`:4662 is NOT mapped (free to claim)`**; after Start -> `:4662 -> 192.168.0.182` again. The UI tracked it honestly throughout (`UPnP: released port 4662`, state `running -> stopped -> running`, the button flipping to Start), and the engine restarted IN PLACE without relaunching the app. |
| 8av | PORTABILITY AUDIT + Tier-1 fix slice + device pass | [[portability-audit]] | **DONE (2026-08-03).** Anthony asked "is this usable for people who do NOT have my setup?" - the answer was NO, with one shared root cause: every degraded path (UDP-blocked, metered, no-UPnP, slow link) is structurally invisible from the dev network. Two parallel audits produced the Tier 1/2/3 findings ([[portability-audit]]); the TIER 1 slice landed the same day: a UDP-blocked network no longer greys out every server (the probe is UDP but login is TCP - rows stay selectable), the splash waits for engine READINESS (min ~2.5s, 20s ceiling) instead of a fixed 7s, and a disconnected add-download tells the user THEY are not connected instead of claiming nobody has the file. Device pass (build 44ba972) verified the slice on glass. Also landed alongside: engine os_log (subsystem us.ajbconsulting.padMule, category padMule.engine; device-verified via idevicesyslog) and agent-driven TOUCH CONTROL via WebDriverAgent ([[ipad-usb-tooling]]). Full narrative + per-commit gate figures: [[log]] 2026-08-03. |
| 8aw | Settings screen (Tier 0) - padMule's first | [[portability-audit]] | **DONE (2026-08-03, commit d910c9e).** eMule-preferences-grounded first Settings screen: persisted Leech Mode (fixing a live bug - it was initialised ON and never persisted, so turning it off silently re-enabled every launch), "pause sharing on cellular/metered" DEFAULT ON (NWPathMonitor isExpensive/isConstrained - the one finding that can cost the user money), multi-URL server lists (eMule addresses.dat model, fetched + merged), default download priority, remembered search filters, keep-screen-awake. Narrative: [[log]] 2026-08-03. |
| 8ax | gzip/zip-wrapped server lists unwrapped | [[feature-server-hunter]] | **DONE (2026-08-03, commit 97ac53c).** bootstrap::maybe_decompress runs inside http_get_bytes, so every fetched server.met and nodes.dat is transparently unwrapped (gzip + single-entry stored/deflate ZIP) before validation; bounded at 32 MiB against a decompression bomb; unrecognised or malformed bytes fall through to the raw form for the validator to reject. |
| 8ay | Gossip crawl FIRST CUT (harvest-on-connect) + connected-line server name | [[feature-server-hunter]] | **DONE (2026-08-03, commits e7c38d0 + 26cc9f8).** The server-event forwarder stashes OP_SERVERLIST-advertised (ip,port)s and the 1s heartbeat merges them into server.met via merge_server_met (routable-public + user-ipfilter gated, pending queue bounded at 2000, runs on the engine task so no server.met write races). DEVICE-PROVEN INERT against real servers: a live Lugdunum connect delivered NO OP_SERVERLIST - modern servers must be ASKED, so the OP_GETSERVERLIST send is the required next step ([[feature-server-hunter]]). Also: the connected status line now cites the server NAME with the address in parens (HighID/LowID moved to its own Status row). Gate at session end: 532 tests, clippy/fmt/ASCII clean. |
| 8az | OP_GETSERVERLIST ask - the gossip harvest goes LIVE | [[feature-server-hunter]] | **DONE (2026-08-03).** The required "ask" step 8ay's device pass proved missing. A fresh login sends the bodiless OP_GETSERVERLIST (0x14) right after the shares offer - BOTH authorities' exact send site (eMule 0.50a sockets.cpp:253-260, aMule ServerConnect.cpp:289-296) - on connect AND resume (a resume is a fresh login; the ask lives in the authorities' ConnectionEstablished, which runs on every reconnect). Gated on eMule's AddServersFromServer pref (engine set_add_servers_from_server -> FFI -> a Settings toggle "Ask connected servers for more servers"); both authorities default OFF (eMule Preferences.cpp:2105, aMule Preferences.cpp:1175), padMule defaults ON as a DELIBERATE documented deviation (wire-identical; the merge is filtered + bounded; default-off would keep the Server Hunter harvest inert on every fresh install). RIDE-ALONG FIX: the event forwarder now stashes a ServerList BEFORE its 30/10s flood limiter - the answer to our own ask arrives inside the busy connect burst, exactly when the window is most likely spent, and the old order silently dropped it (found in this session's reanalysis; regression test was RED first). TDD throughout (byte-exact codec test; recording mock server proves ask-on-connect, pref-off silence, re-ask-on-resume). VALIDATED: the isolated Lugdunum eserver accepts the ask through a full lifecycle (advertises nothing - it knows no other servers), and a LIVE public server answered `[serverlist] 33 servers` on a HighID login. mule-cli `login` now asks + prints the answer, so the live check is one command. Gate: 537 tests, clippy/fmt/ASCII clean. Remaining part-3 work: the recursive UDP crawl ([[feature-server-hunter]]). |
| 8ba | RECURSIVE UDP server crawl - the Server Hunter discovery engine COMPLETE | [[feature-server-hunter]] | **DONE (2026-08-03).** The handoff's #1 open task. Where the 8az harvest learns only from the ONE server we log into, `Engine::crawl_servers(rounds)` asks servers we are NOT connected to over UDP and then asks the ones that answer. **EVIDENCE FIRST, code second:** the wire question - is there a UDP way to ASK for a server list - was settled by probing before a line of production code existed. `OP_SERVER_LIST_REQ2` (0xA4, bodiless, to TCP+4) and `OP_SERVER_LIST_RES` (0xA1) exist in both authorities' opcode tables, but NEITHER eMule 0.50a NOR aMule (3.0.1 or master) EVER SENDS OR PARSES THEM - on this socket they send only the status ping 0x96 and the description request 0xA2. So padMule sending it is a DELIBERATE, documented deviation, justified because many SERVERS answer - measured, not assumed. **The obvious guess was WRONG and is recorded as such:** the vendored Lugdunum eserver 17.15 oracle does NOT answer 0xA4 (nor does ed2k-rust, nor eMule Sunrise), yet a live crawl had 28 of 33 asked servers answer; every silent server answered 0x96 and 0xA2 in the same burst, so the silence is a real negative. SILENCE IS A NORMAL ANSWER - never an error, never a liveness verdict. The 0xA1 payload is byte-identical to TCP OP_SERVERLIST, so `parse_server_list` reads both and no second parser exists. STRUCTURE follows `mule_kad::lookup`: a PURE `server_crawl::ServerCrawl` (frontier, dedup, ceiling, recursion) unit-tested offline, plus a thin I/O driver - so the policy is provable without a network and without a test-only switch in production code. SAFETY (the one path that contacts hosts the user never chose): 3 rounds / 40 asks per round / 40ms pace / 4s collect / 1000 discovered max; the ipfilter gates who is SENT to, not merely what is kept; only routable-public addresses are asked or merged; answers accepted ONLY from an address just asked (anti-spoof); and the merge was REFACTORED into one shared `merge_discovered_servers` used by BOTH discovery channels so the rule cannot drift. Abuse profile is deliberately no worse than the existing Servers-screen status probe. TESTS: 6 pure-policy tests, all MUTATION-CHECKED (breaking recursion, the safety gate, or the ceiling each turns exactly the right one RED); 2 caller-level engine tests incl. the SSRF posture on the production entry point (mutation-checked: ungated it spends 4.16s dialing LAN/loopback, gated it bails in 0.02s); 1 codec test pinning the shared payload. LIVE-PROVEN: 10 seeds -> 2 rounds -> asked 33, 28 answered, 25 new (10 -> 35); a 3-round run converged on the same 25, so it terminates rather than running away. The result was audited byte-by-byte: ZERO private/loopback/multicast/reserved/port-0 entries, and a discovered address matched an independent raw-Python probe exactly. Shipped through FFI + a "Discover more servers" button + `mule-cli server-crawl`. Gate: 548 tests, clippy/fmt/ASCII clean. NOT device-verified yet. |
| 8bb | USAGE-FEEDBACK ROUND: seven real bugs from Anthony's first extended session | [[portability-audit]] | **DONE (2026-08-03).** Anthony used the app properly for the first time and filed 18 items - a mix of questions and requests. Four parallel investigations turned them into SEVEN CONFIRMED BUGS, three of them serious, and every one was reproduced in code before a line was changed. **(1) RESUME WAS BROKEN, and only when Kad was HEALTHY.** `find_sources` joins the server and Kad arms, so it returns in max(), and the Kad arm carries a 15s budget; `resume_fetches` wrapped the whole call in a 4s timeout that DISCARDED everything on expiry - including server sources that had arrived in under a second. So a populated Kad table guaranteed the discard, while an empty one let resume work. `add_download` calls the same function with NO outer timeout, which is exactly why adding a file worked and resuming the identical file did not. Fixed by bounding each arm by a budget PARAMETER so partial results always survive; the mutation test measures the old behaviour precisely (10.001s against a 600ms budget). Also: RESUME_BUDGET 8s meant only 2 downloads were ever attempted per foreground return, and there was NO retry anywhere - `spawn_fetch` was reachable only from start/resume/add_download, so a download that missed its window or exhausted its round budget sat frozen until relaunch ("stuck at 34% forever"). Added `maintain_resume_fetches` on the heartbeat (one idle download per 45s, 2s budget - deliberately small because the heartbeat holds the engine lock; getting these off the one serial queue stays Tier 2). **(2) PHANTOM SHARED FILES.** `load_shared_library` verifies each file is still on disk - but runs ONLY at `start()`, and `serve_shared` looked up by hash in memory with no check at all. Delete a finished file in the Files app and padMule kept announcing the hash via OP_OFFERFILES, answered a requesting peer "COMPLETE", granted it an upload slot, then died silently when the read failed - the worst shape, since the peer had every reason to trust us. Fixed at BOTH ends: the serve lookup now requires present-and-right-size (a miss draws the same FNF the authorities send), and `verify_shared_library` prunes the live library + known.met every 60s. The mutation proof is literal - without the check the answer is opcode 0x50 (FILESTATUS "complete") where 0x48 (FILEREQANSNOFIL) belongs. Six serve fixtures that used `/does/not/matter` now write REAL files, which makes them faithful to a live serve. **(3) STATUS ROW STALE AFTER A KICK** - `poll_server_drop` emitted only ServerDropped, never Status: the exact mirror of 8as, whose fix taught connect/disconnect/failed-dial to emit Status and missed the drop path. **(4) SEARCH ROWS NEVER UPDATED** - `SearchHit.status` is stamped once at search time and `results` is never refreshed, so a completed file never showed "Have" AND a file just tapped reverted to showing "Get" (startable twice). Fixed Swift-side by deriving live status from the polled downloads/shared sets. **(5) pause() STARVATION** (serial queue + unguarded 1s poll + no beginBackgroundTask), **(6) keep-awake gated on "a download exists" rather than bytes moving** - so a stalled download held the screen on forever while Settings claimed otherwise, and **(7) the Transfers footer promised transfers "resume when you come back"**, which (1) made false. Both false claims are now true statements AND the code matches them. ALSO SHIPPED from the same list: per-operation spinners (Discover more servers had ZERO feedback for ~12s - his exact complaint), results count, entry numbers + back-to-top, connected-server highlighting (it rendered DIMMER, being a disabled row), the add-category label, download percentages + Transfers sorting, Status/Server de-duplication with the server NAME finally threaded over the FFI, a two-network health panel, and search-result PROVENANCE - which needed engine work because origin was destroyed when three channels were flattened into one untagged vec before dedupe (now an OR-accumulated bitset, since a popular file legitimately arrives from both). Gate: 559 tests, clippy/fmt/ASCII clean; amuled differential PASSES byte-for-byte and the reverse oracle PASSES (real amuled downloads from padMule + serve-side secure-ident), which is what proves the new serve-path disk check does not break a legitimate upload. |
| 8bc | UPnP: retry a mapping that never succeeded (the last reanalysis UPnP finding) | [[net-highid-and-port-forwarding]] | **DONE (2026-08-03, commits 238267d + 3ff394c).** Both triggers that exist to RECOVER a missing port mapping - a foreground resume, and a server answering LowID - early-returned when `public_ip` was None, on the reasoning that there was nothing to refresh. But the initial `map_port` at `start()` can simply FAIL (a dropped SSDP answer, a gateway busy for a moment), and then the two paths designed to fix exactly that were the two that refused to run: the session stayed LowID for its whole life with no retry short of a full Stop/Start - on the path padMule's entire HighID story runs through. The decision is now the pure `port_mapping_action(running, offline, have_mapping)` (Refresh when we hold one / Map when we do not / None when stopped or offline), so the rule is testable without a gateway; a repeated failure stays SILENT, since a user with no UPnP gateway would otherwise get the same line on every foreground return. `public_ip` became an `Arc<Mutex<..>>` so the SPAWNED retry can record a mapping it creates - a plain field could only ever be written on the engine task. Mutation-checked: restoring the old "no mapping means nothing to do" rule turns the new test red on exactly that assertion. Gate: 560 tests, clippy warning-free, fmt/ASCII clean. PROCESS SLIP worth recording: 238267d was committed while clippy reported ONE warning (an unused `mut` the refactor orphaned) - caught by re-reading the count immediately after and fixed in 3ff394c. The gate is warning-free; a count glanced at is not a count read. |
| 8bd | VPN readiness: configurable ports (listen vs ADVERTISED) + a UPnP toggle | [[net-highid-and-port-forwarding]] | **DONE (2026-08-03).** Anthony is preparing to run padMule behind AirVPN and asked for whatever features that needs promoted. RESEARCH FIRST (recorded in [[net-highid-and-port-forwarding]], sourced from AirVPN's own FAQ + iOS guide rather than memory): ports are USER-CHOSEN not randomly assigned ("You can use ports >= 2048"), so the eD2k default 4662 is itself requestable; TCP and UDP are separately selectable per port; forwarding is configured SERVER-SIDE in Client Area -> Ports, independent of the client app, which is what makes it usable on an iPad at all (official WireGuard app + AirVPN Config Generator by QR or .conf); and crucially AirVPN supports REMOTE-TO-LOCAL mapping - an empty "Local" field forwards n->n, a filled one forwards remote n to a different local x. **That last fact is what dictated the design.** A VPN REPLACES the router-forwarding mechanism padMule's whole HighID story runs on, so two things were needed: (1) the port must be settable, because a provider-assigned port may not be 4662; and (2) the port we ADVERTISE must be separable from the port we BIND, because under remote-to-local forwarding peers must dial the EXTERNAL port while padMule listens on the local one. Landed `Engine::set_ports(listen, advertised, kad)` + `set_upnp_enabled(on)`, threaded through every previously-hardcoded TCP_PORT/KAD_UDP_PORT site (listener bind, Kad bind, the login request's tcp_port, both peer HELLO baselines, the Kad node's advertised TCP port, and all four UPnP paths), exposed over the FFI, and surfaced in a Settings "Network / VPN" section. UPnP now no-ops when disabled rather than emitting a misleading failure line the tunnel makes meaningless. **A MUTATION THAT SURVIVED, AND THE TEST IT FORCED:** making the login advertise the BIND port instead of the ADVERTISED one passed the ENTIRE suite - because everything still binds and connects locally on this box while every real peer would dial a port nothing is listening on. Closed with a wire-level assertion that reads the port back out of the login request's fixed offset (userhash 16 | client_id 4 | tcp_port 2) against a recording mock server; it now fails with `left: 4662, right: 51234`. Gate: 562 tests, clippy warning-free, fmt/ASCII clean. NOT device-verified, and cannot be until the VPN is actually configured. |
| 8be | VPN-drop guard: auto-pause sharing on a public-address change + warn loudly | [[net-highid-and-port-forwarding]] [[security-model]] | **DONE (2026-08-03).** Anthony read AirVPN's iOS footnote ("Apple processes and apps can bypass any VPN tunnel at will") and asked whether it should concern him. The GPL half is a non-issue and the bypass half does not touch padMule's own traffic - but chasing it surfaced the consequence that DOES matter: stock iOS has NO kill switch (Always-On VPN is MDM-only), so a dropped tunnel silently falls back to the normal interface and padMule would keep seeding from the REAL address, with the advertised port now wrong, and nothing on screen to say so - a privacy exposure and a LowID in the same moment. Asked which behaviour he wanted, he chose BOTH: auto-pause and warn. THE MECHANISM USES A FACT WE ALREADY RECEIVE: a HighID client id IS our public address. `note_public_id(id, low_id)` compares it across logins and on a CHANGE pauses sharing, latches `sharing_paused_for_ip_change`, and emits `EngineEvent::PublicAddressChanged`. PRIVACY BY CONSTRUCTION: the address is compared internally and never emitted - the event is deliberately PAYLOAD-FREE, for the same reason connect_to_server refuses to record the client id in user-visible text, and a test asserts no address-like string reaches any Server/Status text. A LowID login carries no public address so it neither trips the guard nor overwrites what we knew (otherwise every LowID server would false-pause). The latch clears when the user turns sharing back on, so the app keeps saying WHY until they decide. Wired at BOTH login sites - connect AND resume, since a resume is a fresh login and exactly when a tunnel is most likely to have gone. MUTATION-CHECKED AT THE CALLER, not just the helper: removing the call from connect_to_server, or removing the LowID exemption, each turns the right test red - and the caller-level test drives a mock server that hands out a DIFFERENT HighID on the second connect, which is the literal shape of the failure. HONEST LIMIT recorded in the wiki: this fires on ANY public-address change, so switching VPN exit servers or moving Wi-Fi-to-cellular trips it too; the action is a pause plus an explanation rather than a silent continue, which is the safe direction, and the wording says so. Gate: 564 tests, clippy warning-free, fmt/ASCII clean. |
| 8bf | On-device usability fixes during the VPN setup: port fields, strip order, a real Open | [[net-highid-and-port-forwarding]] | **DONE (2026-08-03, commits df684d8 + 7374bd8).** Three findings from Anthony driving the actual app. **(1) THE PORT FIELDS WERE UNUSABLE** - the very control the VPN work exists for. Typing a new port, or clearing the field first, made the old value reassert itself. Cause: the TextField's Binding did `get` = read UserDefaults and `set` = write UserDefaults ON EVERY KEYSTROKE, so an empty field was read as "restore the default", 4662 was written straight back, and the getter re-rendered it under the cursor - the user was typing against a value that kept restoring itself. Fixed to the pattern every other field in the app already used: edit a LOCAL @State buffer seeded on appear, validate exactly once at COMMIT, and write back what was actually stored so a rejected entry snaps to the value in force rather than lying. Commit is driven by focus LEAVING the field, by onDisappear, and by a new keyboard-toolbar Done button - REQUIRED because `.numberPad` has no Return key so `.onSubmit` never fires, a quirk an agent had flagged in passing two rounds earlier that I had only half-heard. AUDITED THE WHOLE APP at Anthony's instruction: every other TextField (server-list URL, search query, results filter, new-category name, rating comment) already binds a plain @State/@Published string, and the size filters are preset MENUS; the remaining custom get/set Bindings are all TOGGLES, which are safe with write-through because every intermediate value of a toggle is complete. The port fields were the ONLY instance. GENERAL RULE recorded: write-through-on-keystroke is fine for a control whose every intermediate value is valid, and WRONG for free text, where "partially typed" and "temporarily empty" are states the user must be allowed to occupy. **(2) Function strip reordered** to Servers, Status, Search, Downloaded, Transfers, Shared, Stats - the order the app is actually used in. Only the enum declaration order matters (CaseIterable drives the strip); all seven cases verified still covered, shortTitle exhaustive via `default`. The landing screen stays Search - not asked for, not changed. **(3) A REAL "Open" for finished downloads.** The Downloaded rows carried only a ShareLink, which reaches "open in" via the share sheet but is an extra hop behind a share icon; asked directly whether this was handled, the honest answer was "partly". Tapping a row now opens the file in QuickLook. THE CONSTRAINT: iOS refuses to hand a `file://` URL from the app container to another app via `UIApplication.open`, so the share sheet and QuickLook are the ONLY two routes - there is no "launch app X with this file". QuickLook is what a user means by "open" (video, audio, PDF, images, text, documents rendered natively) and carries its own share button, so ShareLink stays alongside as the distinct "send it elsewhere" intent. `QuickLookView.swift` wraps QLPreviewController (UIKit; no SwiftUI equivalent at the iOS 16 target) and XcodeGen picks it up with no project change. Gate: 564 Rust tests unaffected, clippy/fmt/ASCII clean; CI green on all three workflows for both commits. |
| 8bg | Toolbar Stop/Start + a How-to-use screen + the title bar | [[lifecycle-and-reactivation]] | **DONE (2026-08-03, commits 8437bc8 + a6e1a93).** Stop/Start added to the toolbar beside Settings, reachable from EVERY tab, with the Status control left exactly as it was - same icons (red stop.circle, blue play.circle) and confirmation, so the two are one feature in two places. Anthony's reasoning: Stop is what you do before closing padMule, and hunting for the right tab first is exactly when it gets skipped. **The move exposed a latent bug:** `confirmStop` drove a `.confirmationDialog` attached to `statusScreen`, and such a dialog only presents while its HOST view is on screen - the new toolbar button would have silently done nothing from any other tab. Re-attached at the NavigationStack root beside the kick and VPN alerts. GENERALISABLE: state mutated by a GLOBAL control must have its sheet/alert/dialog attached at the root, or it is dead everywhere but one screen. New `HelpView.swift` behind a questionmark.circle covers the app in one scrollable screen: what it is; THE ONE RULE (foreground-only, because iPadOS suspends backgrounded apps, plus the keep-awake setting); a five-step walkthrough server -> Status -> search -> Get -> Downloaded; Stop/Start and WHY (iPadOS gives an app no way to quit itself, so closing from the app switcher can strand a router forward - Stop releases it, and progress is saved either way); HighID vs LowID and what each costs; sharing and the two self-pauses (metered, public-address change); the two networks; a line per remaining screen; where files live. Every claim is behaviour the app actually has. Title bar now reads the requested `\__ p a d M u l e __/`, with the leading backslash escaped in the Swift literal and verified by decoding it. Gate: 564 Rust tests unaffected, ASCII clean; CI green on all three workflows. |
| 8bh | Title-bar escaping + the duplicate Activity row (both found ON GLASS) | [[lifecycle-and-reactivation]] | **DONE (2026-08-03, commit 89d8a2c).** Two defects Anthony photographed on the running app. **(1) The title bar ate its backslash.** `.navigationTitle("...")` with a string LITERAL takes a `LocalizedStringKey`, which performs ESCAPE PROCESSING, so `\__ p a d M u l e __/` rendered as `__ p a d M u l e __/`. The pre-commit check had decoded the SWIFT literal (`\\` -> `\`) and pronounced it correct - true about the string, false about what SwiftUI would then do with it, which is a nicely specific way to be wrong. Fixed with `Text(verbatim:)` and re-verified by pulling the string out of the COMPILED BINARY rather than the source. RULE: a user-facing string containing a backslash, `%` or other escape-significant character must go through `Text(verbatim:)` or a String variable, never a bare literal. **(2) The Status screen's "Activity" row was a verbatim duplicate of "Server".** It existed to surface the engine's transient text and was guarded by `status != connectionSummary` - but that compares the engine's FULL sentence ("Connected to eMule Sunrise (addr)") against the pure verdict ("Connected"), so once connected it ALWAYS differed and ALWAYS rendered. Removed; nothing lost, since Paused/Stopped/Reconnecting ARE the Status verdict and the boot-time messages happen behind the splash and the "Starting padMule..." banner. LESSON: a "show only when it differs" guard must compare against what the user will SEE beside it, not a different representation of the same fact. **DEVICE-CONFIRMED in the same photos:** the reordered strip (Servers, Status, Search, Downloads, Transfers, Shared, Stats), Status reading Connected / HighID / 146 Kad contacts against a real Lugdunum server, and the two-network health panel green on both - the first on-glass proof of the whole 8bb-8bg batch. |
| 8bi | On-glass corrections: stale UPnP row, 5999 defaults, banner placement + style, landing tab | [[net-highid-and-port-forwarding]] | **DONE (2026-08-03, commits 57a75f5 + d7d555a).** Four items from Anthony running the app behind AirVPN. **(1) THE STALE DURABLE ROW.** The Status screen still read "UPnP: could not map port 4662" after every port setting was changed to 5999 - not a live failure but a FOSSIL: `map_port` returned SILENTLY when the UPnP toggle was off, so the durable row kept the last attempt's text, naming the OLD port, which looked exactly like the new settings had not applied. It now emits "UPnP: off - port forwarding is handled outside padMule". GENERAL SHAPE, and the SECOND instance the same day: a durable UI field fed only on success/failure goes stale the moment its feeding path starts early-returning - the silent path must still SAY something. **(2) Default ports -> 5999** (Settings defaults, boot fallbacks, field placeholders) for convenience while on the VPN; the engine keeps eD2k's 4662/4672 as its own constants for anything not driven by that screen. **(3) The blue server banner was pinned above EVERY tab.** A MOTD is often a dozen lines and it PERSISTS, so it pushed real content down on screens it had nothing to do with. SPLIT rather than merely moved: engine `.server` NEWS (MOTD, discovery results) now goes to `serverNotice`, rendered only on the Servers tab and dismissable, while feedback for an action the user just took stays in `notice` and stays GLOBAL - moving all of it would have silently cost the "Downloading X" confirmation on the Search tab. **(4) Banner styling + landing tab.** Anthony liked the blue banner's look, so all banners adopt that solid treatment (tinted gradient, white text) instead of the washed 15%-opacity fill that was legible only in the blue case; the COLOUR stays semantic (blue informational, orange attention, red broken), since an all-blue error banner trades legibility for prettiness. The app now lands on **Servers** rather than Search - padMule does not auto-connect, so that is step one, and Search offered a box that could not answer yet. CI caught one Swift type error in the process (d7d555a): `Color.blue.gradient` is an `AnyGradient`/ShapeStyle, which `.background()` accepts but `.listRowBackground()` rejects - the third distinct instance today of the standing limitation that Swift type-checks only in CI here. Gate: 564 Rust tests, clippy/fmt/ASCII clean; all three workflows green. |
| 8bj | WAVE 11: AICH block recovery - the last security-scorecard PARTIAL | [[security-model]] [[padmule-amuled-oracle]] | **DONE (2026-08-03).** The 180KB block-repair protocol, end to end, closing wave 11 and taking the scorecard to **24/0/2**. Six slices, each committed green. **PROTO:** the Wave-1c one-shot fold became a materialized CAICHHashTree equivalent (eMule SHAHashSet.cpp): parity split rule, PARTSIZE/EMBLOCKSIZE base switching, wire ident encode/decode (1=left MSB-marker, master ident 1), CreatePartRecoveryData + ReadRecoveryData with the V2 dual-count layout, and an AichLeafHasher so the tree is built in the SAME streaming disk pass as the eD2k hash. Two latent 0.50a bugs deliberately NOT transcribed (the most-vexing-parse skip no-op :456-459, fixed in 0.70b; the lost bAllowOverwrite default .h:156) - both wire-neutral because VerifyHashTree re-derives from the root. **FILES/STORE:** known2_64.met codec + a file-backed store with aMule master's root->offset index (eMule 0.50a rescans the whole file per request - the DoS amplification the aMule comment cites); append rolls back by truncation, a torn tail is truncated at load, and pruning is STARTUP-ONLY against the known.met catalog, which is aMule master's fix for the 3.0.1 prune race (ThreadTasks.h:136-144) taken structurally rather than remembered. **SERVE:** padMule's long-advertised AICH bit is now HONEST - 0x9E answered standalone AND as the multipacket 0x9D sub-answer, 0x9B answered with recovery data under eMule's exact serve conditions, the 16-byte refusal on any miss, eMule's connection-dropping strictness on a malformed size, and both opcodes scored by the per-file request counter (which eMule 0.50a lacks entirely on this path). **DOWNLOAD:** root trust by eMule's /20 vote (>=10 IPs, >=92%) or VERIFIED from a link; a blamed part is claimed by one eligible source at a time, its recovery data verified against the trusted root, good blocks FILLED BACK and only the truly-bad ~180KB blocks left open - and each bad block's sole contributor banned, which CLOSES 8ai's honest limitation (an attacker sharing every poisoned part with a good source used to evade the part-level ban). **ORACLES (the terminal proofs):** padMule's known2_64.met ENTRY bytes are byte-IDENTICAL to what real amuled 3.0.1 writes for the same 10MB file (cmp in scripts/aich-golden.sh - upgrading the golden from root-only to the full store layout); `mule-cli aich-probe` drives 0x9E/0x9D + 0x9B/0x9C against the running amuled and verifies the recovery payload with the engine's own code (PASS); and a padMule-to-padMule wire loop repairs a poisoned block re-sending only a fraction of the 3.7MB part. **THE REVIEW EARNED ITS KEEP AGAIN:** three lenses (protocol / Rust concurrency / security) confirmed 9 findings - protocol found NO HIGH issues (the codec layer is faithful), but the other two found real defects, all fixed: a FALSE BAN of the source repairing the file (a recovery pass reading bytes mid-write blamed the rescuer; blame now comes from a snapshot frozen at blame time and skips any block the re-fetch touched - aMule added a mutex for exactly this, issue #586); a poisoned root could LIVELOCK a download forever (recovery that completes a part now re-checks its MD4 and drops a disproved root into eMule's terminal AICH_ERROR); the request-counter exemption was a MIS-TRANSCRIPTION (upstream exempts a peer WE download from, not one holding OUR slot - which any peer can elect, then hammer the most expensive answer we serve for free); the counter was also dodgeable by rotating the wire hash, with an unbounded map behind it; claims had no owner, so a stale claim's late answer could apply over the new claimant's; and the multipacket sub-op walker assumed ExtReq v2 when the shape is the SENDER's version. Both headline guards are MUTATION-CHECKED - and the first false-ban test was VACUOUS (its block was clean on disk, so the mutation passed) until rewritten, the [[interop-test-fidelity]] lesson landing yet again. Gate: 601 workspace tests, clippy warning-free, fmt/ASCII clean; amuled differential + reverse oracle + the live AICH probe all PASS after the hardening. NOT device-verified (no protocol change is visible in the UI; the repair is engine-internal). |
| 8bk | Independent post-wave-11 sweep: the AICH serve answer was unreachable on its MAIN path | [[security-model]] [[emule-peer-oracle]] | **DONE (2026-08-04).** A fresh reanalysis of the finished wave-11 branch, run as an independent reviewer rather than its author, found two real defects that the branch's own 601-test gate, warning-free clippy and three passing oracles had all missed. **(1) THE SERVE-SIDE ANSWER WAS UNREACHABLE ON THE PATH REAL CLIENTS USE.** `is_upload_request` did not list OP_AICHREQUEST (0x9B) or OP_AICHFILEHASHREQ (0x9E), so `classify_inbound` returned `InboundKind::Other` and `start_listener` DROPPED the connection unanswered. That is exactly how a real eMule asks: `SendAICHRequest` hands the packet to `SafeConnectAndSendPacket`, which - with no socket up - queues it and calls `TryToConnect` (BaseClient.cpp:2402-2414), so 0x9B is the FIRST packet after the hello on a BRAND-NEW connection. And it is the COMMON case, not an edge one: `CPartFile::RequestAICHRecovery` picks a RANDOM client out of the whole `srclist` (PartFile.cpp:6087-6106), filtering only on AICH support, matching root and a HighID preference - NEVER on whether the socket is up. So padMule advertised the AICH bit, built the entire hashset store to honour it, and then hung up on most of the clients that came to collect. **This is the 8ad/8ae bug class a THIRD time**, and for the same structural reason each time: every AICH serve test handed `serve_shared` its packet directly, so not one of them ever asked whether the connection survives long enough to REACH `serve_shared`. The new test drives the production `classify_inbound` gate. **(2) A SECOND CORRUPTION ROUND BLAMED THE FIRST ROUND'S SOURCE.** `localize_corruption` used `pending.entry(p).or_insert(..)`, which silently DROPS the freshly captured per-block contributor map whenever an entry already exists - and one always exists when a part failed before without recovery having run, which is the normal state until 10 unique /20s agree on a root. The stale map is what the recovery pass then blames, so an innocent earlier source could be banned for bytes it never sent. That breaks the sole-contributor no-false-positive rule the whole attribution design rests on - the same false-ban class the branch's own review had already caught once from a different direction. Fresh contributors now REPLACE the stale ones per block; blocks nobody re-sent keep the evidence we had. Also renamed `agrees` to `disagrees` where it holds `Ok(Some(false))` from `verify_part`: the behaviour was right and the name was its exact inversion, which is the shape a later reader 'corrects' into a real bug. Both fixes MUTATION-CHECKED (each new test goes RED with its fix reverted). Gate: 603 workspace tests, clippy `-D warnings` clean, fmt + ASCII clean; the amuled differential AND the reverse oracle (real amuled downloads FROM padMule, the direction the classifier change touches) both re-run and PASS. |
| 8bl | DEVICE-VERIFICATION session: the resume fix PROVEN on glass, + UPnP defaults OFF | [[on-device-test-checklist]] [[net-highid-and-port-forwarding]] | **DONE (2026-08-04).** Drove the running app over WebDriverAgent after a fresh Sideloadly install of f3bc022 (which also renewed the signing cert, retiring the 2026-08-10 deadline). **THE RESUME FIX IS VERIFIED** - the headline bug of 8bb and the top unproven item on the handoff. Two real downloads (125 MB + 75.5 MB) off a live server, backgrounded for 35s via WDA `deactivateApp`, then foregrounded: 2.3 MB -> 15.7 MB and 1.3 MB -> 10.2 MB in the next 90 seconds, roughly 4x the pre-background rate because resume re-ran source discovery. NON-VACUOUS: Kad was HEALTHY throughout (190 -> 204 contacts), which is the exact condition the old bug needed (the 4s timeout discarded server sources that had already arrived - it only broke when Kad was working). Also verified on glass, all previously unseen: the title bar keeps its backslash, the app lands on Servers, the toolbar Stop/Settings/help trio, the reordered function strip, the Status screen self-consistent (Status/Server/ID agree - the 8as fix), and the honest `UPnP: off - port forwarding is handled outside padMule` row (the 8bi fix). **UPnP NOW DEFAULTS OFF** (Anthony's call, commit 88b0983): it is the setting that agrees with the 5999 port defaults beside it, since on a VPN a LAN-router mapping maps a port on a gateway the traffic never traverses. Both Swift sites moved together with a test asserting the REAL registration. **THE VPN PATH STILL DOES NOT YIELD HighID** and the cause is now narrowed: padMule is LowID against TWO independent servers (MO-Server and eMule Sunrise, 47k users), so it is NOT a per-server callback quirk - inbound TCP to 5999 genuinely is not arriving. The tunnel is up (VPN badge present throughout). Remaining suspects are all AirVPN-side: the forward not active on the connected exit, or the account's single forward being consumed by another connected device. AirVPN's 'Test open' on 5999 with padMule running is the decisive next step. |
| 8bm | The serve side was sending every block up to THREE times | [[emule-peer-oracle]] [[interop-test-fidelity]] | **DONE (2026-08-04).** Found while establishing the wire authority for continuous block-request top-up (the open item 7), which is the useful part: the research answered a DIFFERENT question than the one asked. eMule keeps a 3-block window and re-states the WHOLE window every time ONE block completes - `SendBlockRequests` -> `CreateBlockRequests` tops the pending list back up to 3 and re-sends all three (DownloadClient.cpp:870-892, :1270-1276). That is only sane because the UPLOADER dedups: `AddReqBlock` drops any block already in `m_DoneBlocks_list` or `m_BlockRequests_queue` (UploadClient.cpp:665-680). padMule's serve loop had NO such check and streamed every restatement, so a real eMule downloading from padMule received each block up to three times - pure wasted upload on a foreground-only, possibly metered, possibly VPN-tunnelled link. **NEITHER ORACLE COULD SEE IT:** duplicate bytes still verify byte-for-byte at the far end, so the reverse oracle passes either way. It is a BANDWIDTH defect, not a correctness one, and a pass/fail harness is structurally blind to that whole class - a new entry in the [[interop-test-fidelity]] ledger alongside wrong-role and wrong-path. `serve_shared` now remembers the ranges streamed on this connection and skips an exact repeat. DELIBERATE DEVIATION: upstream's done-list is unbounded for the session and cleared on file change; padMule uses a bounded 64-deep FIFO (iPadOS memory, 8 slots x unbounded is real; the deduped window is only 3 blocks, so 64 is 20x) - and the bound also lets a GENUINE later re-request (a peer re-fetching a part it found corrupt) eventually succeed, where upstream refuses it for the whole session. The test drives the REAL serve loop, since the waste is a property of that loop not of a helper; MUTATION-CHECKED. Gate: 604 tests, clippy -D warnings, fmt + ASCII clean, and BOTH oracles re-run and PASS. |
| 8bn | Continuous block-request TOP-UP + a source-origin badge | [[protocol-reference]] [[padmule-amuled-oracle]] | **DONE (2026-08-04).** Open item 7 off the handoff, plus a UI ask. **TOP-UP:** the driver waited for ALL THREE blocks of a batch before asking for more, so every batch boundary cost a full RTT of dead air - worst exactly where padMule runs, on cellular or inside a VPN tunnel at ~200ms. Both authorities keep the window FULL: eMule re-requests the moment ONE block completes (`SendBlockRequests` from the block-finished branch, DownloadClient.cpp:1270-1276; `CreateBlockRequests` tops the pending list back to 3, :870-892). Depth STAYS 3 - eMule never asks deeper, and aMule master's [3,24] clamp cites a pending range eMule does not actually request (the CLAUDE.md authority note, confirmed by reading it). `BlockReceiver` now accounts per BLOCK instead of one batch-wide byte counter, so a landed block is released immediately (freeing it for another source) and replaced. **DELIBERATE DEVIATION, the safer half:** eMule re-states its WHOLE window each time, re-naming blocks still in flight - harmless only because the uploader dedups (row 8bm). padMule asks for ONLY THE NEW blocks: uploaders APPEND to a request queue rather than replacing it, so it pipelines identically, halves the chatter, and removes any chance of re-sent bytes being double-counted against a block still in flight. **The test IS the mechanism:** the mock sends ONE block then refuses the rest until a SECOND request arrives, so a stop-and-wait regression DEADLOCKS - converted to a clean failure by a 20s timeout (which also answers the standing worry about a multi-source test hanging a CI job forever). MUTATION-CHECKED. **SOURCE-ORIGIN BADGE:** `fetch::SourceOrigin` already knew Server/Kad/PeerExchange at dial time and simply was not carried into `SourceInfo`; now aggregated by `source_origins`, exposed on `DownloadInfo`, and rendered beside the percentage as e.g. `4 ed2k / 2 kad`. Empty channels are omitted rather than shown as zeros and the badge vanishes when nothing is connected, so a stalled row stays quiet instead of asserting `0 ed2k`; source exchange is named `sx` rather than folded into the server count. Gate: 605 tests, clippy -D warnings, fmt + ASCII clean; BOTH oracles re-run and PASS - notably the differential, where a REAL amuled 3.0.1 serves the incremental requests correctly including the 15MB multipart file. NOT yet device-verified (the badge needs an install). |
| 8bo | The fourth port field (Kad advertise vs bind), and events off the serial queue | [[net-highid-and-port-forwarding]] [[lifecycle-and-reactivation]] | **DONE (2026-08-04).** The two biggest remaining items, one closed and one HALVED - stated that way deliberately. **(1) KAD ADVERTISED PORT.** [[net-highid-and-port-forwarding]] predicted a fourth field would be needed "only if a remap is needed"; Anthony's AirVPN setup is that case, and it is what the handoff recorded as the KNOWN LIMIT of the 8bd split (which covered TCP only). Kad used ONE value for both bind and advertise, so a provider forwarding remote->local left padMule binding correctly - everything OUTBOUND kept working - while telling every peer to dial the local number nothing forwards. Inbound Kad died SILENTLY, the hardest shape to notice because the broken half is the one you never directly observe. Only ONE wire site advertises it (`build_hello_req` in `KadNode::hello`), so the fix stayed small: an optional advertised port defaulting to the bound one, `set_ports` gains a fourth argument, threaded to a fourth Settings field whose default EQUALS the bound port (and whose 0-fallback is the bound port, not 5999, so a missing value can never advertise a wrong number). **(2) SERIAL QUEUE - THE VISIBLE HALF ONLY.** `work` is a SERIAL DispatchQueue carrying all 28 FFI call sites, and `refresh()` bundled `drainEvents()` with TEN engine-locking calls in one block, so during a ~20s search the event drain could not even START - the precise reason `Reconnecting...` could never render. `drainEvents` locks ONLY the event channel in the seam (mule-ffi keeps `events` in a mutex separate from `inner`), never the engine, so it moved to its own queue and its own pump - the ONLY caller, since a channel drains once and two callers would race for who saw an event. **WHAT IS STILL OPEN, plainly:** `downloads()` and friends still block on the engine mutex, because `Engine::search` holds `&mut self` across its whole `tokio::join!` of the server/Kad/global arms. The NUMBERS still freeze during a search; the app now keeps painting and stays interactive, which is the difference between busy and hung. The deeper half needs `server` and `kad` to become independently shareable rather than `&mut self` fields - a real change to the most concurrency-sensitive code in the tree, and it earns its own pass with an adversarial review rather than being tacked onto a long session. Gate: 605 tests, clippy -D warnings, fmt + ASCII clean; CI green on both. NOT device-verified. |
| 8bp | On-glass UI round: Files button, real Open, floating Top, and sorting everywhere | [[on-device-test-checklist]] | **DONE (2026-08-04).** Five asks from Anthony driving the app. **FILES BUTTON:** a folder glyph left of Settings opening padMule's Documents folder in the Files app (`shareddocuments://`; not public SDK, which would block an App Store app and does not block a sideload-only one). **PREVIEW vs OPEN:** the share arrow on each finished file became an Open button that hands the file to ANOTHER app full screen. Precise about the platform: iOS has NO default-app-per-type, and `UIApplication.open` refuses a `file://` URL outright, so the route is the Open In chooser (`UIDocumentInteractionController`) - the chosen app takes over, which BACKGROUNDS padMule and pauses transfers. That consequence is now in the help screen rather than discovered mid-film. Row tap still previews in-app; sharing lives on QuickLook's toolbar, so nothing was lost. **FLOATING TOP BUTTON:** the old one lived INSIDE the results header, so it scrolled off exactly when it became useful - it only worked when you were already at the top. Now one floating capsule, visibility driven by the header's onAppear/onDisappear (steadier than scroll offset, and iOS-16-safe; onScrollGeometryChange is 18+). Anthony first asked for an arrow on EVERY row; the trade was presented against the real conventions and he chose the single control - 351 results would otherwise repeat identical chrome 351 times. **SORTING:** Downloaded by Name/Size/Date, Servers by tapping its column headers (tap again to reverse, arrow on the active column only, live servers always above dead ones since a dead row cannot be connected to). And search's Name sort was a REAL BUG rather than a missing control: `localizedCaseInsensitiveCompare` puts `part 10` before `part 2`; `localizedStandardCompare` is the Finder ordering that reads digit runs as numbers, which is what alphanumeric means. Tested. Gate: 606 Rust tests, CI green on all three workflows. |
| 8bq | The serial-queue engine half: UI polls off the engine lock | [[lifecycle-and-reactivation]] [[portability-audit]] | **DONE (2026-08-04).** The LAST big structural item on the handoff, and the fix was far smaller than the risk suggested because of one observation. `Engine::search` holds `&mut Engine` across its whole `tokio::join!` (server + Kad + global, ~20s) and EVERY FFI poll went through the same `Mutex<Engine>`, so the transfer numbers froze for the entire search; 8bo had moved the event drain off the serial Swift queue, which kept the app PAINTING, but this is what removes the blocking. **THE OBSERVATION:** five of the ten polls never needed engine STATE at all - `downloads`, `shared`, `sharing` and `public_ip` are already `Arc`s and the byte totals are process-global atomics; they took the lock purely because they were methods on `Engine`. `EngineHandles` hands out clones, which is not a new pattern (the engine already clones these same `Arc`s into spawned tasks in seven places), and none of the fields is ever REASSIGNED - the property that makes a handle taken once at construction stay valid, checked before relying on it and stated at the type. **THE HEARTBEAT:** `downloads()` was also the engine's heartbeat, seven background duties riding the UI's 1s poll - and that bundling is exactly what forced every progress read to queue behind a search. Split into `heartbeat()`, still engine-locked, called FIRST in the slow poll before anything that could early-return; the cost of forgetting (all seven fail SILENTLY) is spelled out at the definition, because the split turned an unmissable side effect into a caller obligation. Swift splits to match: fast poll on the lock-free queue, slow poll + heartbeat on `work`, and `refreshAll()` for action handlers that meant 'update the screen now'. **STILL TRUE, deliberately:** the Status scalars lag during a long search - they genuinely live in the engine. What no longer happens is the transfer list freezing. The test HOLDS the engine mutex and proves the five polls answer; a regression DEADLOCKS rather than failing slowly, so the assertion is a liveness check - mutation-checked by putting one poll back on the lock. Gate: 608 tests, clippy -D warnings, fmt + ASCII clean, BOTH oracles re-run and PASS. NOT device-verified. |
| 8br | The idle-retry sweep STARVED every download but one; amber activity row | [[portability-audit]] | **DONE (2026-08-04).** From Anthony testing overnight: "many files partially download and then either stop or slow to a crawl". **THE BUG IS WORSE THAN SLOW.** `maintain_resume_fetches` retries ONE idle download per 45s, selected with `sort_by_key(Reverse(priority))` then `.first()` - and Rust's sort is STABLE, so with every download at the same (Normal) priority it returned the SAME download on every sweep, forever. The rest were never retried AT ALL. With dozens queued that is not a slow retry cycle, it is NO retry cycle, which is exactly the reported symptom. Selection is now highest-priority-first and, WITHIN a tier, least-recently-retried; priority still wins but cannot starve its own tier. The stamp is applied on SELECTION rather than on success, because a retry that finds no sources must still yield its turn - otherwise it stays the permanent winner and starvation returns by another route. MUTATION-CHECKED, and the test reports the bug verbatim: old selection gives `[a,a,a,a,a,a]` over six sweeps, new gives `[a,b,c,d,e,f]`. **ACTIVITY INDICATOR** (also asked for): a transfer row is tinted AMBER while it is actually receiving bytes. `commit` stamps `last_byte_at`; `is_receiving` answers over a 3s TIME WINDOW rather than an instantaneous rate, because at a block boundary the rate legitimately reads zero and a light that blinks off every few seconds is worse than none (the same reasoning keep-awake already uses). Amber not green - this means WORKING, and Done is already green. Help gained a "Reading the Transfers list" section, since the honest meaning of an untinted row (registered but moving nothing, normal on eD2k) is not guessable from colour. Gate: 609 tests, clippy -D warnings, fmt + ASCII clean. NOT device-verified. **STILL OPEN from the same report:** the sources/availability disparity BETWEEN tabs is not yet investigated, and the dozens-of-downloads stress run is deliberately deferred until this fix is on the device, since the starvation would dominate any measurement taken before it. |
| 8bs | Retry budget becomes a CEILING (30x cheaper); the 99-vs-12 badge; and the QUEUE-BAIL root cause | [[portability-audit]] [[padmule-live-downloads]] | **DONE (2026-08-04).** Continued from 8br, all measured against a new live stress harness (`crates/mule-ffi/examples/stress.rs`, 25 downloads through the SAME FFI the app uses) rather than reasoned about. **(1) RETRY COST, 30x.** `find_sources` joined the server and Kad arms and waited for BOTH, and a Kad lookup essentially always burns its whole budget - so every retry cost the maximum even when the server had answered in a fifth of a second. Traced live: `found=1 ... took=6.001s`, with 4 of 4 retries pinned at exactly their budget. Retries now ask the server FIRST and return the moment it yields sources, paying for Kad only when it has nothing; measured after, 16 of 16 retries were sub-second (~195ms) and 0 hit the budget. That also DISSOLVED the duty-cycle cap added an hour earlier: at 195ms the retry holds the engine lock ~0.8% of the time, not 25%. `add_download` keeps the full parallel path (the user is watching a spinner for that one file). **(2) THE 99-VS-12 BADGE.** Search said 12 sources, the Transfers badge said 99, same file: `note_source` upserts by address and NOTHING ever removed a record, so the badge was every address ever contacted and only grew. Now counts sources seen in the last 180s, with `commit` refreshing the delivering source so a long transfer does not age out mid-flight. MUTATION-CHECKED. **(3) THE ROOT CAUSE OF THE ORIGINAL REPORT, found and NOT yet fixed.** With retries cheap and fair, 15 of 17 retries found 2-7 usable sources plus up to 11 LowID peers - yet only 5 of 20 downloads ever received a byte. Sources are plentiful; padMule is not getting DATA from them. Cause: `OP_QUEUERANKING if bail_on_queue => return Err(TransferError::Queued)` - padMule bails the INSTANT a peer queues it. That policy came from the three-file HUNT (where sitting in a queue is dead time and you want any file fast), but eD2k is a QUEUE-BASED network: peers ration slots and you rise over minutes. A client that never waits can only be served by a source with a free slot at that instant; everything else cycles find -> queued -> bail -> idle -> retry. [REFUTED 2026-08-04, same day: A/B measured on the stress harness with the queue policy as the ONLY variable - BAILING gave 5/20 downloads receiving and 53.7MB in 243s, WAITING gave 1/21 and 0.0MB. The manager runs ~4 concurrent peers per download, so waiting parks them all in hopeless queues instead of cycling to the peer that has a FREE slot. Fast-bail is doing real work. The reasoning below was tidy and wrong, and is kept because the refutation is the useful part. Refined candidate, UNTESTED: bail while untried sources remain, wait only once every known source has queued us.] The original (refuted) claim was: the fix is a policy change on the hot transfer path, entangled with padMule being foreground-only (it cannot hold a queue slot for an hour as desktop eMule does), so it is deliberately left for its own pass. **PROCESS:** a video run showing ZERO bytes was NOT a regression - a control re-run of the scenario that previously moved 372MB moved data normally; four of seven Blender-open-movie keywords returned zero hits because that content lives on blender.org, not eD2k. Gate: 610 tests, clippy -D warnings, fmt + ASCII clean. |
| 8bt | THE FETCH FUNNEL: where peer sessions actually die, and the two bugs it found | [[portability-audit]] [[padmule-live-downloads]] | **DONE (2026-08-04).** The handoff's demand was "a MEASUREMENT, not another theory" - the gap between "found 5 sources" and "0 bytes" had never been observed directly. So it was instrumented, not reasoned about. **THE INSTRUMENT:** cumulative per-stage counters in `stats.rs`, bumped one line at a time through `run_peer`/`fetch_one` (dial -> connect -> filestatus -> hashset -> slot -> blocks -> bytes), plus a 256-entry lock-free tally of every opcode read OUT OF TURN. Cumulative stages need no per-attempt state, and the DROP between two adjacent stages is the loss at that stage - including a loss to the outer per-peer timeout, which no error value can report. Global for the same reason the byte counters are. Surfaced as `mule_ffi::fetch_report()`, deliberately NOT `#[uniffi::export]` (a diagnostic, not a shipped Swift method), printed by the stress harness every 30s. **THE MEASUREMENT** (480s, 25 downloads, HighID, a 41k-user server): `dialed 1583 -> connected 74 -> got filestatus 65 -> asked for a slot 65 -> slot ACCEPTED 7 / queued 50 -> accepted-but-no-block-to-take 6 -> requested blocks 1 -> DELIVERED bytes 1`. ONE peer session in the run moved a byte, and 23 of 25 downloads reached `0/0/0` connected sources against a Search claim of 8-22. Kad contributed ZERO sources to any download. **BUG 1 - OP_OUTOFPARTREQS (0x57) had NO HANDLER.** This is not an edge case, it is how every upload slot ends: both authorities call `SendOutOfPartReqsAndAddToWaitingQueue()` the instant `CheckForTimeOver()` trips (eMule 0.50a UploadClient.cpp:722-725 + :767-782, aMule master UploadClient.cpp:463-466), and that trips at 10 MB uploaded or one hour (UploadQueue.cpp:609-616 - the very 10 MB padMule already encodes as `upload_queue::SESSION_MAX_BYTES`). `BlockReceiver::accept` yields no writes for a non-data opcode, so the block loop went on waiting for bytes that were never coming until the CALLER's 45s per-peer timeout, holding one of only FOUR workers for that download the whole time. Every source that fed us 10 MB parked a worker - the "partially download, then stop or slow to a crawl" report verbatim, and the one download in this run froze at exactly 18.6MB. Fixed per aMule's own receive side (ClientTCPSocket.cpp:727-736, DOWNLOADING -> ON_QUEUE): with `bail_on_queue` it returns `Ok(delivered)` - the BYTES, not an error, so the manager scores a proven deliverer instead of a failure - and without it (a called-back LowID source that cannot be redialed) it releases its blocks and waits out the next turn via the new `await_slot`. **WHY NO GATE CAUGHT IT:** the differential test's 15MB file does cross 10MB, but on loopback it transfers before amuled's kick timer ever ticks. A green oracle proves only the path it drives. **BUG 2 - padMule asked for an upload slot from peers holding nothing it needed.** eMule reads the same OP_FILESTATUS and, when no part is needed, sets DS_NONEEDEDPARTS and swaps away WITHOUT requesting a slot (DownloadClient.cpp:634-641; the slot request at :545-549 is the else-branch). padMule asked unconditionally, and the funnel priced it: 6 of the 7 slots it actually WON went to peers with nothing - the scarcest thing on eD2k, spent on nothing, after sitting through the queue wait to get it. Fast-bail makes this worse than it sounds because it SELECTS for such peers: a client that just started downloading the file has a free upload slot precisely because it has nothing to upload, so it is exactly the peer that answers instantly instead of queueing. Now gated on `Download::has_needed_part` (which ignores reservations - a part another worker holds is still a reason to want this peer). **DISCIPLINE:** both bugs were written test-first with RED observed, then MUTATION-CHECKED - remove the fix and the right test goes red at 5.02s / 5.19s `Elapsed`, restore it and it passes in 0.03s / 0.23s. **THE LIVE A/B IS INCONCLUSIVE, and saying so is the point.** A second 480s run with both fixes gave `1676 dialed -> 33 connected -> 24 slot asks -> 17 ACCEPTED -> 12 no-block-to-take -> 5 DELIVERED`, 2 of 25 downloads receiving and 22.6MB (baseline: 1 of 25, 18.6MB). Every headline number moved the right way, and NONE of it is attributable: `connected` fell from 74 to 33 on a similar dial count, so the two runs sampled very different source populations, and the slot-accept rate going 11% -> 71% is far too large to be caused by a gate that removed six asks. Population variance dominates a single pair of runs on this network. **What IS established is offline:** both bugs are proven by deterministic loopback tests with mutation checks, independent of the network. Note also `slot REVOKED (0x57) = 0` for the whole second run - no source fed padMule 10MB in one session, so the revocation fix was never exercised live here. It is proven by test, not by this run, and the docs must not imply otherwise. **A THIRD OBSERVATION, mechanism partly proven and partly REFUTED:** `accepted, no block to take` did not fall - it rose to 12 of 17. The arithmetic suggests why (4 workers x 3 blocks x 184320 = 2.11MB can be under reservation at once, so a smaller file is entirely spoken for by its own workers and every later session finds nothing), but a test written to prove that FAILED first: below ENDGAME_LIMIT (4 blocks, 737KB) `take_blocks` enters endgame and races the reservations instead of returning empty. The real exposed band is 737KB < still-missing < 2.11MB, plus any peer holding only parts already spoken for - REAL, kept as a regression-documenting test, but too narrow to account for 12 of 17. That remains OPEN. **STILL NOT EXPLAINED, and deliberately not written up as a cause:** the largest single loss is the FIRST stage - 95% of dials never complete a handshake - and neither fix touches it. Part of it is re-dialing, since nothing ever removes a proven-dead source from `download_file`'s pool, so one dead peer costs 8 dials per sweep and again on every retry. A dial-duration histogram (bucketed, split by whether the handshake succeeded) was added to settle the follow-up question with data: both authorities do use CONNECTION_TIMEOUT = 40s (eMule opcodes.h:62, aMule Constants.h:33-35), but eMule multiplexes hundreds of sockets so a stalled one costs it nothing, whereas padMule's four-worker pool turns the same number into a throughput cap. If successful handshakes all land in the first seconds, everything past that is dead air and a shorter CONNECT deadline is free; if they are spread out, the honest answer is to widen the worker pool instead. NOT yet measured. Gate: 614 tests, clippy -D warnings, fmt + ASCII clean. |
| 9 | (v1.1) seedbox mode | - | **DROPPED (Anthony, 2026-08-04).** Cut from the roadmap, not deferred again. The consequence is worth stating plainly: **foreground-only is now padMule's PERMANENT posture, not a v1 limitation awaiting a fix.** Everything built around that stays the final answer rather than a stopgap - the honest pause/resume, the splash that waits for readiness, the keep-screen-awake setting, the "padMule only transfers while it is open" line in Transfers and help, and Stop releasing the forwarded port. Nothing needs removing; what changes is that no doc should promise an always-on mode is coming. See [[lifecycle-and-reactivation]] and [[decisions-and-lessons]], both annotated in place. |
| 11 | AICH block recovery (OP_AICHREQUEST/ANSWER) | [[security-model]] | **[ANNOTATION 2026-08-03: SHIPPED as row 8bj - the text below is the pre-build plan, kept per ledger convention. Every "MISSING" item landed, and the VALIDATION BAR was met: scripts/aich-golden.sh now byte-compares the whole known2_64.met entry against real amuled's and drives a live 0x9E/0x9B probe.]** **NOT STARTED (queued 2026-08-02).** The LAST PARTIAL on the security scorecard (23/1/2) and the fix for 8ai's honest limitation. Two payoffs: repair a corrupt 9.28MB part at 180KB BLOCK granularity instead of re-downloading it whole, and attribute corruption PER BLOCK - today `localize_corruption` can only blame a SOLE contributor, so an attacker who always shares a poisoned part with a good source evades the ban. ALREADY DONE: the SHA-1 tree itself (`mule_proto::aich::aich_master_hash`), byte-validated against real amuled's known2_64.met (row 8u, scripts/aich-golden.sh); and the AICH capability bit is advertised, which 8ac verified is safe to keep even unimplemented - an unanswered AICH request is NON-breaking (eMule calls ClientAICHRequestFailed and re-downloads the part, aMule DownloadClient.cpp:1633/1662, never disconnects or bans). MISSING: the wire exchange (OP_AICHREQUEST 0x9B `<HASH 16><uint16 part><HASH aichhashlen>`, OP_AICHANSWER 0x9C `+<data>`, OP_AICHFILEHASHANS 0x9D - aMule Client2Client/TCP.h:90-92, but ground the body layout in eMule 0.50a first), persisting/serving a full per-file hashset (known2_64.met), block-level re-fetch on a part-MD4 failure, and then the finer 8ai attribution. VALIDATION BAR: amuled can serve a real hashset - extend scripts/aich-golden.sh, which already drives it to produce known2_64.met - then re-run the differential + reverse oracles (transfer path). NOT release-blocking: integrity already holds via whole-file + per-part MD4 plus the poisoning ban, so this is an OPTIMIZATION. |

| 10 | Kad HARD-VERIFY (verified-bit ENFORCEMENT + send-side receiver keys) | [[padmule-kad-notes]] [[security-model]] | **[ANNOTATION 2026-08-03: wave 10 is COMPLETE - Batch B enforcement LANDED as row 8ao (commit 5ef4c2e); the "in progress" status and "REMAINING: Batch B" text below are the mid-build narrative, kept per ledger convention.]** **COMMITTED BUILD, in progress (2026-08-02).** The last real protocol piece for the bulletproof gate. Two coupled halves: (a) SEND-SIDE receiver keys - store each contact's `sender_vk` from its responses (nodes.dat already persists `udp_key`/`udp_key_ip`) and ECHO it as our `receiver_vk` on subsequent requests, so a peer can verify US (today we always send `receiver_vk=0`); (b) verified-bit ENFORCEMENT - actively challenge an unverified contact via the Kad2 HELLO_REQ/HELLO_RES_ACK exchange and exclude/deprioritize contacts that cannot prove their IP (today the verified bit is TRACKED but enforced NOWHERE). Grounded in the eMule 0.50a Kad wire authority + the 8ab receive-side work. VALIDATION CHALLENGE (the reason it is a dedicated build, not a wiring pass): there is no reverse-Kad oracle - a real eMule confirming it verified padMule - so the send-side "a real eMule verifies us" cannot be faithfully proven the way the receive side was live-proven; the plan must include either a faithful mock or a live-Kad revalidation bar, per [[interop-test-fidelity]]. NOTE: `kad_obfuscate_request` currently ALWAYS NodeID-keys (udp_obf.rs:132), whereas real eMule receiver-key-keys a request once it holds the peer's key - a keying-fidelity nuance to resolve against the eMule source before shipping. **PROGRESS (2026-08-02):** the eMule-grounded understand-workflow RESOLVED the keying nuance - padMule's always-NodeID-keying IS byte-faithful (EncryptedDatagramSocket.cpp:278-318: NodeID wins when present; the receiver key is an in-packet FIELD), so the send-side is a safe FIELD FLIP, no codec change. **Batch A send-side mechanism LANDED (commit 3bf0162):** per-contact key storage (routing::Contact udp_key/udp_key_ip + note_verify_key + verify_key_for IP-gate + nodes.dat persistence), KadNode current_public_ip, request() echoes the stored key + captures the peer's, receive-side verify already wired. Offline-tested + live kad-bootstrap non-breakage confirmed. REMAINING: feed the engine public IP so the echo fires; the log-patched-amuled Kad oracle for the terminal "a real eMule verified us" claim (minimal ~2-line logging-only patch on a build copy - amuled already logs the multipacket answer receipt); then Batch B enforcement (flag-gated, offline-provable). The send-side terminal claim is NOT yet proven (mechanism + non-breakage only). **PROGRESS (2026-08-02, cont'd):** (1) engine public-IP wiring LANDED (commit 9c12e88): Engine.public_ip (UPnP-learned, never emitted) -> KadNode::set_public_ip in start_kad, so the echo fires live. (2) A decisive discovery while scoping the oracle: the Batch A key echo is a NO-OP for the verified-bit goal as built - a real aMule marks a v8 peer IP-verified ONLY via a HELLO_RES_ACK echoing the responder's sender key (Process2HelloResponseAck -> VerifyContact) or a HELLO_REQ with a valid receiver key (AddContact2); ProcessKademlia2Request is not even passed validReceiverKey, so echoing on find_node cannot verify us, and the live engine sent only BOOTSTRAP_REQ + KADEMLIA2_REQ - never a HELLO_REQ (CLI-only) and never a HELLO_RES_ACK (no engine caller). Also both eD2k terminal claims are ALREADY logged by stock aMule (multipacket ClientTCPSocket.cpp:1147, secure-ident BaseClient.cpp:2207) - so 8ae/8af need ZERO patch. (3) FIX BUILT + committed (65a186b): hello() now completes the v8 three-way handshake - drops the wrong outbound 0x04 (the responder's ack-request bit), stores the peer's sender key, and sends a HELLO_RES_ACK echoing it when the RES requests one (works even when we do not know our own public IP - we echo what the peer gave us); start_kad HELLOs the bootstrap responder so it runs in production. TDD: a faithful loopback responder confirms the ACK bytes + no-0x04; a guard test confirms no ACK when not requested. 490 tests green. REMAINING: the log-patched amuled Kad oracle (1 line at RoutingZone.cpp:490 or VerifyContact:881) to prove verification end-to-end vs real amuled; then Batch B enforcement. **TERMINAL PROOF LANDED (2026-08-02, commit 7e8fe9c):** built the log-patched amuled Kad oracle - a REAL amuled 3.0.1 (instrumented with a committed, auditable, logging-only patch; pristine amule-3.0.1/ untouched) run in an isolated namespace logs `PADMULE-ORACLE-VERIFIED contact (sender: 77.77.0.9) via VerifyContact` when padMule's v8 HELLO_RES_ACK handshake completes - i.e. Process2HelloResponseAck -> VerifyContact, the exact path the ACK code exercises. Observed reproducibly across consecutive runs. Artifacts: scripts/amule-oracle-kad-verify.patch (AddDebugLogLineC, active in Release), scripts/build-amuled-kad-oracle.sh (copy pristine -> patch -> build), scripts/kad-verify-oracle.sh (isolated 2-node runner: seeds amuled's Kad ID via preferencesKad.dat, one-contact nodes.dat, mule-cli kad-bootstrap+hello with a new optional [bind-ip]). ALSO confirmed: both eD2k terminal claims are already logged by stock aMule (multipacket ClientTCPSocket.cpp:1147, secure-ident BaseClient.cpp:2207) - no patch needed. KNOWN harness flakiness (NOT a padMule bug): amuled's client/Kad UDP socket occasionally binds an ephemeral port instead of UDPPort, defeating the fixed nodes.dat; a readiness gate + WARN mitigate, stabilization ongoing. [CORRECTED 2026-08-02, row 8ak: that diagnosis was WRONG - the port was always right. The harness read only the stdout redirect (this build logs to CFG/logfile) and grepped a socket string amuled never emits, so it could not see "Kad started", the port line, OR its own PASS. Fixed: it is now 3/3 PASS, first attempt, no WARN.] The send-side terminal claim is now PROVEN. Remaining: Batch B enforcement. |
| 8aj | Reanalysis lint round: wiki drift + stale doc comments | - | **DONE (2026-08-02, commits cf33b17 + 87ed596).** A full reacquaintance audit (6 parallel deep-dive agents over every crate, the iOS app, CI, scripts and the wiki, plus a live gate run) found the CODE sound - 490 tests green, all 8 protocol landmines byte-clean vs the vendored sources, the Kad oracle patch verified logging-only, CI secret-free, privacy rule clean - and the DRIFT in the docs. Wiki: [[security-model]] contradicted itself (credit row "still MISSING" inside the secure-ident row vs OPERATIONAL at the credit row) and still claimed the Kad send side emits receiver_vk=0 + "no reverse-Kad oracle exists"; [[emule-070b-features]] listed #9/#10 as remaining and #11 (CorruptionBlackBox, shipped as 8ai) as open backlog, with #10 unmarked and a duplicate #32; [[index]] was ~12 rows behind and had no Updated date; [[decisions-and-lessons]] claimed IP-filter/categories never shipped and that the queue rank is FIFO (superseded by 8ah). NEW [[kad-verify-oracle]] entry - the third oracle finally has its own page. Code doc comments (no behavior change): two engine.rs doc blocks were attached to the WRONG items (finish_download's verify rationale sat on FinishCtx; the ServerInfo public-IP privacy note sat on ServerEntry), a RoutingZone.cpp citation was off by 49 lines, the start_kad lock-budget note predated the wave-10 HELLO (~7s not 5s), upload_queue.rs still described the pre-8ah FIFO gate, message.rs's build_hello_req doc still RECOMMENDED the 0x04 request bit that 65a186b removed (it trips aMule's AddContact2 wxFAIL), frame.rs's module doc contradicted the pack rule, kad_id.rs cited a nonexistent SELF_TOLERANCE, and mule-cli documented fetch-complete as smallest-first when it is best-sourced-first + hid serve-file's eserver args + never disclosed that its `<keyword>` doubles as an extension filter. CLAUDE.md test count ~450 -> ~490. |
| 8ak | Reanalysis FIX round: Kad key capture, load gate, decompression bound, serve-path faithfulness, harness | [[kad-verify-oracle]] [[security-model]] | **DONE (2026-08-02, commits 2ab7800 + 8430c52).** Closed the behavioral findings the 8aj audit surfaced, TDD each (RED observed first). **Kad:** every answered request now records the responder through ONE `note_responder` path - `search_source`/`search_keyword_node` were discarding the peer's sender key entirely (so a node we only ever SEARCHED could never be echoed to, silently narrowing the send-side verification the wave-10 work exists for), and `hello()` was discarding its receiver-key verdict. **nodes.dat:** parse aMule's v3 form (the bootstrapEdition dword before the count; edition 1 switches to 25-byte records, RoutingZone.cpp:156-166/228) and REJECT unknown versions - previously a v3 file misparsed into one garbage contact. **Kad UDP:** bound decompression to aMule's `packetLen*10+300` (ClientUDPSocket.cpp:109) instead of the 2MB TCP cap - a ~1.4KB hostile datagram could inflate ~1400x. **Load gate:** nodes.dat contacts are now filtered at LOAD like aMule (Kad2-only, routable ip:port, user ipfilter, no legacy DNS-port node) - the file may have just been DOWNLOADED, and `load_nodes` bypassed the live-layer gates. **obf:** handshake padding capped at 254 rather than a wrapping `as u8` that would desync the whole RC4 stream. **Serve path (both authorities re-read, not assumed):** an unknown-hash OP_REQUESTFILENAME now draws SILENCE like eMule 0.50a (ListenSocket.cpp:374-380, which even tracks repeat askers) and aMule (ClientTCPSocket.cpp:381-385) instead of FNF - which also stopped padMule asserting "I do not have that file" about a file it may hold; a full upload queue likewise answers with silence (aMule's AddClientToQueue just returns, UploadQueue.cpp:508-510); and OP_SETREQFILEID now answers about the hash in THIS packet rather than a previously latched file, matching aMule's per-request GetFileByID. **Harness (the headline):** the Kad oracle's "known ephemeral-port flakiness" was a MISDIAGNOSIS - amuled always bound 4672. The runner read only the stdout redirect while this build writes to CFG/logfile, and grepped `Client UDP socket (extended eMule) at` which amuled never emits (it logs `Created Client UDP-Socket at port N`), so it was blind to "Kad started", to the port line, and to its own PADMULE-ORACLE-VERIFIED success. Fixed both - now **3/3 PASS on the first attempt, no retries, no WARN**. Also: simulate.sh never actually built mule-cli (cargo's `--example` filters targets across ALL named packages - confirmed empirically) and guarded no binaries; differential-test leaked its temp tree; two oracles ran `kill` with no args. **CI:** the iOS build + test workflows now run on pull_request (previously a PR touching only ios/ got zero CI), and the simulator fallback picks a CURRENT iPad device type instead of the retired "iPad Pro (11-inch) (4th generation)". **Tests:** the ffi sharing test made hermetic via offline_engine; the #[ignore]d live test rewritten for the no-auto-connect design it had contradicted since row 8x. Gate: 500 tests, clippy/fmt/ASCII clean; ALL THREE oracles re-run green - Kad verify 3/3, amuled differential byte-for-byte (3 files), reverse oracle (real amuled downloads FROM padMule + serve-side secure-ident). |

Detailed narratives for the completed milestones above (code-fix rounds, the
live three-file completion, the differential-test history, wave notes) live in
[[build-history]] (split 2026-08-01, verbatim).

## Related

- [[build-history]]
- [[protocol-reference]]
- [[ref-ecosystem]]
- [[arch-upstream-amule]]
- [[decisions-and-lessons]]
- [[kad-verify-oracle]]
