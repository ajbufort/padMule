# docs/raw - immutable source material

Read, never edit. Specs, protocol docs, forum captures, and other reference
material land here before being ingested into `docs/wiki/`.

Note: the upstream aMule source itself lives at `/amule-3.0.1/` in the repo
root (it is the working tree being ported), not here.
