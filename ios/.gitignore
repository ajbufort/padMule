# Everything here is generated by CI (.github/workflows/ios-build.yml) and must
# never be committed. Note: .gitignore has no trailing-comment support, so keep
# comments on their own lines.

# uniffi-bindgen output (mule_ffi.swift, mule_ffiFFI.h, module.modulemap)
gen/

# libmule_ffi.a staticlib, staged for the linker (device build + simulator tests)
libs/
libs-sim/

# xcodebuild derived data
build/

# xcodegen output
padMule.xcodeproj/
padMule/Info.plist
